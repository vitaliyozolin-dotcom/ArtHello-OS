import{asc}from"drizzle-orm";import{ensureCoreTables,getDb}from"../../../db";import{assetMaintenance,assets,entities,financialOperations,inventoryEvents,inventoryItems,procurementDeliveries,procurementSuppliers,purchaseOrders,purchaseRequests,supplierOffers,tasks}from"../../../db/schema";import{offerScore,warrantyState}from"../../../lib/procurement";import{getRequestUser}from"../../../lib/request-user";
const readable=new Set(["OWNER","DIRECTOR","REPRESENTATIVE","PROCUREMENT","FINANCE"]);export async function GET(request:Request){if(!getRequestUser(request))return Response.json({error:"Требуется вход"},{status:401});const role=request.headers.get("x-arthello-role")??"";if(!readable.has(role))return Response.json({error:"Нет доступа к закупкам и имуществу"},{status:403});try{await ensureCoreTables();const db=getDb();const[suppliers,requests,offers,orders,deliveries,items,events,assetRows,maintenance,entityRows,operations,allTasks]=await Promise.all([db.select().from(procurementSuppliers),db.select().from(purchaseRequests),db.select().from(supplierOffers),db.select().from(purchaseOrders),db.select().from(procurementDeliveries),db.select().from(inventoryItems),db.select().from(inventoryEvents).orderBy(asc(inventoryEvents.occurredAt)),db.select().from(assets),db.select().from(assetMaintenance),db.select({id:entities.id,displayName:entities.displayName}).from(entities),db.select().from(financialOperations),db.select().from(tasks)]);const minPrice=offers.length?Math.min(...offers.map(x=>x.priceMinor)):0;const payment=operations.find(operation=>orders.some(order=>order.paymentOperationId===operation.id));return Response.json({suppliers,requests,offers:offers.map(x=>({...x,score:offerScore(x,minPrice)})).sort((a,b)=>b.score-a.score),orders,deliveries,items,events,assets:assetRows.map(x=>({...x,warrantyState:warrantyState(x.warrantyUntil,new Date().toISOString().slice(0,10))})),maintenance,entityNames:Object.fromEntries(entityRows.map(x=>[x.id,x.displayName])),payment,tasks:allTasks.filter(x=>x.sourceType==="Обслуживание имущества"),summary:{requests:requests.length,offers:offers.length,stockUnits:items.reduce((s,x)=>s+x.quantity,0),assets:assetRows.length,serviceDue:maintenance.filter(x=>x.status!=="Завершено").length},chain:{requestId:requests[0]?.id??"",approvalId:requests[0]?.approverEntityId??"",offerId:offers[0]?.id??"",supplierId:suppliers[0]?.id??"",orderId:orders[0]?.id??"",deliveryId:deliveries[0]?.id??"",itemId:items[0]?.id??"",assetId:assetRows[0]?.id??"",documentId:deliveries[0]?.actDocumentId??"",paymentId:payment?.id??""},boundary:"Внешний рынок и учётная система ещё не подключены. Показываются только сохранённые заявки, предложения, поставки и связанные финансовые операции."})}catch(error){return Response.json({error:error instanceof Error&&error.message.includes("D1 binding")?"База закупок ещё не подключена":"Не удалось загрузить закупки"},{status:503})}}
