import test from"node:test";import assert from"node:assert/strict";import{aiActionAllowed,canRecordHumanDecision,canRunContract,forecastCash,marginPercent,riskRank,safeAverage}from"../lib/analytics.ts";
test("AI guard blocks every high-impact action",()=>{for(const action of["change_finance","sign_contract","terminate_employee","assign_penalty","medical_diagnosis","accuse_person","delete_primary_data","grant_critical_access"])assert.equal(aiActionAllowed(action),false);assert.equal(aiActionAllowed("create_review_task"),true)});
test("opt-out or disabled contract blocks future runs",()=>{assert.equal(canRunContract("Активен",0),true);assert.equal(canRunContract("Отключён",0),false);assert.equal(canRunContract("Активен",1),false)});
test("cash forecast keeps weighted assumptions and detects gap",()=>{const rows=forecastCash([{forecastDate:"2026-09-01",direction:"Поступление",amountMinor:10000,probability:50},{forecastDate:"2026-09-02",direction:"Списание",amountMinor:20000,probability:100}],10000);assert.equal(rows[0].weightedMinor,5000);assert.equal(rows[1].balanceMinor,-5000);assert.equal(rows[1].isGap,true)});
test("signals sort by severity then confidence",()=>{assert.ok(riskRank("Высокий",60)>riskRank("Средний",99));assert.ok(riskRank("Высокий",90)>riskRank("Высокий",70))});
test("human decisions need evidence and metrics are safe",()=>{assert.equal(canRecordHumanDecision("Проверить вручную","ACT:T-001"),true);assert.equal(canRecordHumanDecision("Ок","нет"),false);assert.equal(safeAverage([70,80,90]),80);assert.equal(marginPercent(1000,750),25)});
