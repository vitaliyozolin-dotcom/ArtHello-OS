import{eq}from"drizzle-orm";import{ensureCoreTables,getDb}from"../../../db";import{assetMaintenance,auditEvents,inventoryEvents,inventoryItems,procurementDeliveries,procurementSuppliers,purchaseOrders,purchaseRequests,supplierOffers,tasks}from"../../../db/schema";import{stockAfter}from"../../../lib/procurement";import{getRequestUser}from"../../../lib/request-user";
const editors=new Set(["OWNER","DIRECTOR","REPRESENTATIVE","PROCUREMENT"]),leaders=new Set(["OWNER","DIRECTOR","REPRESENTATIVE"]);export async function POST(request:Request){const actor=getRequestUser(request);if(!actor)return Response.json({error:"Требуется вход"},{status:401});const role=request.headers.get("x-arthello-role")??"";if(!editors.has(role))return Response.json({error:"Нет прав на действие закупки"},{status:403});try{await ensureCoreTables();const body=await request.json() as Record<string,unknown>,action=clean(body.action,50);if(action==="createRequest")return createRequest(actor,body);if(action==="approveRequest")return approve(actor,role,body);if(action==="selectOffer")return selectOffer(actor,body);if(action==="receiveDelivery")return receive(actor,body);if(action==="inventoryEvent")return inventory(actor,body);if(action==="createMaintenanceTask")return maintenanceTask(actor,body);return Response.json({error:"Неизвестное действие"},{status:400})}catch(error){return Response.json({error:error instanceof Error?error.message:"Действие не выполнено"},{status:500})}}

async function createRequest(actor:string,body:Record<string,unknown>){const itemName=clean(body.itemName,160),unit=clean(body.unit,120),justification=clean(body.justification,400),quantity=Number(body.quantity),budgetMinor=Number(body.budgetMinor);if(itemName.length<4||!unit||justification.length<8||!Number.isInteger(quantity)||quantity<=0||!Number.isInteger(budgetMinor)||budgetMinor<=0)return Response.json({error:"Заполните предмет, подразделение, количество, бюджет и обоснование"},{status:400});const id=`REQ-${crypto.randomUUID().slice(0,8).toUpperCase()}`;const[row]=await getDb().insert(purchaseRequests).values({id,requesterEntityId:"EMP-T-PROC-001",unit,itemName,quantity,budgetMinor,needBy:"2026-09-15",status:"На согласовании",justification}).returning();await audit(actor,"procurement.request_created","purchase_request",id,{quantity,budgetMinor});return Response.json({request:row},{status:201})}
async function approve(actor:string,role:string,body:Record<string,unknown>){if(!leaders.has(role))return Response.json({error:"Заявку согласует руководитель"},{status:403});const id=clean(body.requestId,80),db=getDb();const[row]=await db.select().from(purchaseRequests).where(eq(purchaseRequests.id,id)).limit(1);if(!row)return Response.json({error:"Заявка не найдена"},{status:404});if(row.status==="Согласована")return Response.json({request:row,reused:true});const[updated]=await db.update(purchaseRequests).set({status:"Согласована",approverEntityId:"EMP-T-004",approvedAt:new Date().toISOString(),updatedAt:new Date().toISOString()}).where(eq(purchaseRequests.id,id)).returning();await audit(actor,"procurement.request_approved","purchase_request",id,{budgetMinor:row.budgetMinor});return Response.json({request:updated})}
async function selectOffer(actor:string,body:Record<string,unknown>){const requestId=clean(body.requestId,80),offerId=clean(body.offerId,80),db=getDb();const[request]=await db.select().from(purchaseRequests).where(eq(purchaseRequests.id,requestId)).limit(1),[offer]=await db.select().from(supplierOffers).where(eq(supplierOffers.id,offerId)).limit(1);if(!request||!offer)return Response.json({error:"Заявка или предложение не найдены"},{status:404});if(request.status!=="Согласована")return Response.json({error:"Сначала нужно согласование"},{status:409});const[supplier]=await db.select().from(procurementSuppliers).where(eq(procurementSuppliers.id,offer.supplierId)).limit(1);if(!supplier?.contractId)return Response.json({error:"У поставщика нет договора"},{status:409});const existingId=`ORD-${requestId.replace("REQ-","")}`;const[existing]=await db.select().from(purchaseOrders).where(eq(purchaseOrders.id,existingId)).limit(1);if(existing)return Response.json({order:existing,reused:true});const[order]=await db.insert(purchaseOrders).values({id:existingId,requestId,offerId,supplierId:offer.supplierId,orderNumber:`PO-${requestId.slice(-3)}/26`,amountMinor:offer.priceMinor,status:"Заказан",orderedAt:new Date().toISOString(),expectedAt:"2026-09-05",contractId:supplier.contractId}).returning();await db.update(supplierOffers).set({status:"Выбрано"}).where(eq(supplierOffers.id,offerId));return Response.json({order},{status:201})}
async function receive(actor:string,body:Record<string,unknown>){const deliveryId=clean(body.deliveryId,80),qualityNote=clean(body.qualityNote,400),db=getDb();if(qualityNote.length<8)return Response.json({error:"Укажите результат приёмки"},{status:400});const[row]=await db.select().from(procurementDeliveries).where(eq(procurementDeliveries.id,deliveryId)).limit(1);if(!row)return Response.json({error:"Поставка не найдена"},{status:404});if(row.status==="Принято")return Response.json({delivery:row,reused:true});const[updated]=await db.update(procurementDeliveries).set({status:"Принято",acceptedQuantity:row.quantity,acceptedBy:actor,qualityNote,deliveredAt:new Date().toISOString()}).where(eq(procurementDeliveries.id,deliveryId)).returning();await audit(actor,"procurement.delivery_accepted","delivery",deliveryId,{quantity:row.quantity,qualityNote});return Response.json({delivery:updated})}
async function inventory(actor:string,body:Record<string,unknown>){const itemId=clean(body.itemId,80),eventType=clean(body.eventType,30),quantity=Number(body.quantity),toLocation=clean(body.toLocation,120),documentId=clean(body.documentId,80),db=getDb();if(!["Приёмка","Выдача","Перемещение","Списание","Инвентаризация"].includes(eventType)||!Number.isInteger(quantity)||quantity<=0)return Response.json({error:"Некорректное складское событие"},{status:400});const[item]=await db.select().from(inventoryItems).where(eq(inventoryItems.id,itemId)).limit(1);if(!item)return Response.json({error:"Номенклатура не найдена"},{status:404});let next;try{next=stockAfter(item.quantity,eventType,quantity)}catch{return Response.json({error:"Недостаточно остатка"},{status:409})}const id=`INV-${crypto.randomUUID().slice(0,8).toUpperCase()}`;const[event]=await db.insert(inventoryEvents).values({id,itemId,eventType,quantity,fromLocation:item.warehouse,toLocation,documentId,occurredAt:new Date().toISOString(),actor}).returning();await db.update(inventoryItems).set({quantity:next,warehouse:eventType==="Перемещение"&&toLocation?toLocation:item.warehouse,updatedAt:new Date().toISOString()}).where(eq(inventoryItems.id,itemId));return Response.json({event,quantity:next},{status:201})}
async function maintenanceTask(actor:string,body:Record<string,unknown>){const id=clean(body.maintenanceId,80),db=getDb(),key=`ASSET_MAINT:${id}`;const[row]=await db.select().from(assetMaintenance).where(eq(assetMaintenance.id,id)).limit(1);if(!row)return Response.json({error:"Обслуживание не найдено"},{status:404});const[existing]=await db.select().from(tasks).where(eq(tasks.automationKey,key)).limit(1);if(existing)return Response.json({task:existing,reused:true});const[task]=await db.insert(tasks).values({title:`${row.maintenanceType} · ${row.assetId}`,owner:"Специалист закупок T-P01",dueDate:row.scheduledAt,priority:"Средний",status:"Входящие",sourceType:"Обслуживание имущества",sourceId:id,description:"Проверить гарантию, подрядчика, результат и закрывающий документ",assigneeEntityId:"EMP-T-PROC-001",kind:"Автозадача",automationKey:key,requiresApproval:false,createdBy:actor}).returning();await db.update(assetMaintenance).set({relatedTaskId:task.id,status:"Задача создана"}).where(eq(assetMaintenance.id,id));return Response.json({task},{status:201})}
async function audit(actor:string,action:string,entityType:string,entityId:string,payload:unknown){await getDb().insert(auditEvents).values({actor,action,entityType,entityId,payload:JSON.stringify(payload)})}function clean(v:unknown,m:number){return typeof v==="string"?v.trim().slice(0,m):""}
