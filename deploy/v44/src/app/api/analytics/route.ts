import{asc,desc}from"drizzle-orm";import{ensureAnalyticsDemoBootstrap,ensureCoreTables,getDb}from"../../../db";import{aiModelRuns,aiOptOuts,aiProcessContracts,analyticsMetricDefinitions,analyticsSignals,clientLifecycles,educationProgress,financeForecastItems,financeReconciliationIssues,financialOperations,foodProduction,foodShipments,foodShifts,hrEmployees,integrationConflicts,integrationConnections,safetyFaults,strategyProjects}from"../../../db/schema";import{forecastCash,marginPercent,riskRank,safeAverage}from"../../../lib/analytics";import{getRequestUser}from"../../../lib/request-user";
const readers=new Set(["OWNER","DIRECTOR","REPRESENTATIVE","ANALYTICS","FINANCE"]);
export async function GET(request:Request){if(!getRequestUser(request))return Response.json({error:"Требуется вход"},{status:401});const role=request.headers.get("x-arthello-role")??"";if(!readers.has(role))return Response.json({error:"Нет доступа к управленческой аналитике"},{status:403});try{await ensureCoreTables();await ensureAnalyticsDemoBootstrap();const db=getDb();const[metricDefinitions,signals,contracts,runs,optOuts,operations,forecastItems,reconciliation,lifecycles,progress,employees,faults,shipments,production,shifts,projects,connections,conflicts]=await Promise.all([db.select().from(analyticsMetricDefinitions).orderBy(asc(analyticsMetricDefinitions.category)),db.select().from(analyticsSignals),db.select().from(aiProcessContracts).orderBy(asc(aiProcessContracts.name)),db.select().from(aiModelRuns).orderBy(desc(aiModelRuns.ranAt)),db.select().from(aiOptOuts).orderBy(desc(aiOptOuts.createdAt)),db.select().from(financialOperations),db.select().from(financeForecastItems).orderBy(asc(financeForecastItems.forecastDate)),db.select().from(financeReconciliationIssues),db.select().from(clientLifecycles),db.select().from(educationProgress),db.select().from(hrEmployees),db.select().from(safetyFaults),db.select().from(foodShipments),db.select().from(foodProduction),db.select().from(foodShifts),db.select().from(strategyProjects),db.select().from(integrationConnections),db.select().from(integrationConflicts)]);const monthlyMap=new Map<string,{period:string;receiptsMinor:number;outflowsMinor:number;netMinor:number;factRows:number;syntheticRows:number}>();for(const item of operations){const row=monthlyMap.get(item.period)??{period:item.period,receiptsMinor:0,outflowsMinor:0,netMinor:0,factRows:0,syntheticRows:0};if(item.direction==="Поступление")row.receiptsMinor+=item.amountMinor;else row.outflowsMinor+=item.amountMinor;row.netMinor=row.receiptsMinor-row.outflowsMinor;if(item.operationKind==="XLSX_AGGREGATE")row.factRows++;else row.syntheticRows++;monthlyMap.set(item.period,row)}const forecast=forecastCash(forecastItems,120000000),floor=forecast.reduce((min,item)=>Math.min(min,item.balanceMinor),120000000),revenue=shipments.reduce((sum,item)=>sum+item.revenueMinor,0),cost=production.reduce((sum,item)=>sum+item.materialCostMinor,0)+shifts.reduce((sum,item)=>sum+item.rateMinor,0);const rankedSignals=[...signals].sort((a,b)=>riskRank(b.severity,b.confidence)-riskRank(a.severity,a.confidence));const domains=[...new Set(signals.map(item=>item.domain))].map(domain=>({domain,total:signals.filter(item=>item.domain===domain&&item.status!=="Закрыт").length,high:signals.filter(item=>item.domain===domain&&item.severity==="Высокий"&&item.status!=="Закрыт").length})).sort((a,b)=>b.high-a.high||b.total-a.total);return Response.json({metricDefinitions,signals:rankedSignals,contracts:contracts.map(item=>({...item,activeOptOuts:optOuts.filter(row=>row.contractId===item.id&&row.status==="Активен").length})),runs:runs.slice(0,40),optOuts,owner:{cashAprilMinor:monthlyMap.get("2026-04")?.netMinor??0,cashForecastFloorMinor:floor,nextPaymentsMinor:lifecycles.reduce((sum,item)=>sum+item.nextPaymentMinor,0),highRiskFamilies:lifecycles.filter(item=>item.churnRiskBand==="Высокий").length,averageProgress:safeAverage(progress.map(item=>item.score)),activeEmployees:employees.filter(item=>item.status==="Работает").length,openSafetyFaults:faults.filter(item=>item.status!=="Закрыт").length,foodMarginPercent:marginPercent(revenue,cost),projectsAtRisk:projects.filter(item=>item.status==="Под риском").length,openDataIssues:reconciliation.filter(item=>item.status!=="Закрыто").length+conflicts.filter(item=>item.status!=="Закрыт").length,verifiedLiveSources:connections.filter(item=>item.verifiedTransfer&&item.isEnabled&&item.lastSuccessAt).length},charts:{cash:[...monthlyMap.values()].sort((a,b)=>a.period.localeCompare(b.period)),forecast,risks:domains},sourceCoverage:{fact:["Атлас ОДДС.xlsx · агрегаты 2026-01…04","Контроль качества исходного ОДДС"],synthetic:["Продажи и семьи","Обучение","HR","Питание","Безопасность","Проекты"],unavailable:["Банки","CRM","электронный дневник","рекламные платформы","ЭДО/1С","СКУД/камеры"]},boundary:"Панель является опубликованным тестовым снимком на 21 августа 2026. Только ОДДС и контроль исходных XLSX основаны на предоставленных файлах; остальные прогнозы и сигналы синтетические. Медицинские данные полностью исключены.",modelBoundary:"Внешняя AI-модель не вызывается: TEST-RULES-v0.1 проверяет контракты, объяснимость и человеческое решение без автоматических high-impact действий."})}catch(error){return Response.json({error:error instanceof Error&&error.message.includes("D1 binding")?"Аналитическая база ещё не подключена":"Не удалось загрузить аналитику"},{status:503})}}
