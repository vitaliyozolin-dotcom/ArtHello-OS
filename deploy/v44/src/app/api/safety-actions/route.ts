import{eq}from"drizzle-orm";import{ensureCoreTables,getDb}from"../../../db";import{auditEvents,safetyChecks,safetyEquipment,safetyFaults,safetyIncidents,safetyNextChecks,safetyRepairs,tasks}from"../../../db/schema";import{nextSafetyCheck}from"../../../lib/safety";import{getRequestUser}from"../../../lib/request-user";
const editors=new Set(["OWNER","DIRECTOR","REPRESENTATIVE","SAFETY"]);export async function POST(request:Request){const actor=getRequestUser(request);if(!actor)return Response.json({error:"Требуется вход"},{status:401});const role=request.headers.get("x-arthello-role")??"";if(!editors.has(role))return Response.json({error:"Нет прав на действие безопасности"},{status:403});try{await ensureCoreTables();const body=await request.json()as Record<string,unknown>,action=clean(body.action,50);if(action==="createFaultTask")return faultTask(actor,body);if(action==="completeRepair")return completeRepair(actor,body);if(action==="recordIncident")return incident(actor,body);if(action==="scheduleCheck")return scheduleCheck(actor,body);return Response.json({error:"Неизвестное действие"},{status:400})}catch(error){return Response.json({error:error instanceof Error?error.message:"Действие не выполнено"},{status:500})}}
async function faultTask(actor:string,body:Record<string,unknown>){const id=clean(body.faultId,80),db=getDb(),key=`SAFETY_FAULT:${id}`;const[fault]=await db.select().from(safetyFaults).where(eq(safetyFaults.id,id)).limit(1);if(!fault)return Response.json({error:"Неисправность не найдена"},{status:404});const[existing]=await db.select().from(tasks).where(eq(tasks.automationKey,key)).limit(1);if(existing)return Response.json({task:existing,reused:true});const[task]=await db.insert(tasks).values({title:`Устранить неисправность · ${fault.equipmentId}`,owner:"Ответственный по безопасности",dueDate:"2026-08-22",priority:fault.severity,status:"Входящие",sourceType:"Неисправность безопасности",sourceId:id,description:fault.description,assigneeEntityId:"EMP-T-SAFE-001",kind:"Автозадача",automationKey:key,requiresApproval:true,createdBy:actor}).returning();await db.update(safetyFaults).set({relatedTaskId:task.id,status:"В работе"}).where(eq(safetyFaults.id,id));await audit(actor,"safety.fault_task_created","safety_fault",id,{taskId:task.id});return Response.json({task},{status:201})}
async function completeRepair(actor:string,body:Record<string,unknown>){const id=clean(body.repairId,80),result=clean(body.result,400),act=clean(body.actDocumentId,100),db=getDb();if(result.length<8||act.length<5)return Response.json({error:"Для завершения нужны результат и акт"},{status:400});const[row]=await db.select().from(safetyRepairs).where(eq(safetyRepairs.id,id)).limit(1);if(!row)return Response.json({error:"Ремонт не найден"},{status:404});if(row.status==="Завершён")return Response.json({repair:row,reused:true});const completedAt="2026-08-21T18:00:00Z",scheduledAt=nextSafetyCheck(completedAt,30);const[updated]=await db.update(safetyRepairs).set({completedAt,result,actDocumentId:act,status:"Завершён"}).where(eq(safetyRepairs.id,id)).returning();const[fault]=await db.select().from(safetyFaults).where(eq(safetyFaults.id,row.faultId)).limit(1);if(fault){await db.update(safetyFaults).set({status:"Устранена"}).where(eq(safetyFaults.id,fault.id));await db.update(safetyEquipment).set({status:"Работает",nextCheckAt:scheduledAt}).where(eq(safetyEquipment.id,fault.equipmentId));await db.insert(safetyNextChecks).values({id:`SAFE-NEXT-${crypto.randomUUID().slice(0,8).toUpperCase()}`,equipmentId:fault.equipmentId,sourceRepairId:id,scheduledAt,checkType:"После ремонта",responsibleEntityId:"EMP-T-SAFE-001",status:"Запланирована"})}await audit(actor,"safety.repair_completed","safety_repair",id,{result,act,scheduledAt});return Response.json({repair:updated,scheduledAt})}
async function incident(actor:string,body:Record<string,unknown>){const description=clean(body.description,500),systemId=clean(body.systemId,80),objectId=clean(body.objectEntityId,80),category=clean(body.category,80)||"Наблюдение",severity=clean(body.severity,30)||"Средняя",db=getDb();if(description.length<8||!systemId||!objectId)return Response.json({error:"Укажите систему, объект и описание"},{status:400});const id=`SAFE-INC-${crypto.randomUUID().slice(0,8).toUpperCase()}`;const[row]=await db.insert(safetyIncidents).values({id,objectEntityId:objectId,systemId,happenedAt:"2026-08-21T18:00:00Z",category,severity,description,response:"Назначена первичная проверка",status:"Открыт"}).returning();await audit(actor,"safety.incident_recorded","safety_incident",id,{systemId,objectId,severity});return Response.json({incident:row},{status:201})}
async function scheduleCheck(actor:string,body:Record<string,unknown>){const equipmentId=clean(body.equipmentId,80),scheduledAt=clean(body.scheduledAt,10),db=getDb();if(!/^\d{4}-\d{2}-\d{2}$/.test(scheduledAt))return Response.json({error:"Укажите дату проверки"},{status:400});const[equipment]=await db.select().from(safetyEquipment).where(eq(safetyEquipment.id,equipmentId)).limit(1);if(!equipment)return Response.json({error:"Оборудование не найдено"},{status:404});const id=`SAFE-CHK-${crypto.randomUUID().slice(0,8).toUpperCase()}`;const[row]=await db.insert(safetyChecks).values({id,equipmentId,objectEntityId:"OBJ-T-001",checkType:"Внеплановая",scheduledAt,result:"Ожидает",evidence:"Назначено вручную",responsibleEntityId:"EMP-T-SAFE-001",status:"Запланирована"}).returning();await audit(actor,"safety.check_scheduled","safety_check",id,{equipmentId,scheduledAt});return Response.json({check:row},{status:201})}
async function audit(actor:string,action:string,entityType:string,entityId:string,payload:unknown){await getDb().insert(auditEvents).values({actor,action,entityType,entityId,payload:JSON.stringify(payload)})}function clean(v:unknown,m:number){return typeof v==="string"?v.trim().slice(0,m):""}
