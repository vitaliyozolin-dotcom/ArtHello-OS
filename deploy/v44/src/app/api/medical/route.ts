import{desc,eq,like}from"drizzle-orm";import{ensureCoreTables,getDb}from"../../../db";import{auditEvents,medicalAccessGrants,medicalActions,medicalCases,medicalDocuments,medicalIncidents,medicalRestrictions}from"../../../db/schema";import{medicalAccess,medicalExpiry}from"../../../lib/medical";import{getRequestUser}from"../../../lib/request-user";
export async function GET(request:Request){const actor=getRequestUser(request);if(!actor)return Response.json({error:"Требуется вход"},{status:401});try{await ensureCoreTables();const db=getDb(),role=request.headers.get("x-arthello-role")??"";const[grant]=await db.select().from(medicalAccessGrants).where(eq(medicalAccessGrants.principalRef,`ROLE:${role}`)).limit(1);if(!medicalAccess(role,grant,"2026-08-21")){await db.insert(auditEvents).values({actor,action:"medical.access_denied",entityType:"medical_scope",entityId:`ROLE:${role||"UNKNOWN"}`,payload:JSON.stringify({scope:"NONE",reason:"separate_grant_required"})});return Response.json({error:"Медицинские данные скрыты. Нужен отдельный действующий допуск."},{status:403})}await db.insert(auditEvents).values({actor,action:"medical.records_viewed",entityType:"medical_scope",entityId:grant.id,payload:JSON.stringify({scope:grant.scope,fields:"minimum-necessary",purpose:"workspace-view"})});const[documents,restrictions,cases,incidents,actions,audit]=await Promise.all([db.select().from(medicalDocuments),db.select().from(medicalRestrictions),db.select().from(medicalCases),db.select().from(medicalIncidents),db.select().from(medicalActions),db.select().from(auditEvents).where(like(auditEvents.action,"medical.%")).orderBy(desc(auditEvents.id)).limit(30)]);return Response.json({documents:documents.map(x=>({...x,expiryBand:medicalExpiry(x.validUntil,"2026-08-21")})),restrictions,cases,incidents,actions,audit:audit.map(x=>({id:x.id,action:x.action,entityType:x.entityType,entityId:x.entityId,createdAt:x.createdAt})),summary:{documents:documents.length,expiring:documents.filter(x=>medicalExpiry(x.validUntil,"2026-08-21")==="Истекает").length,activeRestrictions:restrictions.filter(x=>x.status==="Активно").length,openCases:cases.filter(x=>x.status!=="Закрыт").length,pendingActions:actions.filter(x=>x.status!=="Выполнено").length},boundary:"Только синтетические обезличенные записи. Реальные медицинские файлы не загружены; защищённое медицинское хранилище не подключено. Данные исключены из общих отчётов."})}catch(error){return Response.json({error:error instanceof Error&&error.message.includes("D1 binding")?"Защищённая база ещё не подключена":"Не удалось открыть медицинский контур"},{status:503})}}
