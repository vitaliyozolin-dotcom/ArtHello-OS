import{eq}from"drizzle-orm";import{ensureCoreTables,getDb}from"../../../db";import{auditEvents,businessEvents,strategyDeviations,strategyKpis,strategyResults,tasks}from"../../../db/schema";import{canCloseStrategyDeviation,kpiState}from"../../../lib/strategy";import{getRequestUser}from"../../../lib/request-user";
const editors=new Set(["OWNER","DIRECTOR","REPRESENTATIVE","PROJECTS"]);export async function POST(request:Request){const actor=getRequestUser(request);if(!actor)return Response.json({error:"Требуется вход"},{status:401});const role=request.headers.get("x-arthello-role")??"";if(!editors.has(role))return Response.json({error:"Нет прав на изменение стратегии"},{status:403});try{await ensureCoreTables();const body=await request.json()as Record<string,unknown>,action=clean(body.action,50);if(action==="createEvent")return createEvent(actor,body);if(action==="recordEventResult")return recordEventResult(actor,body);if(action==="updateKpiActual")return updateKpi(actor,body);if(action==="createCorrectiveTask")return correctiveTask(actor,body);if(action==="closeDeviation")return closeDeviation(actor,body);return Response.json({error:"Неизвестное действие"},{status:400})}catch(error){return Response.json({error:error instanceof Error?error.message:"Действие не выполнено"},{status:500})}}
async function createEvent(actor:string,body:Record<string,unknown>){const title=clean(body.title,180),projectId=clean(body.projectId,80),db=getDb();if(title.length<5||!projectId)return Response.json({error:"Укажите проект и событие"},{status:400});const id=`EVENT-${crypto.randomUUID().slice(0,8).toUpperCase()}`;const[row]=await db.insert(businessEvents).values({id,projectId,title,eventAt:"2026-08-29T11:00:00Z",location:"Школа · тестовая площадка",responsibleEntityId:"EMP-T-PROJ-001",budgetMinor:30000000,actualMinor:0,status:"Запланировано"}).returning();await audit(actor,"strategy.event_created","business_event",id,{projectId,title});return Response.json({event:row},{status:201})}
async function recordEventResult(actor:string,body:Record<string,unknown>){const id=clean(body.eventId,80),result=clean(body.result,400),score=Number(body.feedbackScore),db=getDb();if(result.length<8||!Number.isInteger(score)||score<0||score>100)return Response.json({error:"Нужны результат и оценка 0–100"},{status:400});const[event]=await db.select().from(businessEvents).where(eq(businessEvents.id,id)).limit(1);if(!event)return Response.json({error:"Событие не найдено"},{status:404});if(event.status==="Проведено")return Response.json({event,reused:true});const[updated]=await db.update(businessEvents).set({status:"Проведено",result,feedbackScore:score}).where(eq(businessEvents.id,id)).returning();const resultId=`STR-RES-${crypto.randomUUID().slice(0,8).toUpperCase()}`;await db.insert(strategyResults).values({id:resultId,projectId:event.projectId,eventId:id,resultType:"Событие",metricName:"Обратная связь",metricValue:score,unit:"%",evidence:result,recordedAt:"2026-08-21T18:00:00Z"});await audit(actor,"strategy.event_result_recorded","business_event",id,{score,resultId});return Response.json({event:updated,resultId})}
async function updateKpi(actor:string,body:Record<string,unknown>){const id=clean(body.kpiId,80),actual=Number(body.actualValue),forecast=Number(body.forecastValue),db=getDb();if(!Number.isInteger(actual)||!Number.isInteger(forecast))return Response.json({error:"Значения KPI должны быть целыми"},{status:400});const[row]=await db.select().from(strategyKpis).where(eq(strategyKpis.id,id)).limit(1);if(!row)return Response.json({error:"KPI не найден"},{status:404});const state=kpiState(row.targetValue,actual,forecast),[updated]=await db.update(strategyKpis).set({actualValue:actual,forecastValue:forecast,varianceValue:state.variance,status:state.status,updatedAt:"2026-08-21T18:00:00Z"}).where(eq(strategyKpis.id,id)).returning();await audit(actor,"strategy.kpi_updated","strategy_kpi",id,{actual,forecast,...state});return Response.json({kpi:updated})}
async function correctiveTask(actor:string,body:Record<string,unknown>){const id=clean(body.deviationId,80),db=getDb(),key=`STRATEGY_DEVIATION:${id}`;const[row]=await db.select().from(strategyDeviations).where(eq(strategyDeviations.id,id)).limit(1);if(!row)return Response.json({error:"Отклонение не найдено"},{status:404});const[existing]=await db.select().from(tasks).where(eq(tasks.automationKey,key)).limit(1);if(existing)return Response.json({task:existing,reused:true});const[task]=await db.insert(tasks).values({title:`Корректирующее действие · ${row.kpiId}`,owner:"Руководитель проектов",dueDate:"2026-08-28",priority:"Высокий",status:"Входящие",sourceType:"Отклонение KPI",sourceId:id,description:`${row.explanation}. Решение: ${row.decision}`,assigneeEntityId:"EMP-T-PROJ-001",kind:"Корректирующее действие",automationKey:key,requiresApproval:true,createdBy:actor}).returning();await db.update(strategyDeviations).set({relatedTaskId:task.id,status:"В работе"}).where(eq(strategyDeviations.id,id));await audit(actor,"strategy.corrective_task_created","strategy_deviation",id,{taskId:task.id});return Response.json({task},{status:201})}
async function closeDeviation(actor:string,body:Record<string,unknown>){const id=clean(body.deviationId,80),evidence=clean(body.evidence,400),decision=clean(body.decision,300),db=getDb();if(!canCloseStrategyDeviation(evidence,decision))return Response.json({error:"Нужны доказательство и решение"},{status:400});const[row]=await db.select().from(strategyDeviations).where(eq(strategyDeviations.id,id)).limit(1);if(!row)return Response.json({error:"Отклонение не найдено"},{status:404});const[updated]=await db.update(strategyDeviations).set({explanation:`${row.explanation} · Доказательство: ${evidence}`,decision,status:"Закрыто"}).where(eq(strategyDeviations.id,id)).returning();await audit(actor,"strategy.deviation_closed","strategy_deviation",id,{evidence,decision});return Response.json({deviation:updated})}
async function audit(actor:string,action:string,entityType:string,entityId:string,payload:unknown){await getDb().insert(auditEvents).values({actor,action,entityType,entityId,payload:JSON.stringify(payload)})}function clean(v:unknown,m:number){return typeof v==="string"?v.trim().slice(0,m):""}
