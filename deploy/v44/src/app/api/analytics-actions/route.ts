import{eq}from"drizzle-orm";import{ensureAnalyticsDemoBootstrap,ensureCoreTables,getDb}from"../../../db";import{aiModelRuns,aiOptOuts,aiProcessContracts,analyticsSignals,auditEvents,tasks}from"../../../db/schema";import{canRecordHumanDecision,canRunContract}from"../../../lib/analytics";import{getRequestUser}from"../../../lib/request-user";
const editors=new Set(["OWNER","DIRECTOR","REPRESENTATIVE","ANALYTICS"]),now="2026-08-21T10:30:00Z";
export async function POST(request:Request){const actor=getRequestUser(request);if(!actor)return Response.json({error:"Требуется вход"},{status:401});const role=request.headers.get("x-arthello-role")??"";if(!editors.has(role))return Response.json({error:"Нет прав на запуск и решения"},{status:403});try{await ensureCoreTables();await ensureAnalyticsDemoBootstrap();const body=await request.json()as Record<string,unknown>,action=clean(body.action,50);if(action==="runScenario")return runScenario(actor,body);if(action==="createSignalTask")return signalTask(actor,body);if(action==="recordDecision")return recordDecision(actor,body);if(action==="optOut")return optOut(actor,body);if(action==="restoreContract")return restoreContract(actor,body);return Response.json({error:"Неизвестное действие"},{status:400})}catch(error){return Response.json({error:error instanceof Error?error.message:"Действие не выполнено"},{status:500})}}
async function runScenario(actor:string,body:Record<string,unknown>){const id=clean(body.contractId,100),db=getDb(),[contract]=await db.select().from(aiProcessContracts).where(eq(aiProcessContracts.id,id)).limit(1);if(!contract)return Response.json({error:"AI-контракт не найден"},{status:404});const optOuts=await db.select().from(aiOptOuts).where(eq(aiOptOuts.contractId,id));if(!canRunContract(contract.status,optOuts.filter(item=>item.status==="Активен").length))return Response.json({error:"Сценарий отключён или действует отказ"},{status:409});const runId=`AI-RUN-${crypto.randomUUID().slice(0,8).toUpperCase()}`,summary=`Контрольный запуск «${contract.name}»: сформирована объяснимая рекомендация без изменения бизнес-данных`;const[run]=await db.insert(aiModelRuns).values({id:runId,contractId:id,ranAt:now,modelVersion:"TEST-RULES-v0.1",status:"Завершён",inputSnapshotRef:"ANALYTICS-SNAPSHOT-T-0821",outputType:"Контрольная рекомендация",outputSummary:summary,confidence:60,costMinor:contract.costMinor,explanation:`Входы: ${contract.inputData}. Разрешено: ${contract.allowedActions}. Решение остаётся за ${contract.humanOwner}.`,isSynthetic:true}).returning();await audit(actor,"analytics.scenario_run","ai_contract",id,{runId,modelVersion:run.modelVersion});return Response.json({run},{status:201})}
async function signalTask(actor:string,body:Record<string,unknown>){const id=clean(body.signalId,100),db=getDb(),key=`ANALYTICS_SIGNAL:${id}`;const[signal]=await db.select().from(analyticsSignals).where(eq(analyticsSignals.id,id)).limit(1);if(!signal)return Response.json({error:"Сигнал не найден"},{status:404});const[existing]=await db.select().from(tasks).where(eq(tasks.automationKey,key)).limit(1);if(existing)return Response.json({task:existing,reused:true});const[task]=await db.insert(tasks).values({title:`Проверить сигнал · ${signal.title}`,owner:"Человек-владелец решения",dueDate:"2026-08-24",priority:signal.severity,status:"Входящие",sourceType:"Аналитический сигнал",sourceId:id,description:`${signal.explanation}. Рекомендация: ${signal.recommendation}`,assigneeEntityId:"EMP-T-004",kind:"Human review",automationKey:key,requiresApproval:true,createdBy:actor}).returning();await db.update(analyticsSignals).set({relatedTaskId:task.id,status:"В работе"}).where(eq(analyticsSignals.id,id));await audit(actor,"analytics.signal_task_created","analytics_signal",id,{taskId:task.id});return Response.json({task},{status:201})}
async function recordDecision(actor:string,body:Record<string,unknown>){const id=clean(body.signalId,100),decision=clean(body.decision,400),evidence=clean(body.evidence,400),db=getDb();if(!canRecordHumanDecision(decision,evidence))return Response.json({error:"Нужны решение человека и доказательство"},{status:400});const[signal]=await db.update(analyticsSignals).set({humanDecision:decision,decisionEvidence:evidence,status:"Закрыт",decidedAt:now}).where(eq(analyticsSignals.id,id)).returning();if(!signal)return Response.json({error:"Сигнал не найден"},{status:404});await audit(actor,"analytics.human_decision_recorded","analytics_signal",id,{decision,evidence});return Response.json({signal})}
async function optOut(actor:string,body:Record<string,unknown>){const id=clean(body.contractId,100),reason=clean(body.reason,400),scopeRef=clean(body.scopeRef,100)||"ALL",db=getDb();if(reason.length<8)return Response.json({error:"Укажите основание отказа"},{status:400});const[contract]=await db.select().from(aiProcessContracts).where(eq(aiProcessContracts.id,id)).limit(1);if(!contract||!contract.optOutAllowed)return Response.json({error:"Отказ для сценария недоступен"},{status:409});const[existing]=await db.select().from(aiOptOuts).where(eq(aiOptOuts.contractId,id)).limit(1);if(existing?.status==="Активен")return Response.json({optOut:existing,reused:true});const optId=`AI-OPTOUT-${crypto.randomUUID().slice(0,8).toUpperCase()}`,[record]=await db.insert(aiOptOuts).values({id:optId,contractId:id,scopeType:scopeRef==="ALL"?"Сценарий":"Субъект",scopeRef,requestedBy:actor,reason,status:"Активен",stopsProcessingAt:now,historicalDataPolicy:contract.historicalDataPolicy}).returning();if(scopeRef==="ALL")await db.update(aiProcessContracts).set({status:"Отключён",updatedAt:now}).where(eq(aiProcessContracts.id,id));await audit(actor,"analytics.opt_out_recorded","ai_contract",id,{optId,scopeRef});return Response.json({optOut:record},{status:201})}
async function restoreContract(actor:string,body:Record<string,unknown>){const id=clean(body.contractId,100),reason=clean(body.reason,400),db=getDb();if(reason.length<8)return Response.json({error:"Нужно основание возобновления"},{status:400});await db.update(aiProcessContracts).set({status:"Активен",updatedAt:now}).where(eq(aiProcessContracts.id,id));const rows=await db.select().from(aiOptOuts).where(eq(aiOptOuts.contractId,id));for(const row of rows.filter(item=>item.status==="Активен"))await db.update(aiOptOuts).set({status:"Отозван"}).where(eq(aiOptOuts.id,row.id));await audit(actor,"analytics.contract_restored","ai_contract",id,{reason});return Response.json({restored:true})}
async function audit(actor:string,action:string,entityType:string,entityId:string,payload:unknown){await getDb().insert(auditEvents).values({actor,action,entityType,entityId,payload:JSON.stringify(payload)})}function clean(value:unknown,max:number){return typeof value==="string"?value.trim().slice(0,max):""}
