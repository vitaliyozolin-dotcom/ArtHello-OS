FROM arthello-os-api:local

USER root
WORKDIR /school

COPY deploy/offline-runtime-v3.part-* /tmp/runtime-parts/

RUN cat /tmp/runtime-parts/offline-runtime-v3.part-* > /tmp/school-runtime.tar.gz \
    && printf '%s  %s\n' \
      '9125b43319706f4bf3a9a45813b50a0495e61bef5b73f3305a1b97717406b349' \
      '/tmp/school-runtime.tar.gz' \
      | sha256sum --check --strict \
    && tar -xzf /tmp/school-runtime.tar.gz -C /school \
    && rm -f /tmp/school-runtime.tar.gz \
    && rm -rf /tmp/runtime-parts \
    && mkdir -p /data /backups \
    && chown -R node:node /school /data /backups

ENV NODE_ENV=production
ENV NEXT_TELEMETRY_DISABLED=1
ENV PORT=3000
ENV HOSTNAME=0.0.0.0
ENV DATABASE_PATH=/data/school-1-11.sqlite

USER node
EXPOSE 3000

ENTRYPOINT ["./scripts/container-entrypoint.sh"]
CMD ["node", "server.js"]
