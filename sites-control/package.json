{
  "name": "@workspace/sites-control",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/validate-artifact.mjs",
    "dev": "vite --config vite.config.mjs",
    "test": "pnpm run build && node --test tests/*.test.mjs",
    "typecheck": "node --check main.js && node --check scripts/build.mjs && node --check scripts/validate-artifact.mjs"
  },
  "devDependencies": {
    "vite": "catalog:"
  }
}
