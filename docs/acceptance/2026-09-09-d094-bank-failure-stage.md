# D094 — Fixed bank failure-stage observation

Application source is prepared for a future release; accepted live R12 is unchanged. The actual D091 observation at 2026-09-09T09:05:27.182Z (run34332656641/job102404738568) reports four accounts, twelve import rows, zero bank/financial operations, three scheduler failures, callback status500, updated09:01:19Z and next ordinary tick10:01:19.940Z. This does not identify an upstream response.

Three application overlays add a lexical observer to the existing scheduled callback, persist/return nullable failureStage and preserve all auth/eligibility/claims/leases/backoff/callback/finally semantics. Only eleven fixed operation names are possible; no error properties/messages/stack, provider identifiers, credentials or financial payload are exported. Non-error outcomes clear the stage; skipped/superseded results retain their prior shape. An escaping release error retains its original finally precedence.

Four diagnostic/test overlays extend the existing fixed SQL report and strict launcher by failureStage/failureStageState only. SQL returns a stage only for an error outcome and an exact allowlisted literal; missing/invalid data stays null and explicitly classified. The output schema rejects arbitrary or inconsistent pairs before printing. Existing completion criteria, exit2 for incomplete issues and final runtime consumer verification remain unchanged.

Local evidence:21 focused scheduler and actual assembled-route tests,39 SQLite read-only aggregate tests,21 launcher subprocess tests pass. The first launcher run in this partial scratch candidate lacked the unchanged live-d1-identity.mjs test dependency and stopped before the probe; adding the byte-identical existing dependency resolved it. That dependency is excluded from publication. Independent three-overlay review verified actual route assembly against accepted raw source and pending patch, strict service auth, exception identity and finally precedence. Full exact-source CI, final seven-overlay review and actual future runtime observation remain required. No controlled retry, banking request, direct database write or production deployment occurs in this source change.
