# D087 / PR371 — counts-only bank observation

Status: preparation; production diagnostic outcome `not_run`. This note indexes evidence for the bounded D075-authorized observation. It does not assert financial reconciliation or completion of the original business acceptance.

| Identity | Actual evidence |
|---|---|
| Accepted deployed source / tree | `77f26ec9bcba7233f39d5e8cb9f59c276bc8c1ed` / `ac3fcaac06acf33bf9ee32e438d0c040adb38fd7` |
| Accepted application image | `sha256:5a39c36001cb79abe6d0bc8d691275b58cdec5456e7d13d95fc717eba6702284` |
| Protected release | Run `34326582961`, attempt `1`, deploy `102386111240`, outcome `success`, completed `2026-09-09T08:11:09Z` |
| Release proof | [R12 protected release](https://github.com/vitaliyozolin-dotcom/ArtHello-OS/actions/runs/34326582961) |
| Candidate / post-public browser | [R12 deploy: candidate SSO at 2026-09-09T08:10:46.386Z](https://github.com/vitaliyozolin-dotcom/ArtHello-OS/actions/runs/34326582961/job/102386111240) / [R12 deploy: post-public SSO at 2026-09-09T08:11:02.888Z](https://github.com/vitaliyozolin-dotcom/ArtHello-OS/actions/runs/34326582961/job/102386111240) |
| Backup evidence | [R12 deploy: backup state verified](https://github.com/vitaliyozolin-dotcom/ArtHello-OS/actions/runs/34326582961/job/102386111240) — retain its actual proven scope |
| Fresh base / diagnostic PR-head / tree | `77f26ec9bcba7233f39d5e8cb9f59c276bc8c1ed` / `recorded in PR371` / `recorded in PR371` |
| Diagnostic merge (`CHECKED_SOURCE_SHA`) | `pending` — separate from accepted deployed source |
| Exact-source hosted checks | Quality `pending`, Proof `pending`, diagnostic Verify `pending`; outcome `pending` |
| Protected observation | Run `not_run`, attempt `not_run`, job `not_run`; observed `not_run`; not_run |

The coordinator's safe R12 log confirms the active release above and runtime fingerprint `a28b64b3a57f96eb8e23c18d888d033f70d90f92b2ef98df5cc7b07c920d153a`. Both natural SSO passes verified employee, Education and School identity, with **medical** as the denied module. `feedbackVisible` establishes visibility only. Backup state was verified with one history record and `nextAt=2026-09-10T00:15:00+00:00`; this does not prove the future scheduled execution or restore. Bundle job `102385622798` and deploy job `102386111240` succeeded. The coordinator verified this production evidence; this local documentation task did not fetch it independently.

The previously reviewed candidate passed 57 local tests: 26 aggregate, 27 metadata/consumer and 4 launcher. These templates did not rerun tests. That evidence does not establish hosted PASS on a subsequently rebased source. Quality remains unchanged. The protected trigger retains the `D075: read-only production data` merge-title prefix, owner/main restrictions, `production-ru` and both shared locks.

Runtime selection requires the exact accepted public application and its exact R7 backup worker, immutable identity, private release state, gateway evidence, public route and canonical D1 file descriptors. The helper uses only the read-only/no-copy canonical volume, UID1000, network:none, read-only rootfs and existing limits. No upstream bank requests, SQL writes, data copy, credentials, resync, payments, role changes or production deployment occur.

Observed counters/statuses: not_run; actual bank counts and bounded completion are not yet observed. Until the actual report exists, record `not_run` rather than fixture numbers. Completion requires the actual JSON status and successful final consumer re-observation; partial logs alone are not a completed observation.

The bank window starts `2026-09-01` and ends on the observation date in UTC. Counts cover all observed account keys and legal entities for the connection, saved statements, booked RUB transactions, register links and duplicate/missing external identities. Exactly four valid unique account keys and one legal entity are required for bounded completion; the query does not filter the observations to four keys. `accountsWithContainingStatementInLatestRun` is an additional count of latest ready statements containing the window. It does not relax exact-window `coveredInLatestRun` or `accountsWithMatchingTransactionCount`. Production supplies no `syncNotBefore`: `freshness=not_requested` cannot prove a new resync after release.

`bounded_checks_complete` and `READONLY_FINISHED=aggregate_observation_not_live_acceptance` have only the scope above. Money/DDS reconciliation, upstream completeness, fresh resync, UI/API permissions, requests, selective AlfaCRM and manual/automatic backup history/restore remain subject to their own evidence. The backup worker marker establishes identity and read-only consumption, not backup history.

Next evidence step: Refresh current main after the completed R12 run; rebase and verify PR371, pin the proved accepted runtime, then obtain the protected counts-only observation. Money/DDS and the remaining business checks retain their own evidence criteria. Append later actual outcomes; preserve the preparation history and do not replace missing proof with overall business PASS.
