# D092 — Browser assembly preparation after D089 smoke failure

Status: fix preparation; 4 real pnpm assembly regression tests PASS. Hosted image/Chromium follow-up not_run; no visual PASS. The technical trigger remains `D089: hosted Content Tasks visual`.

Observed failure: run `34331175805`, job `102399900660`, head `3632008d57f149b9e300b22f502e6dc48a5326ac`. Accepted application artifact verification/import passed. pnpm installed `playwright-core@1.62.1` and Docker build exited 0; smoke failed with `ERR_MODULE_NOT_FOUND` from `/opt/arthello-e2e/run.mjs`. Fixture was not reached. Artifacts: 0; PNGs: 0 of 14.

Real local pnpm `11.7.0` reproduced directories `0700` and files `0600` under `umask 077`; symlinks stayed inside the context. A later install under `022` can reuse cached `0600` files. The failed hosted run did not retain mode/link diagnostics. UID1000 execution is unavailable locally (`OS EINVAL`), and Docker is unavailable; local reproduction does not prove the repaired image or Chromium.

Proposed assembly: copy eight frozen public inputs into an owned isolated browser context; run locked copy import under scoped `umask 022`; reject absolute/dangling/escaping links and special files before normalizing only that context for readable files and traversable directories under root-owned Docker COPY plus UID1000. Keep global launcher `077`, private credential paths, original Dockerfile/browser/lock, accepted R12 pin, original harness/adapter/tests and existing trigger unchanged.

Application pin: source `77f26ec9bcba7233f39d5e8cb9f59c276bc8c1ed`, tree `ac3fcaac06acf33bf9ee32e438d0c040adb38fd7`, producer run `34326274447`. Local evidence: 4 assembly tests PASS using real pnpm `11.7.0` and frozen inputs/lock. Relocation plus deletion of the install context still permits a real `playwright-core` import. Private permissions are detected; absolute/escaping/dangling links and shared hardlinks fail before chmod; the caller's `077` and an existing context are preserved. Next evidence: independent exact-source review/CI, then actual hosted smoke and full scoped 14-PNG output with manifest/scoped-result/evidence. Inspect screenshots before accepting the bounded scenario. Preserve all limits on synthetic APIs, form persistence, content generation, numbering behavior and financial/integration proof. Do not change main while a protected bank observation is active.
