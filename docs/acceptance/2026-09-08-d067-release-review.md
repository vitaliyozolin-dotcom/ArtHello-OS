# R5 / technical D067 release review

The owner authorized completing the six ArtHello fixes and publishing a working product autonomously. Main before this PR is ce7c50ba367fca14d71669305bf4e82b791ce4f7; prior refactoring changes are preserved. Architectural decision D-071 is separate from technical release label D067.

Actual read-only diagnostic34207628005/job102000795971 observed12 School/backend snapshots after failed D066. Only the order of two complete Mounts changed; normalizing that order yields the same full School hash for all12 observations, while the shared backend hash never changes. School identity/image/source/StartedAt remained healthy and unchanged. Relay, egress network and prior configuration directory were absent after cleanup; own state root0700 and lock0644 were valid. Both public health endpoints returned200 from the gateway. Original failed-pair object values were not retained; this independent interval reproduces the defect without reconstructing that pair. Full sanitized actual evidence accompanies this review.

R5 changes only complete-Mounts array ordering in the fingerprint and uses its own bundle/owner/config identity. Every field, unknown field and duplicate is retained; Config, HostConfig and network arrays remain exact. Tests reject real Source/Destination/RW/count/duplicate changes and exercise the full mocked controller through permuted inspections. A new real hosted Docker fixture inspects a container with two readonly mounts repeatedly and after adding a shared-network peer. TLS/DNS/UID/source-ACL smoke remains mandatory.

New consumer is pinned to planned PR360, branch codex/school-arthello-recovery-r5-20260908 and parent ce7c50ba367fca14d71669305bf4e82b791ce4f7. Old R3/R4 code, all historical consumers/replay guards and shared queue serialization remain unchanged. R5 reuses the unchanged R4 bootstrap/normalizer and schema2 receipt validation; fresh schema3 R5 natural-browser evidence is required before image loading/cutover. Backup/clone, durable public-write boundary and delayed Tochka activation remain intact.

Included Alfa correctness closes actual code defects: unknown balance is rejected while preserving existing projection/lineage; confirmed zero remains valid; import success time requires a complete result without rejected rows; rejected chunks invalidate the cursor/preview so later batches cannot hide them; preview expires after30minutes before upstream/credential use. No live import is enabled and no new monetary meaning, scheduler or family UI is claimed. Actual credentials/coverage, balance units and family snapshot display remain unaccepted.

Validation:56 local release tests pass (17 gates,29 controller,10 Node), all10 workflow shell steps and2 smoke scripts parse, and all15 technical paths are covered by both Verify triggers. Alfa route/SQLite suites pass40/40 plus targeted ESLint. Independent reviews report no remaining P1/P2 after correcting a provisional Ruby contract reference to the unchanged R4 bootstrap test. Actual Ruby/Docker/full application and exact PR/main CI remain mandatory.

Frozen technical manifest SHA256161a09157fff66c7b53673cabe8c764d49836aff13c5c3d8f775dc62d7abf3b3; relay manifest26d1e31aff6fe40b7dbc7bd38b31244c8cb951e09c6f05dd12e9088f1b6ea930; Alfa manifest2ac001b9da6a34ef0af420f6b2022ea96727f51cc87133179781c40a7c3fb80f. New repair config2ba0ad1ce530db9c9abb622e5727e6345ab74a85699e121b718d9a0405b1f40a.

No new ArtHello cutover or passing SSO has happened at this review. A cloud-browser502 is separate from gateway health200 and remains an acceptance limitation to resolve. Real bank reconciliation, personnel authorization, feedback persistence, manual/daily backup and staged Alfa synchronization are not marked complete before live evidence.
