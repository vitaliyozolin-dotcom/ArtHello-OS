# D093 — Employee own feedback and backup denial preparation

Status: revised PR387 source preparation. Original hosted employee browser fixture passed (run34333434145/job102407142571), but frozen R8/R9 bundle-equality contracts rejected changes to the canonical D076 workflow. Restore canonical workflow/flow/smoke/test byte-for-byte to base b4c39d5af8ab348882759c48c0f82dbe810a8065. Add a separate employee-controls workflow, Dockerfile, flow/smoke/test. The dedicated Dockerfile keeps the same pinned browser base/package/runtime and explicitly maps the new flow/smoke inputs to in-image canonical paths. Local 74 browser/retirement/canonical tests pass; full Ruby contracts and new hosted real image/Chromium remain CI gates. Live employee acceptance is pending.

The normal flow uses the ordinary authenticated ArtHello session to open «Разработчикам», verify GET `/api/developer-feedback?scope=mine`, select «Мои обращения», wait for the own list to finish, require no «Все обращения» control, and close the dialog. It then requires HTTP 403 from GET `/api/settings/backups` with redirects rejected and response bodies unread. No form fill, Send or mutation/network exception is added. The existing employee and denied navigation checks remain; the Education/School SSO checks follow these employee controls. Output adds only fixed `feedbackDialog`, `feedbackOwnList`, `feedbackAllHidden` and `backupApiDenied` verification flags.

Opening feedback can lazily initialize its server schema. This is ordinary authenticated navigation, not the strict read-only D075 database probe. The scoped result does not establish full permissions, positive backup status/history, manager integration access, financial completeness or successful message delivery.

The only new housekeeping target is the accepted R12 browser: source `77f26ec9bcba7233f39d5e8cb9f59c276bc8c1ed`, image `sha256:e07d75e5964218b4382d98e255c5f17829dc6a4429ccef3060fb833d4cc3b61f`, fingerprint `3bd2ca4a24dad705752258568b501414a640cdca95cfd51d36c951eb1c6bbf30`. Recovery artifact `10094111549`, name `server-browser-77f26ec9bcba7233f39d5e8cb9f59c276bc8c1ed-34326582961-1`, size `640811234`, digest `sha256:b4b05bbddb30dbd6a0745a6d5e54f9f1b6199a4e5166d12a511fd2b27aedf821`, main/source/run/repository identity were independently reconciled to the accepted evidence. Artifact metadata read on 2026-09-09 said expired=false and expiry `2026-09-11T08:01:35Z`; validity must be freshly checked at execution.

Current image presence, consumers and capacity are unverified. Successful ID-and-tag absence causes no removal or artifact read. A present image requires exact identity, unused status including stopped containers, repeated matching unexpired artifact and main checks; deletion addresses only the fixed immutable browser ID without force/prune, verifies absence, and remeasures capacity. The new helper implements no application, worker, container, volume or data deletion; existing ephemeral browser cleanup remains unchanged. The new standalone workflow performs this R12 retirement before the first capacity gate/download. It does not perform unrelated R8 retirement; the original canonical D076 workflow and recovery contracts remain unchanged.

Publication base is current main `b4c39d5af8ab348882759c48c0f82dbe810a8065`, tree `ef5a22b22caaa46c28c7b18ed3a3d5374bd6f47a`. Shared documents preserve their complete D092 base prefix. The new technical trigger is `D093: employee controls`; preserve owner/repository/Quality push-main/first-attempt/environment/locks and accepted R12 application. Keep main stable through the terminal protected result and final browser cleanup. Record exact source/tree, hosted CI, run/job and actual result before claiming bounded acceptance complete. The initial failed PR checks are retained as history; do not merge until corrected exact-head checks pass.
