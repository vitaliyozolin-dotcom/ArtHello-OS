# D101: Tochka R15 preparation

Status: PREPARED, NOT DEPLOYED. No production or banking acceptance is claimed.
Owner: Vitaliy. Executor: Codex. Existing permission covers reviewed source
changes, merges, protected release and ordinary read-only observations.

## Verified starting point

D100 PR403 merged as `9862a6e863d4d791c00ddeaba9480154ad5b8c4d`, tree
`7a08a2e88e8ea45c2ef1e9631f62ba5c612d6034`. Its exact PR head passed Quality
`34440928812`, Proof `34440928740`, V52 `34440928818`, R14/R13/R12 `34440928953`.
All materialized application source was preserved. See the D100 source report.

Accepted live remains R13 `f5fa3e46e3510e6fc98ae4455f4b499c0ba30695`.
R14 source `d44137d8342b7eacec510f9f70ffeba6c3bf4f3a`, protected run
`34391105865` attempt 2 / deploy job `102631320528`, failed before cutover.
Cleanup completed. The application artifact action stopped without its normal
download-completion/digest lines, and the next step failed before its first
checksum output. One of three file-presence checks failed; the exact missing
file and the root cause of the action's early success are not established.

The hosted application artifact `10119642989` was independently downloaded:
ZIP SHA-256 `d4c0b95307cd2f2eed5bd2868ecd72f4fe53eb01c170235da8d171667d2c208c`,
173098451 bytes; three members and their checksums were valid. This proves the
hosted bytes, not the files that reached the gateway. No third R14 replay.

## Delivery and capacity preparation

The new application downloader validates the owner/main/attempt-one successful
producer, complete artifact inventory, expiry, source, size and ZIP digest.
It streams into its own run/attempt directory, accepts exactly three ordinary
members, checks CRCs, and rechecks current main before extraction. The GitHub
bearer never reaches the signed storage URL. Network retries are bounded and
do not retry any cutover. It does not import Docker or change application data.
The existing image, evidence, fingerprint, capacity and gzip checks must still
run after delivery. Thirteen local downloader tests pass, including bounded network recovery, refusal of integrity failure, main movement and pre-existing output directories.

The R14 browser was imported in attempt 2. Its actual immutable identity is
`sha256:19a8671fd03ba31bcfc3ddc7d479dceeabcfbcb836b61bfa7fda17fbf8f77f1a`,
fingerprint `666c2af60d83da845ca39e8930b659fd70e7a9030eb5060ef5ba90031fa24f40`.
Recoverable artifact `10119716232`, producer run `34391105865` / bundle attempt
1, is named `server-browser-d44137d8342b7eacec510f9f70ffeba6c3bf4f3a-34391105865-1`:
640801625 bytes, ZIP digest
`0835c0a93f375c847d52d9e5178b4f5570e450cba1a61407b976536a46872102`.
Fresh metadata on 2026-09-10 reports expiry `2026-09-11T18:48:33Z`.
The new fixed retirement adapter preserves the prior exact-ID, aliases,
fingerprint, unused-container, recoverable-artifact and current-main checks.
It can remove only that browser image and must recheck capacity afterward.
It cannot remove application images, containers, volumes or backups.
Fifteen local retirement tests pass.

R15 history retains the R14 durable protocol (schema 14), the actual accepted
R13 history and all replay boundaries. Only the new consumer path, the fixed
retirement step and forbidden failed R14 source/run change. Twenty-three local
history tests pass. These fixture results are not live receipts.

## Remaining release gate

The candidate is PR404, branch `codex/tochka-r15-20260910`, parent
`9862a6e863d4d791c00ddeaba9480154ad5b8c4d`; technical merge prefix
`D101: guarded Tochka R15`. A pinned recipe applies exactly 17 counted changes
to the byte-identical R14 controller archived at `deploy/v52/recovery-r15/r14-controller.yml`.
The old active controller is retired; active workflow count remains 14.
The historical R14 runner restores that exact controller only inside its own
fixture, together with the five existing comparison inputs and verifier YAML.
All current frozen protocol helper digests remain enforced.

R15 uses materialized checkout paths, a new history consumer, exact unused
R14 browser retirement, and bounded ZIP delivery. The original controller
from application verification/import through snapshot, cutover, natural
candidate/after acceptance, backup adoption and final cleanup is byte-identical,
except for the run/attempt artifact-directory suffix. This is enforced by a
regression assertion in addition to the complete pinned transformation.
The new source verifier checks 68 exact source inputs and the hosted verification
workflow. Hosted Ruby verifies that verifier and its pins; production uses Python
and does not acquire a new Ruby/package-manager dependency. Full R12/R13/R14
historical tests remain separate required jobs, with R15 as a fourth job.

Before merge: review final SHA/tree/full diff and require successful exact-head
Quality, Proof, V52 and all four continuation jobs. Do not substitute new source
into the old R14 run or trigger the separate legacy v44 push deployment.

Before cutover: fresh R13 live identity, School, current main, both existing
locks, capacity, fresh snapshot and healthy preserved R12 backup/history.
After seal/auth/public boundary: no restore of an old database. Require real
candidate and after-public browser receipts plus backup and School evidence.
Then bind the read-only observation to actual receipts, preserve bank backoff,
and verify all four accounts from 2026-09-01 to the observation date: real bank
and linked DDS rows, matching income/expense kopecks, zero duplicates, missing
or shared links and financialMismatchRows. PR399 remains UNBOUND meanwhile.

No new services, credentials, roles, sessions, payments, messages, manual SQL,
forced resync or raw bank/person exports. Existing platform cost is unknown.
