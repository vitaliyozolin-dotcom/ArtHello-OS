# Read-only D066 fingerprint diagnosis

D066 run34206196494 attempt1/job101996459827 passed exact release gates and normalized the existing own lock from0664 to0644 at2026-09-08T08:46:30Z. The controller then refused school_changed_during_setup before starting the relay. ArtHello image loading/cutover were skipped. Its cleanup code was executed, but current object absence must be observed independently. Do not rerun the failed repair.

This main-only diagnostic takes12 bounded pairs of exact School/backend Docker inspections. It compares the original controller hashes, fixed component hashes and a diagnostic hash sorting only complete Mounts entries. Duplicate counts and every value are preserved. Only allowlisted field paths and hashes are printed; raw environment, paths, labels, application data and credentials are not exposed. Original failed-pair values were not retained, so current observations cannot reconstruct that pair.

Existing safe lock/home/state/relay/egress metadata checks confirm actual cleanup state. Two fixed public /api/health requests from the gateway use normal TLS, no redirects and15-second timeouts, reporting only allowlisted status fields. They distinguish gateway observations from the fresh cloud browser's502 response without changing either service.

Authorization is the owner's existing request to diagnose, fix and publish autonomously. The workflow executes only signed current main with parent055773c73f214b43caddb643cbccbe4b3d3d1755, checks the exact failed D066 job and skipped cutover, and uses the existing production Environment, fixed School SSH fingerprint and shared School concurrency queue. No PR code/checkout, configuration writes, lock acquisition, permission changes, Docker mutations, sessions or application writes occur.

Author validation:8 focused tests and5 shell blocks pass. Root independently reviewed fixed output fields, full-entry sorting, unknown-field masking, unchanged safe filesystem checks, exact source/transport boundaries and redirect rejection; no remaining P1/P2 in this bounded diagnostic. Frozen four-file manifest SHA256:c9538d5236caae0ea943ebd016925979cb60b81a3f0d0910d042f03cef15d1e8.

This PR is diagnostic only. It does not claim successful repair, SSO, deployment or completion of the six user scenarios. Current concurrent refactoring changes are preserved in full; a subsequent repair needs a separately verified candidate.
