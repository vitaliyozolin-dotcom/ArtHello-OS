# D110 — защищённый выпуск статей и разнесения: подготовка

Поручение Виталия: «Проверяй и выпускай в прод». D108 PR410 уже принят в source fdb93556bf3cff27849a872c3f3bba1ae712364b/tree f37059d94d65211803e29ae9a04aaaa4920a8765. Это не production receipt. Последний принятый production — R16 cb990279e070fddbfa9a0adbd21b56de25b85588, run34461449594 attempt1.

## Изолированная проверка

Существующий hosted-only Verify v52 получает дополнительный обязательный шаг после сборки точного image. Тот же app image, настоящий D1/SQLite, Chromium с проверенным sandbox, новый пустой контейнер/volume с CI label и internal network. Ни production environment/secrets, ни новые live sessions/роли не используются. Fixture требует пустых таблиц до трёх явно синтетических банковских/финансовых строк и выделенной локальной CI credential. Вход выполняется формой; session/cookie injection и API mocks отсутствуют. Фиксированный HTTPS origin отображается только на localhost контейнера; другие назначения и лишние POST отказывают. Запросы двух font origins из фактического stylesheet блокируются без сети; проверяется обычный системный шрифт. Исходные server responses/CSRF/разрешения и запись приложения сохраняются.

Сценарий: пустой справочник → черновик → утверждение → предпросмотр ИНН AND назначение → ручное ДДС/ОПиУ → reload → точные копейки и переход из ДДС → тот же сценарий на 390 px → архив → сохранение исторического назначения. Независимое чтение fixture DB сравнивает hash всех bank rows, связи/суммы и точное число записей аудита. Все fixtures удаляются штатным cleanup; только синтетические PNG и bounded result остаются в CI artifact.

Локально проверены синтаксис scripts и отказ seeder без fixture context. Настоящий браузерный результат ещё не получен. Cloud Browser после одного reload показывает 502 до login; это не доказательство состояния production и не повод менять доступы.

## Выпуск

Новый R17 adapter подготовлен для PR412, ветка codex/finance-r17-20260910, squash prefix `D110: guarded finance R17`, parent `93cb27839584f7f59d2ef5f1f0ecafc5b6bbd682`. 28 counted transformations из byte-identical archived R16 controller, 139 source inputs. Допустимы ровно шесть ранее reviewed изменений из frozen 97 inputs D105/D107/D108; остальные guards остаются byte-identical. Новый history consumer проверяет R16 → R15 → R13 → R12 → older. Старый active R16 controller архивируется без изменения bytes; active budget остаётся 14. Новый protected R17 ещё не запускался. Старые R16/15/14 controllers/pins/history не переопределяются и успешные runs не повторяются. Перед merge нужны actual R16 graph/receipts, полный reviewed новый source contract, exact-head CI и отдельный защищённый release с прежними backup/snapshot/seal/auth/current-main/identity/capacity/dual locks. После публичной границы восстановление старой БД запрещено. Проверка новой функции не создаёт выдуманные рабочие статьи и не разносит реальные платежи без утверждённого назначения.

Критерий завершения: successful R17 receipt + candidate/after-public natural acceptance + сохранённый School/backup/history + подтверждённые финансовые инварианты на bounded read-only наблюдении. Стоимость отдельного запуска неизвестна, новые платные услуги не подключаются. Ответственный — Виталий.

## Actual CI findings before final review

First browser run34480268293 could only report broad natural_login failure. Bounded diagnostics run34480848651 proved natural login succeeded and navigation failed: UI exposes an anchor «Деньги», while the test looked for a «Финансы» button. Synthetic failure PNG was downloaded, ZIPdigest/CRC checked, and visually reviewed. The same run recorded blocked external font request; stylesheet confirms the two known font origins. The test now uses the actual navigation and explicitly blocks those optional font GETs while retaining fail-closed handling of every other unexpected destination/write. No application/authentication changes were made for this finding. The final successful browser proof is still required.

Actual accepted R16 history is recorded in `.github/scripts/fixtures/r17-accepted-r16-history.json`: terminal run34461449594 attempt1, complete bundle/deploy graph, signed commit source/tree. Backup live context a43ac4fa996ffcf18854afc83c3245552fe804e80abb94a25a0fa515cd2a9eef and app image sha256:556216a36f878fad5b99bc1e2cca0158ebdcc3f7d2ee23fda191285d4ad4ef44 come from the finished deploy receipt. Browser retirement is limited to unused actual R16 browser sha256:104c3c4b35d236f020111e6dfc0bf2bfd0d99ca9a083cf373424747f2f6846b9, fingerprint738757d0a2ef0805c5f139fb48e9afc079cfdae874eaf6364eab838a983daa04. Recovery artifact10145756776, size640802987, ZIPsha256aa4ea805899ff06244cfad407aa1ea0393edf0c4b6e73030fcfc656c17477073 expires2026-09-12T09:36:49Z. All original twice-fresh recoverability, identity, aliases/consumer, and capacity gates remain.

Browser run34481672143 reached the empty finance catalog and the article-name field; it stopped before the first article write. The downloaded synthetic screenshot proves the actual form. Native select locators now use their combobox role and form scope; per-action fixed diagnostics distinguish missing/ambiguous selectors from API status/assertions. This is still pending execution evidence, not a runtime defect claim.

Review also found cleanup was armed before CI namespace-absence checks. Cleanup now becomes active only after all three absence checks; a dynamic fake-Docker test proves each occupied container/volume/network causes no create/remove commands, while a later owned-create failure triggers only the exact CI cleanup. Production cleanup/controller bytes are unaffected.

## Compatible current main

While PR412 was open, independently reviewed PR413 was squash-merged as signed current main93cb27839584f7f59d2ef5f1f0ecafc5b6bbd682/tree6cbf75f5002addf12c3e3a1e6522fb4d1c76c8a7. D109 is occupied by that accepted source decision, so this release decision is D110. Conflicts were limited to DECISIONS, hosted history wrapper and its workflow pins. Both decisions and the full main source changes are preserved. R17's parent is now exact93cb278.

The sixth changed frozen input is main's runtime-server.mjs. Existing D109 evidence proves its entire diff consists of two separate optional Atlas bindings; the wrapper verifies their exact presence and verifies all other runtime bytes against frozen R16. R17 supplies no Atlas secret/configuration and creates no role/session. Existing School entry retains its accessible name and route. No Atlas source was changed in this release preparation; current canonical source plus the full hosted tests and natural School acceptance remain mandatory.

## Exact public digest scanner finding

Actual Quality34482898978 / secret-scan102889848396 failed on generic-api-key at recovery-r17/source-pins.json: the extracted value is exactly SHA-256 b7276c29aa18837416f49b9d18d7899f5b9886e0548554e2ee037c59f5ea38e3 of the public finance API regression test. It was independently recomputed and equals the inventory pin; no credential was found or exposed. The scanner image and Quality workflow remain unchanged. `.gitleaks.toml` extends all default rules and adds one AND condition: exact inventory path plus exact public hash. No ignored commits, whole-path exemption, disabled rule or wildcard value is introduced. The behavior is documented by the pinned [Gitleaks v8.30.0 configuration](https://github.com/gitleaks/gitleaks/blob/v8.30.0/README.md#configuration).

The hosted test runs the same immutable scanner against four disposable cases: default scanner must reproduce the original finding; only the exact public digest at the exact path is accepted; a different generated value at that path and the same public digest at another path must both be detected. Static configuration/hash checks pass locally; actual scanner behavior remains pending hosted execution.

## Actual browser lifecycle and portal CSS fix

Run34482898849 reached the end of create/approve/assign/reload/DDS/mobile/archive: all nine expected finance writes returned success, exact DDS cents and historical archived selection were observed, no page errors. Final browser result still failed because its strict transport treated a blocked optional metadata asset at the historical metadataBase as unexpected. layout.tsx declares only /favicon.svg and /og.png there; CI now explicitly blocks those exact GET paths, without contacting Sites or allowing arbitrary destinations.

Artifact10154515969 ZIPsha25694fddf76943610f20a0b1807ff65a22a4a395d8e5f7b89cad7fa3ff7565cd3fb was downloaded, CRC/digest checked and all four desktop/mobile PNG reviewed. The mobile allocation PNG revealed a real styling defect: the body portal is outside .ahFinancePage, so its form rules did not apply. The only new application file is FinanceWorkspace.ds.css: scope allocation rules to .finance-drawer, use the existing primary color token, provide 44px controls, one mobile column and internal article-panel padding. Bank/auth/runtime logic is unchanged. Browser assertions now require the actual computed grid and mobile control sizes, in addition to the existing lifecycle and exact DB verification. New full browser/DB acceptance is still pending.

Actual final-build run34483751194 caught the page-prefix isolation contract on the portal CSS. The operation dialog now carries its own `ahFinanceOperationDrawer` scope; all 19 allocation selectors use that scope. Existing `.finance-drawer` layout remains. The isolation assertion is unchanged. Only the dialog class and allocation CSS change application behavior; current canonical source includes those exact bytes. Final browser assertions still require a one-column mobile form and 44px controls.

Browser run34484508244 confirmed build and 797 tests; mobile measurement failed during the existing 180ms `central-pop` scale animation. Final fixture waits for actual finite dialog animations before unchanged >=44px layout assertions and emits only bounded dimensions on failure. It also permits the actual fresh-owner default dashboard PUT /api/dashboard-layout (<=3 calls, <=4096 bytes, schema1/widgets, actual HTTP200 required), under the same isolated normal auth/CSRF; all other non-finance writes remain refused. This normal hydration call is separate from the nine finance lifecycle writes. No production code changed in this refinement.
