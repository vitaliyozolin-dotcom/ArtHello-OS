# D080 preparation — not a release approval

Current main: 582edaf1a66a953edb2d61e03040d1a13e70a0ad. No application cutover has been performed.

The permanent password works. Protected D076 run34314770145 / attempt1 / job102348936617 reported blocked, education_forbidden, liveAcceptance=not_passed at2026-09-09T05:29:05.827Z. Its browser image was retired. The current source already contains the branch-scoped explicit Education grant fix; the old-live full-PASS prerequisite creates a circular release dependency.

This branch prepares a new D080 protocol under the owner's standing release instruction. It is not operational: release PR binding remains0, candidate continuation and the full contract are unfinished, and the required real Caddy/Chromium checks have not yet passed. D078 is not silently relaxed. D080 must be finalized in DECISIONS and the project passport before activation.

Intended sequence: preserve all source/CI/clone/backup checks; install an exact ArtHello maintenance route after a durable no-snapshot-restore boundary; keep ordinary requests and bank activation closed; permit the real test browser through a temporary per-hop Authorization header and retain only the exact normal PKCE code-exchange POST exception; require full same-candidate natural browser acceptance; open the normal route and repeat the test. School routes, roles and passwords are unchanged. Exchange uses one-time code + PKCE + current grants, not a shared-secret check.

The temporary header is sent to Chromium over stdin, scoped to the exact ArtHello origin with CDP per-hop interception, removed before the application, and absent from School. It never replaces application authentication. The hosted HTTPS fixture now requires normal ArtHello and School cookies created by actual server responses, along with real303 redirects. The original one-login, network, denied-module and sandbox checks remain.

After real authentication starts, failures preserve the candidate database and independent School state. A reviewed candidate-only continuation is still being implemented; this draft must not be armed without it. After public activation, snapshot restoration and candidate-only replay are forbidden.

Verified locally so far: browser policy27 tests, state16 tests and independent exact state review, maintenance8 tests, baseline5 tests, receipt8 tests. The maintenance fixture contains118 actual HTTP assertions but Caddy is unavailable locally; it must run on a hosted runner. These are preparation results, not live acceptance.

Outstanding: complete and review continuation/runtime identity checks; bind fresh candidate receipts to context and maintenance start; complete controller contract and hosted gates; perform real Caddy and Chromium fixtures; finalize D080 and append-only passport; exact PR/SHA/tree review and CI; protected release; post-release business acceptance including all four Tochka accounts, selective AlfaCRM and backups.
