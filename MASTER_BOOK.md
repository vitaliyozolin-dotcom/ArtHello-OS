# ArtHello OS — Master Book

## Дополнение A.11 — exact correction candidate и граница owner verification

Исполняемая версия третьего correction cycle зафиксирована commit
`025f82973807c03c90c558136b3a508aa17ed345`, tree
`6fa44f49c99c9f21e05550ae8621f61a87dce4a1`, Sites version 28,
deployment `appgdep_6a66b291be588191909e6daff1fc23d1` со статусом
`succeeded`. Access policy повторно подтверждена как `custom`, один
разрешённый владелец, ноль групп.

Sites официально передаёт workspace identity в
`oai-authenticated-user-email`. Protected `ARTHELLO_OWNER_EMAIL` и email
единственного разрешённого Sites account совпадают. Unit matrix доказывает
`200` для этой identity и `403` для отсутствующей/чужой identity. Машинный
Sites bypass удаляет подделанный caller header, а служебный cloud renderer
передаёт собственную redacted identity; его `403` не является проверкой
реальной сессии владельца. Live URL не открывался во внутреннем браузере по
требованию проекта. Поэтому окончательный production-browser `200` должен
быть подтверждён владельцем на своём устройстве; до этого факт не объявляется
проверенным.

Post-deploy machine status безопасно подтвердил существующее состояние D1:
active atomic batch отсутствует, публикация маркируется
`legacy_unattested`, восемь operational AlfaCRM datasets пусты, платёжные
действия выключены. Эти legacy rows не переаттестовывались и новые PII не
передавались. Их замена одним digest-bound atomic snapshot возможна только
после отдельного информированного разрешения владельца.

## Дополнение A.10 — серверная аттестация и единая owner identity

Независимый Ревизор проверил checkpoint v27 (`f0eea01f0d27ac5afdacbd71c9753401222577b9`, tree `48fe2669cf7ebf28fe5ed125117abd71b35cc52a`) и выдал `FAIL`; Координатор подтвердил `NO-GO`. Причины: incremental AlfaCRM batch мог выглядеть terminal, SHA-256 только возвращал присланную строку, Sites identity не совпадала с backend owner identity, а UI показывал raw JavaScript error. V27 оставлен только как закрытая диагностическая оболочка; новый PII import не выполнялся.

Третий correction cycle вводит обязательные инварианты:

- terminal AlfaCRM publication разрешена только для `mode = full_sandbox_read_only`, `completed`, `entitiesFailed = 0`, `incompleteScopes = 0` и полного согласованного scope inventory;
- `incremental_discovery_read_only` никогда не является terminal materialization;
- publisher передаёт counts и digest каждого из 14 datasets, а Worker после блокировки staging сам канонизирует и SHA-256-хеширует все staged rows, сверяет каждый dataset и общий snapshot до изменения live tables;
- active publication хранит и возвращает только вычисленный Worker `computed_digest`; отсутствие active batch показывается как `legacy_unattested`, а не как atomic snapshot;
- backend owner identity совпадает с единственным разрешённым Sites-пользователем; без identity и с чужой identity чувствительные API остаются `403`;
- owner loaders принимают только ожидаемую форму ответа, используют безопасные пустые массивы и не выводят raw exception message.

Эти исправления не разрешают повторную передачу реальных ФИО/зарплат. Существующие legacy D1 rows остаются неаттестованными до отдельного информированного owner approval и одного нового atomic snapshot.

## Дополнение A.9 — fail-closed publication gate

Checkpoint v25 не открыл следующий этап: независимый Ревизор обнаружил, что одна смена `dataMode` могла провести неполный AlfaCRM payload как terminal, а последовательная замена datasets могла оставить смешанную read-модель при сетевом отказе. PII импорт остановлен до сетевой отправки; фактической утечки не было.

Terminal publication теперь требует одновременно `alfaTerminalGatePassed = true`, завершённый source batch, 0 failed entities, 0 incomplete scopes и согласованный `sync_status` с `real_read_only_terminal`. Частичный режим по-прежнему разрешает только проверенный payroll и агрегированный статус AlfaCRM при физически пустых восьми operational datasets.

Read-модель публикуется как один snapshot: все строки сначала попадают в изолированный staging batch с полным manifest и SHA-256 payload, затем D1 transaction атомарно заменяет все 14 datasets и переключает active batch. Оборванный staging не меняет live rows; ошибка любого `NOT NULL`/money/data constraint откатывает замену целиком. Старый последовательный import endpoint закрыт кодом `410 ATOMIC_SNAPSHOT_REQUIRED`.

Эта коррекция относится только к owner-only Sites. Она не разрешает PII-публикацию без отдельного информированного подтверждения владельца, не делает partial AlfaCRM полным, не доказывает банковскую сверку и не открывает production, финансовые расчёты или AI CFO.

## Дополнение A.8 — branch-scoped восстановление и частичный безопасный checkpoint

Sandbox-аудит доказал, что CRM-ID AlfaCRM повторно используются в разных филиалах. После объединения с Front Office соответствующая migration имеет номер `0017`; она и вся owner read-модель идентифицируют ученика, профиль, группу, педагога, оплату, занятие и посещаемость составным ключом `branch_crm_id + crm_id`. Из immutable raw-наблюдений без обращения к провайдеру воспроизводимо восстановлены 38 045 строк пяти основных сущностей. Нормализованных branch-key дублей и нарушений raw/batch/scope/page provenance нет.

Migration `0018` переносит составную идентичность в кандидатов семей. После отдельного backup с SHA-256 `e3631c6aecc40e8097d137ee7e2ef3e1fa4162867b18af38314cce938ac22805` построено 555 диагностических кандидатов со статусом `pending_review`; подтверждённых семей — 0, автоматических объединений — 0. Additive migration `0019` безопасно сводит конкурентные истории Front Office и AlfaCRM без перезаписи business data. Пока AlfaCRM batch не завершён, family rows не являются полным реестром и не публикуются в Sites.

Последний полный read-only batch остаётся `partial`: запрошено 2 672 scope, завершено 2 355, не завершено 317, сохранено 56 659 raw-наблюдений. Все незавершённые scope имеют безопасный код `request_timeout`. Повторный одиночный probe остановился на `/auth/login` по сетевому timeout до domain endpoint. Текущие credentials не записываются в source, Git, Sites или отчёты; причина не классифицируется как неверный ключ без ответа провайдера.

Для текущего owner-only checkpoint разрешён строго ограниченный split: проверенная зарплатная read-модель и фактический runtime-статус «Точки» могут публиковаться независимо, а частичная AlfaCRM передаёт только агрегированные counts/status. Экспорт физически содержит 0 операционных строк AlfaCRM. В нём 114 сотрудников, 842 помесячные строки, 1 318 компонентов, 1 855 связанных выплат и 36 unresolved rows; source-to-minor mismatches = 0. Формулы, ставки, KPI, налоги, teacher↔employee связи, ДДС, ОПиУ, финансовая модель и AI CFO не активируются.

Runtime «Точки» ранее машинно подтвердил `active`, 16 счетов, read-only scope и отсутствие платёжных действий. Это не доказывает полноту выписок или банковскую сверку. Production PostgreSQL/Replit не подключались и не изменялись.

## Дополнение A.7 — денежная точность и фактические статусы источников

Версия v16 не прошла независимые ворота: Ревизор обнаружил, что единый frontend formatter скрывал копейки, округляя реальные начисления, выплаты, ставки и банковские остатки до целых рублей. Этот checkpoint остаётся только диагностическим и не используется для денежных решений.

В correction candidate все авторитетные денежные значения owner read-модели хранятся и передаются как целые minor units с масштабом 2. Legacy `REAL`-колонки оставлены только для совместимости и не являются источником расчёта. Экспорт сверяет каждую исходную сумму с точным числом копеек; publisher и deployed machine status требуют ноль отсутствующих minor-unit полей и ноль расхождений. Интерфейс всегда показывает две цифры после запятой. ДДС, ОПиУ, финансовая модель и любые новые денежные расчёты по-прежнему выключены.

Статус интеграции в интерфейсе определяется runtime API, а не статическим текстом checkpoint. Последняя машинная проверка подтвердила read-only «Точку» в состоянии `active`, 16 счетов, отсутствие безопасного error code и физически выключенные платёжные действия. Это доказывает подключение счетов, но не полноту выписок и не банковскую сверку.

Payroll read-модель сверена до копеек: 114 сотрудников, 842 помесячные строки, 1 318 компонентов, 1 855 связанных выплат и 36 unresolved rows; source-to-minor mismatches = 0. Налоги остаются `not_sourced`, формулы и ставки не активируются как правила.

AlfaCRM остаётся частичным непубликуемым snapshot: terminal export намеренно завершается `ALFACRM_TERMINAL_SNAPSHOT_REQUIRED`. Возобновление разрешено только после защищённой передачи `ALFACRM_DOMAIN`, `ALFACRM_EMAIL`, `ALFACRM_API_KEY`; старые раскрытые значения не восстанавливаются из истории и не записываются в source.

## Дополнение A.6 — возобновляемая загрузка и доказуемая классификация

Текущий candidate развивает существующую owner-only read-модель без изменения production БД. Прерванный полный AlfaCRM batch можно продолжить только явным флагом `ALFACRM_RESUME_RUNNING_BATCH=1`: завершённые scopes того же batch пропускаются, незавершённые перечитываются, а итог `completed` разрешён только при отсутствии незавершённых scopes. Частичная выгрузка не публикуется как полная.

Педагоги, занятия, группы и зарплатные evidence вынесены в отдельные представления. Связь педагога AlfaCRM с сотрудником зарплатной ведомости по точному ФИО, выделение класса из названия группы и признак дополнительного занятия являются только кандидатами для проверки. Они не создают кадровые, расчётные или учебные правила автоматически.

В источнике зарплат не найдены подтверждённые правила налогов и страховых взносов. Поэтому налоги имеют статус `not_sourced`: система не рассчитывает их, не достраивает ставки и не включает догадки в начисления.

Migration `.openai/drizzle/0002_familiar_blockbuster.sql` относится только к D1 read-модели Sites, является аддитивной и не затрагивает production PostgreSQL/Replit. Переход к ДДС, ОПиУ, финансовой модели и AI CFO остаётся закрыт до терминального AlfaCRM audit, сверки банка, подтверждения зарплатных правил и независимых ворот качества.

## Дополнение A.5 — owner-only рабочая read-модель

Главный документ `ArtHello_OS_Master_Plan_and_Agent_Prompts_v0.1.md` повторно найден в ChatGPT Library и прочитан полностью 26.07.2026. Текущий срез продолжает существующую кодовую базу и следует порядку: качество данных → объяснимые карточки → финансовые отчёты → AI CFO.

Владелец отдельно разрешил использовать текущие AlfaCRM и Tochka credentials для end-to-end проверки и показать реальные операционные, зарплатные и банковские данные в единственном owner-only Sites-проекте. Доступ Sites подтверждён для одной учётной записи без групп и публичной аудитории. Разрешение не включает запись в AlfaCRM, инициирование платежей, автоматическое объединение семей, production-миграции legacy Replit или расширение аудитории.

Sites является ограниченной server-side read-моделью, а не источником истины: D1 получает только необходимые представления из изолированной БД, API проверяет владельца, чувствительные просмотры журналируются, индексация запрещена. Секреты находятся только в protected environment; банковские токены шифруются, номера счетов маскируются, а внешние account IDs хешируются. Технические настройки находятся в «Системе», а «Деньги» показывает только счета, остатки и будущую сверку.

Payroll source содержит 2 593 фактические строки, 114 карточек сотрудников, 1 881 принятую выплату и 1 328 принятых начислений. Две строки без суммы отклонены, 26 выплат и 17 начислений остаются unresolved. Формулы сохранены как evidence и не активированы как правила.

Migration `0015` усиливает `0014`: каждая из 13 нормализованных AlfaCRM-таблиц имеет обязательную ссылку на точное observation того же raw/batch/scope/page; raw и observation защищены от UPDATE, DELETE и TRUNCATE. Rollback `0014` исправлен и проверяется сценарием rollback → reapply → `0015`. Stale student каскадирует зависимые memberships, attendance и payments; дочерний импорт не может воскресить данные stale-родителя.

Полный AlfaCRM read-only импорт 26.07.2026 выполняется в отдельной PostgreSQL-совместимой sandbox-БД. Пока нет terminal report и integrity audit, интерфейс не утверждает фактические количества. Legacy Replit остаётся старой небезопасной сборкой и не используется для размещения новых данных.

OAuth «Точки» реализован server-side в Sites Worker для зарегистрированного root redirect: state проверяется, code обменивается на сервере, токены сохраняются шифрованно. Запрашиваются только `accounts balances customers statements` и permissions чтения; payment scopes, endpoints и действия отсутствуют. Consent должен отдельно подтвердить владелец.

Юридический контур зафиксирован в `LEGAL_DATA_CHECKLIST.md`. Это рабочий контрольный список, не юридическое заключение; расширение доступа и долговременный production-режим требуют закрытия указанных там пунктов.

## Дополнение A.4 — изолированный импорт реальных источников

Correction candidate после `FAIL` v13 использует migration `0014`: `alpha_raw_records` является append-only, каждое наблюдение хранится отдельно с batch/scope/page, а нормализованные AlfaCRM-строки требуют существующие raw и batch FK. Snapshot reconciliation разрешён только завершённому scope; partial/repeated-page/max-guard оставляют предыдущий current-state без tombstone. Findings `A4-13-01`, `A4-13-02`, `A4-13-03` считаются исправленными только в candidate и остаются открыты до независимого PASS финального Ревизора и Координатора.

Владелец расширил разрешение на текущие временные credentials: AlfaCRM можно читать полноценно и сохранять в отдельную sandbox-БД, а новый production-клиент «Точки» можно использовать для проверки read-only банковского контура. Это разрешение не включает запись в AlfaCRM, платежи, production-миграции, публикацию персональных или финансовых строк в Sites и хранение секретов в Git.

Реальная зарплатная таблица импортирована в отдельную PostgreSQL-совместимую БД: исходные строки и формулы сохранены как evidence, выплаты и вычисленные начисления сверены с источником. Формулы, ставки и KPI не активированы как правила ArtHello OS. Юридические лица заведены по прямому подтверждению владельца, но фактическая связь каждого филиала AlfaCRM с юрлицом ждёт свежей выгрузки.

Полный AlfaCRM importer готов читать филиалы, учеников и лидов раздельно, оплаты, группы, состав, занятия, посещаемость, педагогов, абонементы клиентов, платёжные справочники и журнал изменений. Raw-слой и нормализованные таблицы имеют обязательный provenance и snapshot-state в candidate `0014`; incremental-режим пока является только обнаружением изменений по `log/index` с перекрытием watermark, а не полной материализацией изменившихся сущностей. Свежий запуск из текущей среды завершился безопасным `request_timeout` до получения филиалов и сохранил ноль CRM-записей; историческое число 8 не является текущей истиной.

Новый клиент «Точки» получил service token, но запрос счетов закономерно требует пользовательский hybrid OAuth. Redirect URI на Sites запрещён: authorization code должен принимать только точный HTTPS callback защищённого backend `/api/banking/oauth/callback`. Consent нельзя создавать до исправления зарегистрированного URI, защищённого runtime environment, backup/rollback и проверки state. Платёжные scopes и платёжные действия запрещены.

## Дополнение A.3 — ограниченная live read-only проверка

Владелец разрешил временно использовать текущие credentials для проверки работоспособности. Допустимы только ephemeral auth, metadata/count probes и read-only запросы; секреты и полученные записи не сохраняются и не публикуются. Подтверждены AlfaCRM auth и 8 филиалов, а также Tochka client credentials/service token. Банковские счета требуют отдельного hybrid OAuth consent. Эти результаты не открывают production, sync, финансовую правду или AI CFO.

## Дополнение A.2 — технические ворота

Bank connector config хранится в authenticated encrypted envelope и fail closed при legacy plaintext secrets. AlfaCRM read-only клиент сериализует запросы ниже лимита 4 req/s. PostgreSQL 16 проверки исторических checkpoints прошли migrations, HTTP/session/CSRF/audit smoke, webhook rollback/retry, schema drift, rollback и fail-closed startup. Candidate цикла 3 дополнительно публикует immutable CI head/tree/digest evidence. Эти PASS не открывают production gate без оставшихся HIGH, восстановленной sandbox-копии и ротации credentials. Credentials, появившиеся в переписке или скриншотах, считаются раскрытыми и требуют ротации.

Статус документа: рабочая книга проекта, восстановлена 2026-07-23 и обновлена 2026-07-25 на основе `ArtHello_OS_Master_Plan_and_Agent_Prompts_v0.1.md`.

## Миссия

ArtHello OS — единая операционная и финансовая система ArtHello. Она должна соединять AlfaCRM, семьи, учеников, группы, сотрудников, зарплату, банки, ДДС, ОПиУ, финансовую модель и рекомендации собственнику.

Система не считается готовой, пока данные нельзя проследить до источника, расчёты нельзя воспроизвести, а денежные и персональные данные не защищены.

## Неподвижные принципы

1. Реальные источники важнее исторических отчётов и интерфейсных заглушек.
2. Наличие таблицы, маршрута или экрана не доказывает полноту и корректность данных.
3. Банк — источник факта движения денег; AlfaCRM — операционный источник. Различия должны быть явно сверены.
4. Нельзя автоматически объединять семьи по одному слабому признаку.
5. Нельзя придумывать оклады, ставки, KPI, финансовые статьи и продуктовые правила.
6. AI CFO и финансовые рекомендации запрещены до прохождения ворот качества финансовой правды.
7. Интеграции банков в этом проекте не инициируют платежи.
8. Production-миграции выполняются только после backup, проверенного rollback и отдельного разрешения.
9. Sites — постоянная приватная контрольная оболочка интерфейса. Реальные персональные, зарплатные и банковские данные в неё не публикуются без подтверждённой защищённой авторизации.
10. Все неподтверждённые значения показываются как «не подтверждено» или «неизвестно», а не заменяются случайными числами.

## Источники истины

Приоритет доказательств:

1. свежая read-only выгрузка или запрос к авторизованному источнику;
2. воспроизводимый запрос и журнал синхронизации;
3. схема БД и код текущей проверенной версии;
4. исторические отчёты — только как гипотеза для перепроверки.

Архив исходников не содержит исходную `.git`-историю. Поэтому прежняя ветка, незавершённые изменения и авторство коммитов пока не подтверждены.

## Контуры

| Контур | Назначение | Допустимые данные | Текущий статус |
|---|---|---|---|
| Replit / рабочее приложение | исторический runtime приложения | только после проверки доступа и защиты | исходники восстановлены; live-выпуск не выполнялся |
| Production DB / интеграции | операционные и финансовые данные | только авторизованный read-only доступ | legacy production DB не подключена; AlfaCRM импортируется в отдельную sandbox-БД, «Точка» ждёт owner consent |
| Sites `arthello-os-control` | owner-only контроль интерфейса | минимальная разрешённая read-модель, без raw и секретов | доступ подтверждён для одной учётной записи; deployment A.5 готовится |
| Локальная рабочая копия | разработка и тестирование | без production-секретов | активна |

## Архитектурный контур

- pnpm-монорепозиторий;
- React/Vite интерфейс `artifacts/alpha-crm-sync`;
- Express API `artifacts/api-server`;
- PostgreSQL/Drizzle в `lib/db`;
- AlfaCRM read-only клиент и синхронизация;
- банковские коннекторы и raw/normalized слои;
- модули семей, обучения, сотрудников, зарплаты и финансов;
- отдельная owner-only Sites control surface в `sites-control` с server-side D1 read-моделью, protected environment и аудитом чувствительных просмотров.

## Этапы и ворота

### Фаза A — аудит и восстановление

Выход:

- восстановлена реальная кодовая база;
- зафиксирована её доказуемая версия и происхождение;
- составлена карта сервисов, интеграций, БД и рисков;
- публичные демо-реквизиты удалены из исходников;
- служебный интерфейс закрыт от индексации в исходниках;
- production, локальная разработка и Sites разделены;
- подготовлены документы эксплуатации и качества;
- опубликован owner-only Sites checkpoint; реальные строки допустимы только после отдельного разрешения владельца, проверки access policy и минимизации read-модели.

Фаза A не включает production-миграции, банковские платежи или подтверждение актуальных бизнес-метрик без доступа.

Owner-only checkpoint принимается только если он показывает полный известный реестр существенных рисков. Решение D-040 отдельно разрешает ограниченную owner-only read-модель из изолированного source snapshot; это не разрешает production/Replit release, фоновые записи в источники, production-миграции или расширение аудитории. Реализация sessions/RBAC в legacy-приложении сама по себе не открывает production gate: migrations `0009–0010` должны пройти backup → sandbox restore → apply → HTTP/PostgreSQL smoke → rollback и интеграционные проверки.

### Фаза A.1 — security perimeter

В исходниках подготовлен первый security-срез:

- устойчивые PostgreSQL sessions с хранением только hash session token;
- Secure/HttpOnly/SameSite cookie вместо bearer token в browser storage;
- double-submit CSRF для изменяющих запросов;
- route-level RBAC `owner/accountant/viewer` с deny-by-default;
- branch/legal-entity scope metadata в session; non-owner business routes fail closed до handler-level predicates;
- fail-closed security access audit с registry шаблонов маршрутов: slug, email, phone, UUID, numeric и percent-encoded identifiers не записываются открыто;
- распределённый login lockout через PostgreSQL;
- централизованная redaction структурированных logs;
- public 5xx response boundary с фиксированным error code и без exception/upstream/PII details;
- историческая `/sync` поверхность fail closed до замены на scoped source jobs;
- generic banking errors и strict-allowlist detailed health response, неизвестные будущие поля автоматически отбрасываются;
- migration failure и отсутствие обязательных columns/indexes/точных hash записей migrations `0009–0010` блокируют listener и banking polling;
- website lead handler требует idempotency key, связывает его с canonical payload, выполняет raw + lead + source update в одной PostgreSQL transaction под advisory lock и возвращает `409` при повторном key с другим payload; внешний вызов всё ещё закрыт;
- удалён fallback-ключ шифрования Evotor;
- sessions/lockout оформлены migration `0009`, scope/audit — migration `0010`; обе не применялись к production.

Pure/unit regression tests доказывают rollback mid-write, retry с одной парой raw/lead, восстановление legacy raw-only записи, payload conflict, schema inventory, allowlist, route canonicalization, cross-scope deny, public error redaction и базовые синтетические финансовые инварианты. Одноразовый PostgreSQL 16 CI подтвердил runtime apply/rollback, session/CSRF, разрешённый access audit, транзакционный failure/retry и fail-closed startup. Candidate evidence всегда связывается с exact PR head/tree через immutable CI artifact. Production и восстановленная sandbox-копия не затрагивались.

Исторический срез A.1 не закрывал plaintext bank secrets и AlfaCRM concurrency limiter. В A.2 vault и serialized queue реализованы и покрыты unit-тестами; A.3 добавляет PostgreSQL 16 CI evidence. Цикл 3 закрыл public 5xx boundary и заблокировал legacy `/sync`, но удаление недоступного legacy code остаётся отдельной release-задачей. Открыты guarded migration legacy rows, ротация ключей, handler-level scope predicates, provider-specific webhook authentication/replay protection, PostgreSQL deny/outage tests и restored-sandbox evidence.

### Следующие фазы

1. Авторизованная read-only инвентаризация источников и покрытия.
2. Надёжный raw → normalized → domain pipeline с наблюдаемостью и дедупликацией.
3. Семьи, ученики, группы и образовательная модель.
4. Сотрудники и проверяемая зарплата.
5. Банк, сверка, ДДС и ОПиУ.
6. Финансовая модель, закрытие месяца и только затем AI CFO.

## Ворота финансовой правды

Переход к расчётам разрешён только когда:

- подтверждены филиалы, юридические лица и банковские счета;
- известны временные границы и свежесть каждого источника;
- проверены пропуски, дубли и несверенные записи;
- утверждены финансовые статьи и правила признания;
- зарплатные формулы имеют подтверждённый источник;
- денежные расхождения объяснены или вынесены в явный реестр исключений;
- Reviewer и Coordinator оценили одну и ту же точную версию.

## Роли контроля

- **Создатель** — единственный редактор исходников и Sites checkout.
- **Ревизор** — новый read-only агент, независимо проверяющий безопасность, данные, расчёты, интерфейс и тесты; итог `PASS`, `CONDITIONAL PASS`, `FAIL` или `BLOCKED`.
- **Координатор** — новый read-only агент, проверяющий единство версии и решение о переходе дальше.

Не более трёх циклов «Создатель → Ревизор → Координатор → исправления».

## Обязательный комплект проектной документации

- `MASTER_BOOK.md` — неизменяемые принципы и ворота проекта;
- `CURRENT_STATE.md` — доказанное состояние и точные версии;
- `DECISIONS.md` — существенные решения и ограничения;
- `BACKLOG.md` — открытая работа по приоритетам;
- `DATA_COVERAGE.md` — покрытие и свежесть источников;
- `RUNBOOK.md` — воспроизводимые операции, backup и rollback;
- `SECURITY_CHECKLIST.md` — security gate;
- `MONTH_CLOSE_CHECKLIST.md` — финансовые ворота закрытия периода.

Наличие файла не означает прохождение gate. Каждый статус должен ссылаться на доказательство одной точной версии.

## Текущая управленческая формулировка

На 2026-07-25 технический фундамент и security perimeter усилены, payroll source проверяемо загружен в изолированную sandbox-БД, а новый клиент «Точки» прошёл только auth gate. Операционная полнота AlfaCRM, банковские данные и финансовая правда ещё не подтверждены. Sites checkpoint обязан показывать разницу между «подтверждено источником», «проверено в sandbox», «реализовано в коде» и «выпущено live», а не имитировать готовую аналитику.
