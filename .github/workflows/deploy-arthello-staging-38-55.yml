name: Deploy ArtHello STAGING 38.55 (legacy manual fallback)

on:
  workflow_dispatch:

jobs:
  disabled-by-default:
    if: ${{ false }}
    runs-on: [self-hosted, arthello-gateway]
    steps:
      - run: echo 'Legacy fallback only. STAGING is deployed by /usr/local/sbin/arthello-staging-pull on 188.225.38.55.'
