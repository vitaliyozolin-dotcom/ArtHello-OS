# ArtHello OS — Decisions

## D-086 — URL является источником состояния раздела back-office

Дата: 2026-09-09. Статус: принято владельцем явным поручением закончить Фазу 5.

Каждый раздел owner back-office имеет один канонический URL. `wouter` читает текущую
секцию из location и записывает внутренние переходы в browser history; прямое открытие,
обновление страницы и Back/Forward восстанавливают тот же раздел. Неизвестный URL
заменяется на `/` fail closed, без выбора раздела по догадке. API, сервер, права и формы
ответов не меняются.

Visual acceptance выполняется по четырём каноническим маршрутам в desktop/mobile,
записывает URL каждого screenshot и отклоняет redirect/mismatch. Расширение набора
канонических страниц выполняется данными в `quality-gates/visual-canon.json`, без
возврата к click-driven состоянию.

## D-085 — Декомпозировать route-модули без изменения HTTP-контракта

Статус: принято владельцем явным поручением выполнить Фазу 4.

Фаза 4 выполняется одним кандидатом по прямому указанию владельца. Структурный
кандидат сохраняет порядок регистрации, HTTP method/path, status codes, response
shapes и permission semantics. Тематические каталоги экспортируют именованные
Router entrypoints; публичные `routes/**/*.ts` остаются тонкими и также используют
только именованные exports. Общие технические зависимости выносятся во внутренние
модули, но бизнес-правила, SQL и диагностические формулировки не изменяются попутно.

Acceptance: нулевая дельта исполняемой route table относительно OpenAPI/inventory,
зелёный permission proof, typecheck, regression tests, application builds и visual
acceptance. Изменение поведения требует отдельного D-решения и не может маскироваться
под декомпозицию.

## D-084 — R10: привязка штатного домена шлюза и продолжение после раннего отказа R9

Дата: 2026-09-09. Статус: подготовка в рамках действующего поручения Виталия продолжить проверку и выпуск. Владелец Виталий; исполнитель Codex.

Основание: R9/PR377 source30f825d674cbf498be713c7b637e1dfd9f9c42cd/treebf1c67e32ea329546ce8870a4c3589f9b2c6a16d прошёл exact main gates и118 проверок установленного Caddy. Run34318973875/job102361578781 на2026-09-09T06:29:38Z отказал в render maintenance до clone/auth/public; rollback VERIFIED. Прежний live6596f69390ad539577ec2640e8ef40c7e12c22dc сохранён. D083/PR378 sourceedb7bfa1c666853be162c99295e0044820753ab0: main Quality34320649119 и Proof34320649038 SUCCESS; защищённая диагностика run34320812445/job102366801535 на2026-09-09T06:49:28.520Z подтвердила stage=main_structure, reason=main_environment_expansion. Никакого входа или изменения gateway при диагностике не выполнялось.

R10 — новая собственная identity PR379 из codex/school-arthello-recovery-r10-20260909, префикс D084: guarded R10, ожидаемый непосредственно предыдущий main edb7bfa1c666853be162c99295e0044820753ab0. Его разрешение не переиспользует R9 как повтор после pre-auth failure. Новый guard отдельно подтверждает неизменяемые R9 run/job/source/tree, полный порядок шагов и фактические render-blocked/rollback-verified markers; отсутствие clone/auth/public/bank activation обязательно. Все прежние исторические R5/R8 abort guards и запреты повторного успешного или неоднозначного выпуска сохраняются.

Поддерживается только штатный APP_DOMAIN placeholder общедоступного соседнего сайта в исходном Caddyfile StroiOS blob37ff734e50f904982407baffe43cbc787dc3ce9f. Значение читается заново из Config.Env конкретного уже работающего Caddy и должно быть единственным каноническим hostname без схемы, порта, wildcard, управляющих символов, дополнительных адресов или совпадения с ArtHello/School. Разрешена лишь одна некавыченная переменная отдельным адресом top-level site; прочие environment expansions и дополнительные imports по-прежнему запрещены. Подстановка применяется только к проверке в памяти: реальный Caddyfile и env не изменяются.

Приватное свидетельство связывает полный gateway container ID, image ID, домен и хеш исходного main config/исходных routes. Его путь и canonical SHA включаются в durable context, а значит в context hash реальной browser receipt. При load/advance, resume и перед browser/public повторяются прежние полные runtime checks плюс свежая проверка gateway ID/image/domain/main; live external-route hash отдельно проверяется по текущей phase. Подмена sidecar, старый receipt с иным context, изменённый gateway или state останавливают продолжение. Порядок preflight/backup/rollback/maintenance/auth/public/bank activation и запрет snapshot restore после реальной авторизации остаются. Frozen R9 файлы не меняются; новые технические d083 имена относятся к решению D084.

Перед новой проверкой ёмкости разрешено завершить жизненный цикл ровно одного собственного неиспользуемого браузерного образа неуспешного R9: ID sha256:413b76d9ca6024d9ecc229f9d5b16810f5b9a50861f984e0ecb925020209e4ef, source30f825d674cbf498be713c7b637e1dfd9f9c42cd, единственный tag arthello-e2e:30f825d674cbf498be713c7b637e1dfd9f9c42cd, fingerprintb1d04b748b75b1283502aca1faef903dbcc7e1fed258e7ad4419ba67076153c5. Восстановимость — unexpired artifact10091157744, producing run34318973875, name server-browser-30f825d674cbf498be713c7b637e1dfd9f9c42cd-34318973875-1, digestsha256:9cfcd718f3bf69a8cb682092ee4ecdfef6bf5803cfb8fa976214db69f5b27274, размер640809272bytes. Обязательны exact image/tag/роль e2e-browser/UID1000:1000/fingerprint, допустимые прежние self-RepoDigest, отсутствие любых consumers включая остановленные, fresh main и повторные проверки перед единственным docker image rm --no-prune по полному ID. Подтверждённое отсутствие обоих ID/tag допускает skip; ошибка Docker/доступа не считается отсутствием. Caller-selected target, force, общий prune, application images, volumes, контейнеры и данные не удаляются. Во время browser acceptance очистка не выполняется. После операции обычный capacity gate остаётся обязательным.

Полный R10 contract расположен под уже отслеживаемым deploy/v52/recovery-r10 и закреплён SHA в Quality. Он связывает новые scripts/workflow/tests и отдельно подтверждает сохранность прежних Quality и frozen V52/R9. Любое изменение контроллера требует обновления contract под watched path и нового exact V52 gate; неявное исключение из CI запрещено. Изолированный hosted Caddy должен выполнить прежние118 HTTP проверок и16 проверок сохранности соседнего StroiOS на точном шаблоне; на VPS повторяется проверка фактического установленного Caddy. Полный естественный browser flow с тем же сотрудником и обычными server-created sessions обязателен до и после public opening. Локальные/hosted проверки не называются live PASS.

AI-контракт: вход exact source/CI, действующая защищённая тестовая учётка и приватные gateway/runtime evidence; выход проверенный кандидат и фактическая исходная бизнес-приёмка. Разрешены уже одобренный выпуск, bounded gateway validation и точное завершение жизненного цикла своего воспроизводимого R9 test image. Запрещены вывод паролей, Caddy config, nonce, кодов SSO или дневника, смена ролей ради PASS, session injection, произвольная очистка, платежи и snapshot restore поверх новых данных. Ресурс существующие runner/CI, новые услуги не подключаются, фактическая стоимость тарифа неизвестна. Метрика — natural employee Education/diary PASS и исходные бизнес-сценарии. Срок текущая итерация; пересмотр после реального результата. Несовпадение identity/state/прав или отказ gate останавливает переход; при отказе Виталия новые действия прекращаются. Новое общее разрешение владельца не требуется вместо уже предоставленного.

## D-083 — Ограниченная диагностика отказа маршрута R9

Дата: 2026-09-09. Статус: подготовка по действующему поручению Виталия продолжить проверку и выпуск. Владелец Виталий; исполнитель Codex.

R9/PR377 merged30f825d674cbf498be713c7b637e1dfd9f9c42cd, treebf1c67e32ea329546ce8870a4c3589f9b2c6a16d. Main Quality34318683959, Proof34318683960 и v5234318684003 прошли. Protected run34318973875: bundle102361193671 SUCCESS, deploy102361578781 на2026-09-09T06:29:38Z остановился в candidate-maintenance-route до clone volume, авторизации кандидата и изменения публичного маршрута; ARTHELLO_ROLLBACK=VERIFIED. Установленный шлюз выполнил118 проверок Caddy. Прежний live6596f69390ad539577ec2640e8ef40c7e12c22dc остаётся опубликованным; liveAcceptance не пройдена.

Отдельная диагностика использует существующий D074 workflow, production-ru, оба shared locks и exact trusted current-main. Она запускается только для префикса коммита D083: read-only route diagnostic после успешного main Quality. Читает только метаданные одного подтверждённого старого приложения и существующего шлюза, затем копирует два конкретных файла Caddy в собственный приватный временный каталог. Frozen R9 validator проверяется по SHA256 и выполняется в памяти с синтетическим nonce; формируемые строки отбрасываются. Оригинальные helper, workflow, критерии и политика повтора R9 не изменяются.

В отчёт входят только kind/result/stage/reason из фиксированного словаря. Содержимое конфигурации, домены соседних сайтов, upstream, nonce, credentials и exception text не выводятся. Копии удаляются по точным собственным путям. Диагностика не читает рабочую БД, не входит в сайты, не меняет настройки/права/маршруты/контейнеры, не перезагружает Caddy и не исполняет сетевые пробы с nonce. Несовпадение текущего main, source, container identity, hash frozen helper или приватных файлов прекращает проверку.

Исходник общего шлюза StroiOS deploy/Caddyfile blob37ff734e50f904982407baffe43cbc787dc3ce9f содержит APP_DOMAIN placeholder. Frozen validator отвергает его; локальный отказ воспроизведён. Это сильная гипотеза, а не наблюдение живого файла. Дальнейшее исправление допускает только конкретное подтверждённое условие с новым exact-source review и gates; произвольные environment expansions, обход проверок и повтор R9 pre-auth failure не разрешаются.

AI-контракт: вход exact source и два приватных Caddy-файла внутри защищённого задания; выход фиксированная причина отказа. Действие — ограниченное чтение в рамках уже разрешённой подготовки выпуска. Запрещены вывод конфигурации/секретов и любые изменения production. Ресурс — существующий runner, без новых услуг; стоимость по тарифу неизвестна. Метрика — однозначный подтверждённый invariant. Срок текущая итерация; пересмотр после фактического отчёта. При отказе Виталия новые действия прекращаются. Готовность приложения этим результатом не объявляется.

Идентичность после параллельного изменения: commit aaa0b2b0d31a936751076472736ee1ba07bcbbfd добавил принятое D-082 о границах API. Диагностике присвоен следующий свободный D-083; технические d082-route-diagnostic.py и kind=d082-route-diagnostic сохранены как проверенные идентификаторы. Все55 изменений параллельного API-кандидата сохранены. Предыдущий head6a3d48a2b60d9ab11a6df1f505338b4707d9052c прошёл Quality34319867568, Proof34319867545 и v5234319867526, но объединённому источнику нужны собственные checks.
## D-081 — Проверка исправленного кандидата до открытия ArtHello пользователям

Дата: 2026-09-09. Владелец: Виталий; исполнитель: Codex. Статус: preparation. Основание: действующее поручение исправить заявленные функции, самостоятельно проверить и выпустить в production, повторные «Выполняй», «Разрешаю», «продолжай». PR377. Новый общий запрос разрешения на тот же выпуск не требуется.

D076 run34314770145/attempt1/job102348936617 на2026-09-09T05:29:05.827Z подтвердил обычный вход, постоянный пароль, явный Education grant и отрицательную проверку неназначенного раздела, затем получил education_forbidden. Это FAIL старой версии, liveAcceptance:not_passed; конкретная apiRole не выводилась. Исправление branch-scoped Education уже включено через PR354 и соответствует D050/D063. Полный PASS старого приложения перед доставкой содержащего исправление образа образует циклическую зависимость.

D081 явно заменяет только порядок приёмки D078 новым R9: свежая readonly School diagnostic и точная историческая baseline403; прежние provenance/CI/browser-import/capacity/clone/backup проверки; проверка реального кандидата через временный закрытый публичный маршрут; затем открытие обычного маршрута и новый полный браузерный проход. Старые R1–R8 workflow и их история не переармляются. Baseline403, synthetic fixture и ручная проверка владельца не становятся PASS кандидата.

До окна обслуживания выполняются все доступные проверки исходников, immutable образов, sandbox, резервной копии и clone. Реальный установленный Caddy отдельно проверяется на локальном fixture в network:none контейнере с уже проверенным образом: без скачивания, сборки, подключения БД или изменения шлюза.

Временный маршрут меняет только точный ArtHello site. Он допускает GET/HEAD и единственный штатный login POST с случайным64hex ключом прохода, а также точный обычный POST /api/school-sso/exchange без этого заголовка. Остальные запросы получают503/no-store. Exchange сохраняет одноразовый код, PKCE, срок, rate-limit и повторную проверку действующих прав; общего секрета на этом endpoint нет. School и остальные сайты не переключаются. Банковская активация остаётся закрытой.

Ключ прохода не является сессией: тест входит через обычную форму с существующей отдельной учёткой. Ключ передаётся браузеру только через stdin и добавляется только к текущему запросу точного ArtHello origin посредством CDP; штатные cookies создают сами сайты. Authorization удаляется до приложения и не передаётся School. Конфигурация и recovery-файлы приватны, log_credentials запрещён; fixture проверяет журналы и реальные303/cookie transitions. Не меняются роли, пароль, назначенные филиалы, deny-проверки и персональные границы данных.

До установки маршрута и любых реальных cross-service auth writes сохраняется durable no-restore boundary. После него любая ошибка или неопределённый исход сохраняют текущую БД кандидата и независимое состояние School. Восстановление снимка поверх этих записей запрещено. Данные для продолжения находятся вне RUNNER_TEMP; очистка GitHub temporary directory не должна делать продолжение невозможным.

Полный PASS в maintenance обязан подтверждать employee login, Education, скрытый неназначенный раздел и API403, естественный переход и School identity. Receipt связывается с source, полным container ID, неизменным context hash, началом maintenance, точной School repair receipt, run и attempt. Старый, будущий, просроченный или иной receipt запрещён. Переход состояния монотонен под файловой блокировкой. Перед maintenance→public повторяются current-main, runtime/route hashes, отрицательные HTTP probes и freshness. Прежняя durable public boundary сохраняется; банк включается только после проверок открытого маршрута.

Повтор failed job допустим только для точно установленной незавершённой candidate-maintenance фазы этого же run/source. Нужны точный предыдущий job graph, фактический hold marker и новая независимая проверка сохранённых приватных файлов, БД/контейнера/образов/сетей/mounts/backup и отсутствия public/bank activation. Повтор продолжает тот же кандидат без новых миграций, импорта, сброса БД или перенастройки School. Неизвестный исход, смена source или начавшаяся public activation не допускают такой retry: требуется отдельный конкретный проверенный forward-fix. Непроверенный кандидат не открывается автоматически ради выхода из503.

До активации обязательны завершённая реализация продолжения, отрицательные проверки отказов и очистки temporary files, реальные Caddy/Chromium fixtures, полный применимый CI, exact SHA/tree/PR review и append-only passport. Статус preparation не является разрешением пропустить эти ворота. После публикации остаётся исходная бизнес-приёмка: четыре счёта Точки и реальные операции в реестре/ДДС, выборочный AlfaCRM, права, «Разработчикам»/backlog, контент/номера задач, ручной и ежедневный backup.

Уточнение идентичности 2026-09-09: пока кандидат PR377 проходил проверки, main обновился с 582edaf1a66a953edb2d61e03040d1a13e70a0ad до 59372b139fb0b2345cf3e41fc23c8223099b187f (checked PostgreSQL migrations), заняв D-080. Решению этого выпуска присвоен следующий свободный номер D-081. Технические имена файлов d080-*, схемы и маркеры ARTHELLO_D080 сохранены как неизменённые идентификаторы проверенного протокола; они относятся к D-081 и не запускают миграции PostgreSQL. Префикс активации нового выпуска — D081: guarded R9. Все изменения другого участника сохранены; R9 собирает отдельный v52 runtime с D1 и не выполняет deploy/api-entrypoint.sh или SQL0018.

## D-082 — OpenAPI и единый клиентский transport являются проверяемой границей API

Дата: 2026-09-09
Статус: принято владельцем в явном поручении выполнить Фазу 3
Владелец: Владелец ArtHello OS

`lib/api-spec/openapi.yaml` является источником публичного типизированного API-контракта. Фактическая Express route table сравнивается с ним постоянным исполняемым gate. Существующий server-only долг не считается контрактом: каждый такой method/path обязан находиться в machine-readable inventory с disposition `document-in-openapi`; новый необъяснённый маршрут, исчезнувший документированный маршрут и stale inventory блокируют CI.

Браузерный код не вызывает глобальный `fetch` для API. Операции из OpenAPI используют сгенерированный клиент; пока legacy server-only операция ожидает документирования, совместимый `apiFetch` из того же пакета обеспечивает единые credentials, base URL и CSRF. Добавление нового совместимого вызова не освобождает от OpenAPI inventory и contract-drift gate.

Каноническая нормализация телефона извлекает цифры, приводит российские десятизначные номера и префиксы 7/8 к `+7`, возвращает `null` при отсутствии цифр и не объединяет семьи или людей автоматически. Общие SHA-256, CSRF-cookie и RUB-format helpers живут в `@workspace/shared`; их прежняя семантика закреплена characterization-тестами.

## D-078 — Единый R8 выпуск с настоящим браузером до и после cutover

Дата: 2026-09-08. Владелец: Виталий; исполнитель: Codex; status: preparation. Источник: исходное поручение проверить систему и выпустить в production, повторные «Выполняй»/«Разрешаю». Это продолжение действующего разрешения на выпуск.

R7 PR362 завершился до image import/cutover: run34227595623/job102065872459, source dbe630145a3828643fcfb9f202b1239e8cc54826. R5 relay остаётся установленным. Новый R8 PR370 получает собственные branch/main identities; предыдущие R1–R7 не меняются и не переармляются. Проверяются исторические R5/R6/R7 observations и отсутствие любого начавшегося предыдущего cutover. Полный clone/backup/activation/rollback step R8 идентичен R7 и сравнивается hosted contract.

Первый D076 protected run остановился до логина на archive/import prerequisite без точной причины; второй run34278442727/job102237824163 остановился до checkout, поскольку main продвинулся до e5a51ca8ef26d150ac02d30eb1318603e140f08f. Этот параллельный коммит политики доступа к БД и его D-077 сохраняются. Нехватка места пока не измерена. Для выпуска выбран D-078; отдельный устаревший browser run не повторяется.

R8 использует проверенную D076 реализацию в одном workflow с выпуском: hosted bundle собирается для exact candidate, проходит настоящий Chromium namespace sandbox и HTTPS303 fixture; producing-job outputs связывают archive checksum, размеры и portable fingerprint с protected import. До скачивания измеряется место, до импорта оно сверяется повторно. До и после cutover выполняются обычный вход выделенного сотрудника, разрешённый Education, запрещённый API403 и естественный переход в School. Настоящий результат связывается с live SHA и свежей проверкой relay через неизменный schema3 validator. После переключения live ArtHello SHA обязан совпасть с кандидатом.

Обязательная реальная приёмка выполняется внутри R8 перед cutover; synthetic fixture или ручной отчёт владельца её не заменяют. Ни один предыдущий D076 запуск не объявлен PASS. Если браузер/учётка/источник/receipt/ресурсы не подходят, cutover не начинается. Ошибка после public activation сохраняет новые записи и candidate; snapshot restore поверх них запрещён.

Подробный scope и критерии завершения: D-078 паспорта. Exact-head CI, review, clone preflight, первый проверенный backup и public-write guards остаются обязательными. Проверка банковских данных, выборочного AlfaCRM, прав, обращений, контента и ежедневных/ручных бэкапов на опубликованном кандидате остаётся частью исходного задания.

## D-080 — Checked-in Drizzle SQL является единственным авторитетом PostgreSQL-миграций

Дата: 2026-09-09
Статус: принято владельцем в явном поручении выполнить Фазы 2.3 и 2.4
Владелец: Владелец ArtHello OS

Production и sandbox применяют только последовательность SQL из `lib/db/drizzle`, точное содержимое которой связано с journal через immutable manifest SHA-256. Runner до подключения проверяет отсутствие изменённых, пропущенных и незарегистрированных SQL-файлов; существующий migration ledger обязан быть точным префиксом manifest. Каждая новая миграция выполняется в отдельной транзакции и только после успеха записывается в ledger. `drizzle-kit push`, `generate`, introspection-driven mutation и незакоммиченный SQL в runtime запрещены. Production entrypoint не запускает package manager или codegen.

Legacy `artifacts/api-server/src/lib/migrate.ts` удалён. Его доказанная schema-дельта против пути Drizzle 0000–0017 перенесена в явную миграцию `0018_retire_orphan_chat`; нормализованные catalog snapshots после обоих путей совпали на одноразовом PostgreSQL 16. API до listener выполняет только read-only проверку полного migration ledger, затем существующий security schema gate. Любое расхождение останавливает listener и polling.

Схемы `messages` и `conversations` не входили в barrel, checked-in migrations или runtime-потребителей и удаляются из исходников. Миграция удаляет одноимённые legacy-таблицы только если обе пусты; наличие хотя бы одной строки блокирует всю транзакцию. Живые `front_office_messages` и `front_office_conversations` не затрагиваются. Rollback восстанавливает структуру пустых legacy-таблиц, но не заявляет восстановление данных; backup и отдельное разрешение D-009 остаются обязательными перед любым production применением.

## D-077 — Ограничить новые способы доступа к БД постоянным ratchet-гейтом

Дата: 2026-09-09
Статус: принято владельцем в явном поручении выполнить Фазу 2.6, закоммитить и отправить изменение
Владелец: Владелец ArtHello OS

Новый application-код использует Drizzle builder по умолчанию. `db.execute(sql)` допускается для отчётных и иных запросов, которым недостаточен builder, при сохранении параметризации и действующих security-инвариантов. Новые вызовы `pool.query` запрещены; существующие вызовы фиксируются точным сокращаемым baseline по файлам. Сокращение или удаление legacy-вызовов требует синхронного уменьшения baseline, поэтому прежний лимит нельзя незаметно переиспользовать.

PGlite остаётся инструментом sandbox-импортёров и тестов в `scripts/` и запрещается в application/library `src`. Постоянный policy-check входит одновременно в formatting/lint gate и агрегатный `test:refactoring`; поведенческие тесты доказывают разрешённые и запрещённые ветки. Решение не переводит существующие SQL-вызовы, не меняет запросы или бизнес-поведение и не разрешает PGlite как production-БД.

## D-076 — Реальный браузерный проход с выделенной учёткой сотрудника

Дата: 2026-09-08. Владелец: Виталий; исполнитель: Codex; status: active; supersedes: нет.

На запрос создать отдельного активного тестового сотрудника с доступом к «Обучению» и School и сохранить `ARTHELLO_E2E_LOGIN` / `ARTHELLO_E2E_PASSWORD` в protected Environment `production-ru` владелец ответил «готово». Новый prerequisite job `102209067350` в19:37:42Z подтвердил наличие обоих секретов и HTTP200 обоих сайтов; логин не выполнялся. Ручной вход владельца через Education в разных браузерах уже подтверждён и не переоткрывается как дефект.

В рамках ранее принятого поручения проверить и выпустить систему Codex готовит immutable browser bundle вне VPS и запускает его через тот же защищённый runner. D-074 остаётся неизменной историей inventory; отсутствующие image variables заменены в новом исполняющем пути фактическим off-host build, digest и portable fingerprint. Строка ACCOUNT_CONFIRMED не подделывается: выделение учётки подтверждено владельцем, активный non-owner статус, постоянный пароль, явный Education grant и совпадение School identity проверяются реальным входом.

Полный контракт и ограничения: `docs/PROJECT_PASSPORT_PRODUCTION_RU.md`, D-076; реализация и workflow audit: `deploy/browser/README.md`. Этот путь не меняет старые R1–R7 consumers, не устанавливает пакеты на VPS, не выпускает приложение автоматически и не ослабляет release receipt/backup/public-write gates. Настоящий вход имеет штатные auth/session/SSO side effects; он не называется strict read-only D075 probe. Продуктовые записи, роли, платежи и синхронизации тест не изменяет.

## D-075 — Отдельное read-only наблюдение рабочих данных

Дата: 2026-09-08. Владелец: Виталий; исполнитель: Codex. Статус: approved, реализация начата.

Виталий ответил «Разрешаю» на отдельный запрос проверки банковского реестра, прав, обращений, импорта и бэкапов через существующий служебный доступ, без изменения данных, извлечения паролей и выгрузки персональных записей. Это отдельная область после D-074, не изменение исторических ограничений его prerequisite-проверки. Разрешены необходимые обезличенные агрегаты из канонической живой D1 на сервере. Источник сверяется с открытыми файловыми дескрипторами приложения; обычный UID1000 получает существующий volume только read-only в изолированном временном helper без сети, secrets и Docker socket. Нет root/sudo, копирования базы, upstream sync, платежей, миграций, restore или переключения продукта.

Источник решения, контракт, ограничения и критерий завершения находятся в паспорте `docs/PROJECT_PASSPORT_PRODUCTION_RU.md`, D-075. Первый probe различает отсутствие таблиц, пустоту, ограничение объёма и нарушения ссылок; это не проверка UI/RBAC или финансовой сверки с банком. Наличие backup-control отдельно от проверенной истории бэкапов. Старые consumers, exact-SHA release и SSO gates сохраняются. Подтверждённый владельцем вход в дневник в нескольких браузерах не переоткрывается как дефект.

## D-074 — Серверная браузерная проверка начинается с read-only диагностики выделенного окружения

Дата: 2026-09-08  
Статус: принято в рамках явного поручения владельца «Тогда действуй» после предложения проверки через существующий self-hosted runner  
Владелец решения и тестовых доступов: Виталий; исполнитель: Codex

Разрешён первый этап подготовки изолированной серверной браузерной проверки на существующем `arthello-gateway` в защищённом Environment `production-ru`. Реализация опубликована PR #363, main `b7183df258105a5a94446121ad66f8a0b7d73d2c`. Запуск — после успешного Quality текущего main либо вручную владельцем на main; PR/fork source не выполняется на production-capable runner. Общие gateway/School locks и существующие release consumers сохраняются. Используется только уже установленный runtime, включая Node самого Actions runner; host package installation не разрешается.

Диагностика проверяет фиксированные ArtHello/School HTTPS origins методом HEAD без авторизации и переходов, наличие специально выделенных Environment inputs и metadata точного уже загруженного browser image по immutable ID. Не запускает браузер или контейнеры, не создаёт учётные записи, не читает production БД/пароли/ключи и не меняет данные, сеть, права или рабочие контейнеры. Тестовый логин/пароль задаются отдельно; canonical owner и переиспользованные учётки сотрудников не допускаются. TLS bypass, платежи, широкие импорты, session injection и запись неподтверждённого SSO receipt запрещены.

Результат — минимальный обезличенный prerequisite report. `blocked` фиксирует отсутствующие/неверные входы или недоступность; `prerequisites_observed` означает только наблюдение окружения. В обоих случаях `liveAcceptance=not_run`. Выделенный browser bundle, активная тестовая учётка и настоящие шесть сценариев не считаются готовыми до собственных доказательств. Отсутствующие полномочия/доступы — причина остановки и запроса владельцу, не разрешение добывать credentials из production.

В ходе PR #363 выявлена гонка существующего CI visual harness: однократный DOM-замер через1.8 секунды дал desktop ROOT_NOT_RENDERED, хотя соответствующий screenshot уже содержит форму и mobile прошёл. Добавлено ожидание complete document и rendered root с отказом по timeout; канон и все прежние визуальные assertions сохранены. 12 prerequisite и7 readiness TDD-тестов включены в test:full. Эти CI проверки не заменяют production SSO.

D-073 и fresh identity-bound natural browser receipt, exact-SHA CI/image, clone-preflight, проверенный backup, public-write boundary и запрет восстанавливать старую БД поверх новых записей остаются обязательными. Новый автоматический деплой этим решением не вводится. Исходное поручение завершено только после реальной навигации Education→дневник, проверки Точки/ДДС, прав сотрудников, обращений, выборочного AlfaCRM и бэкапов на опубликованном кандидате.

Подробный scope, контракт AI-процесса, остановка/отказ и следующий шаг: `docs/PROJECT_PASSPORT_PRODUCTION_RU.md`, D-074. Технические inputs и границы: `docs/acceptance/server-browser-prerequisites.md`. Эта запись устраняет найденный ревьюером пропуск канонического журнала и не расширяет уже подтверждённые полномочия.

## D-073 — Бэкапы через отдельный Docker worker без host sudo; отдельный непривилегированный авторитет активации Точки

Дата: 2026-09-08  
Статус: необходимое устранение подтверждённого препятствия публикации в рамках явного поручения владельца  
Владелец: Исполнитель ArtHello OS

PR361 слит как eb47c1360fbd701876a3c49efe029194707304db. Exact PR/main Quality, Proof, Verify прошли; приложение756/756. R6 run34221458013/job102045375780 в11:35:42Z заново подтвердил существующий School R5 read-only с первоначальной активацией09:16:10Z. Затем проверка обнаружила gateway UID995 без root и без уже доступного passwordless sudo. Host-установщик бэкапов не запускался, image/clone/cutover пропущены. Живой ArtHello SHA6596f69390ad539577ec2640e8ef40c7e12c22dc прочитан непосредственно в этом run. Это отказ требуемого способа установки, а не ошибка backup.py и не разрешение получать host root.

Выбран новый R7/D069 из нового точного проверенного образа. Backup worker работает UID1000:1000, которому уже принадлежит приложение; получает только точный существующий D1 volume read-only с volume-nocopy и canonical path, подтверждённым открытыми файловыми дескрипторами рабочего приложения. Чужие права не изменяются. Backups и backup-control — новые именованные volumes с точными release labels. Владельцы и modes0700/0750 задаются при hosted image build, затем проверяются после стандартного Docker copy-up. При несовпадении — отказ без production chown. Неизвестные существующие ресурсы не принимаются за собственные.

Сохраняются SQLite Online Backup, отдельное восстановление с проверкой содержимого, flock, fsync и атомарный manifest. Первая проверенная копия обязательна до включения ежедневного расписания00:15UTC и до остановки старого ArtHello. Manual и scheduler имеют общий admission;202 означает запуск, а не завершение. Состояние расписания устойчиво к перезапуску, пропущенный запуск догоняется без ложной записи об успехе. UI и owner/Origin/CSRF права сохраняются. Приложение получает только backup-control read-only; сокет выполняет только фиксированный status/create контракт. Backups остаются на том же сервере и охватывают только ArtHello SQLite, без School, внешних файлов и ключей.

Автоактивация Точки переносится в отдельный volume, недоступный backup worker. Каталог принадлежит UID1002:GID1000, mode0750; marker0640. ПриложениеUID1000 монтирует этот volume только read-only. Единственный writer запускается protected deployment job под UID1002 из того же immutable image только после существующих public/current-main/secret gates. Новый фиксированный V2 протокол требует exact release SHA и свежий nonce; чтение на каждом tick сохраняет UID/GID/mode/regular-file/size/no-follow/nonblocking проверки. Старый V1 marker не активирует новый выпуск. UID1002 — ограниченный владелец собственного контейнерного ресурса, не новая host-admin учётная запись. Просто разрешить UID1000 или любого владельца в прежнем общем backup-control запрещено.

Все новые вспомогательные контейнеры — network:none, read-only rootfs, cap-drop ALL, no-new-privileges, ограниченные ресурсы, без secrets и Docker/systemd socket. Writer не получает D1, backups или backup-control. Новых host-root mounts, sudoers, privileged/root helpers, host package installation и изменения чужих файлов нет. Прежние уже разрешённые scoped Docker clone/snapshot helpers и Caddy route operations сохраняются; весь consumer не объявляется выполняющимся без container UID0.

Независимый design/rights review признал это уменьшением требуемых полномочий с сохранением независимого разрешения активации. Перед публикацией обязательны реальные hosted Docker проверки copy-up UID/mode, RO WAL backup при записи, restore, socket/manual/restart/catch-up и невозможности записи activation приложением/worker. Отдельно проверяются производственные exact mounts и первая копия. Durable public-write boundary и запрет восстановления старой БД поверх новых записей не меняются; при неоднозначном public результате сохраняются текущая БД, кандидат и вспомогательные ресурсы.

School R5 остаётся неизменным, проверяется существующим R6 read-only verifier. Настоящий Education→дневник SSO, exact PR/main CI/image и живая приёмка шести сценариев остаются обязательными. Недоступность облачного браузера и неподтверждённый финансовый PayType AlfaCRM не превращаются в PASS.

Источник поведения volumes: https://docs.docker.com/engine/storage/volumes/ (стандартный copy-up и отдельные RO/RW mounts; точные UID/modes доказываются исполняемыми тестами, не предполагаются).

## D-072 — Сохранить установленный School R5 и читать денежный депозит из подтверждённого поля AlfaCRM

Дата: 2026-09-08  
Статус: необходимые исправления и продолжение выпуска в рамках явного поручения владельца  
Владелец: Исполнитель ArtHello OS

R5 PR360 слит как ee8f3080d941a12be357eec0fd1d902acd01a2f9. Exact Quality34208709041/Proof34208709085/Verify34208709057 прошли, включая 733/733 теста приложения. Actual run34208952716/job102005327418 установил ограниченный School relay в09:16:10Z и подтвердил его в09:16:12Z: config2ba0ad1ce530db9c9abb622e5727e6345ab74a85699e121b718d9a0405b1f40a, UID1000, прежний School source54242340f2d9b6a9887d69ecc03520ddf9f7982c. School-to-ArtHello DNS/HTTPS200 работают, но настоящий browser SSO ещё не подтверждён. Image/clone/ArtHello cutover пропущены. Результат сохранён отдельно от passing acceptance.

Main затем получил два изменения ленивой конфигурации и достиг c9e7da06f7d3637c0032af542085f28acb02816a. Эти изменения сохраняются. Quality34212381727 выявил устаревший source assertion ALFACRM_DOMAIN; обновление проверки обязано сохранять фактический отказ при отсутствующем/пустом tenant до fetch. Старый R5 не перезапускается с другим main и не перепривязывается.

Для нового точного выпуска R6 установка School R5 и кандидат ArtHello имеют отдельные identity. Уже установленный relay проверяется read-only по прежнему install SHA, фактическому receipt и полному контракту runtime/School/network; его labels, файлы, контейнер и конфигурация не изменяются. Исходный R5 controller и старые consumers сохраняются. Новая release identity требует своих PR/main Quality/Proof/Verify, защищённого Environment, общих блокировок, immutable artifact и настоящей свежей natural SSO evidence. Проверка необходимых gateway backup полномочий может выполняться ранее как read-only prerequisite; это не разрешает получать новые права.

Официальный AlfaCRM API https://alfacrm.pro/usefull/integrations/integration/api определяет Customer.balance как деньги и Customer.paid_lesson_count как остаток занятий. Единицы CustomerTariff.balance и числовое соответствие pay_type_id5/12 не определены. На выбранном этапе депозитов читается свежий Customer.balance по уже выбранным/импортированным клиентам и филиалам. Monetary projection получает собственную lineage; raw абонементы сохраняются без неподтверждённого перевода balance в деньги. Отсутствующий/нечисловой остаток не превращается в0. Истинный ноль остаётся допустимым.

Карточка семьи показывает только относящиеся к ней разрешённые денежные snapshots с источником и временем наблюдения; филиальные и модульные ограничения сохраняются. CRM-остаток не подменяет банковские операции и ДДС. Payment observations без проверенного соответствия типа направлению не считаются успешно импортированным финансовым фактом; raw происхождение и причина неполного результата сохраняются. Автоматическое расписание, широкий исторический импорт и включение live gate не вводятся.

Перед публичным переключением остаются clone-preflight, проверенный backup и защита новых записей после открытия; активация Точки выполняется только после предусмотренных проверок. Успешная подготовка, CI и School transport не равны завершению шести пользовательских сценариев.

## D-071 — Сравнивать School mounts по содержимому и не заявлять неполный Alfa импорт успешным

Дата: 2026-09-08  
Статус: необходимое исправление подтверждённых дефектов в рамках поручения владельца исправить систему и опубликовать обновления  
Владелец: Исполнитель ArtHello OS

D066 run34206196494/job101996459827 прошёл exact gates, сузил права собственного lock0664→0644 и остановился на school_changed_during_setup до запуска relay. Image/cutover ArtHello пропущены; failed repair не перезапускается. Read-only PR359 maince7c50ba367fca14d71669305bf4e82b791ce4f7, run34207628005/job102000795971 от09:00:57Z воспроизвёл дефект на реальном Docker: в12 наблюдениях только порядок двух Mounts менялся, полный канонический School fingerprint e1f9dbfd5172ad224b97dfd215c81b914b1be6872e322128e05477b71ea417aa и backend fingerprint86218fb5096b6225e4e45d23d396b44498553e57a9fe9ddb5cd4b11fdf3489be оставались неизменными. Исходная пара failed-run inspect не сохранялась и не реконструируется. School healthy, image/source/StartedAt прежние; созданные relay/egress/config-dir действительно отсутствуют после очистки, own state-root0700 и lock0644 валидны. Оба публичных health endpoint с gateway ответили200; отдельная cloud-browser502 не объявляется падением сервиса.

Новый bundle R5 нормализует только порядок полного массива Mounts при расчёте fingerprint. Каждый ключ/значение, неизвестные поля и число дубликатов сохраняются. Config, HostConfig и сетевые массивы не сортируются и не ослабляются. Изменение Source/Destination/RW, добавление/удаление/дублирование mount по-прежнему блокирует. Старый R3 bundle и все failed consumers остаются прежними. Новый технический release D067/PR360 имеет свои OWNER/config/source pins; D067 в имени задачи не заменяет архитектурное D-067 из рефакторинга.

R5 повторно использует неизменённые R4 transport/нормализатор lock и schema2 receipt validator. Дальнейшая установка требует exact PR/main Quality/Proof/Verify и immutable image; mandatory Docker smoke дополнен настоящими многократными inspect контейнера с двумя readonly mounts и соседним контейнером в изолированной сети. Shared queues, protected Environment/SSH identity, fixed TLS upstream/source ACL, сохранение School и запрет root/sudo сохраняются.

Узкая Alfa correctness правка устраняет три доказанных ошибки до разрешения live-импорта: отсутствующий/нечисловой balance отклоняется вместо ложного нуля и сохраняет прежнюю проекцию; import last_success_at обновляется лишь при полном результате без rejected; ошибочный пакет аннулирует preview/cursor, чтобы последующий пакет не скрывал ошибку; preview действует30мин и проверяется до credentials/upstream/write. Подтверждённый ноль остаётся допустимым. Новая денежная интерпретация balance, перенос CRM-сумм в банковский ДДС, расписание autosync и включение live gate этим решением не вводятся. Реальные credentials/coverage/единицы API и вывод CRM-снимков в карточке семьи остаются непринятыми.

Перед ArtHello cutover сохраняются реальные свежие Education→Diary browser evidence, clone-preflight, проверенный backup, защита новых записей после публичного открытия и отложенная активация Точки. Успешный код/CI/relay не заменяет живую приёмку шести сценариев. Изменения параллельного рефакторинга D-066/D-067/D-068/D-070 сохраняются целиком.

## D-069 — Исправить права собственного School lock и сохранить очередь выпуска

Дата: 2026-09-08  
Статус: необходимое техническое исправление в рамках поручения владельца восстановить систему и опубликовать обновления  
Владелец: Исполнитель ArtHello OS

PR356 слит как M3 `8a0bf989e0c271b009d8f14f0f3d21b3ab47966c`; exact Quality34198562442, Proof34198562451 и Verify34198562465 прошли, включая 724 теста приложения. D065 run34198718003 был сначала отменён до создания jobs общей очередью, затем владелец запустил attempt2. Job101982122878 прошёл source gates, но bootstrap отказал; image/clone/cutover пропущены. Отказавший repair не перезапускается.

Отдельная read-only диагностика PR357, main `52770ad3f10b0ed874263c28e932eb532d77e461`, run34203383943/job101987156275 от 08:14:10Z доказала: реальные/effective UID/GID1000; passwd-home0750 собственного владельца и безопасные предки; существующий School lock regular, uid1000/gid1000, mode0664, nlink1, стабильный inode и доступ на чтение. Нарушено именно отсутствие group-write. State-root R3 и relay container отсутствуют; School image/source/StartedAt/network совпадают с проверенным baseline и healthy. Для egress network диагностический статус unknown сохраняется unknown, а не объявляется отсутствием.

R4 может только сузить права ЭТОГО существующего собственного lock с exact0664 до0644. До изменения обязательны проверка защищённой release identity и frozen bundle, совпадение реального/effective UID/GID1000, fixed path, regular file, owner/group1000, nlink1, стабильность path/fd inode. Открытие только existing descriptor O_RDONLY|O_NOFOLLOW|O_NONBLOCK, bounded exclusive flock; под lock выполняются повторная проверка, fchmod(fd,0644), fsync и подтверждение того же inode/владельца/режима. Exact0644 — идемпотентное чтение без повторной мутации. Другие modes/owners/types, отсутствующий файл и неоднозначность блокируют. Запрещены create/truncate/replace/chown/root/sudo и изменения чужих файлов. Права owner-write и общий path/inode сохраняются; исторические remote consumers используют того же DEPLOY_USER. Нормализация фиксируется как реальная конфигурационная мутация; последующий отказ не скрывает её и не возвращает0664. Перед запуском controller lock освобождается, controller заново берёт ту же общую блокировку.

Для исправления доказанной отмены ожидающего выпуска используется поддерживаемая GitHub очередь `queue: max` при `cancel-in-progress: false`. Ключи общей gateway workflow concurrency и School job concurrency не меняются. Только активные D059/D063/D064/D065 consumers того же Verify workflow_run получают параметр queue; все их source/PR/parent/replay/Environment/step guards остаются побайтово прежними. Исторические manual/push workflows не меняются и не запускаются. Старые snapshot attempts не считаются обновлёнными и не перезапускаются. Новая семантика очереди опубликована GitHub 2026-05-07: https://github.blog/changelog/2026-05-07-github-actions-concurrency-groups-now-allow-larger-queues/ .

Новый consumer закреплён за PR358, `codex/school-arthello-recovery-r4-20260908` и parent3cc30b6ad2790bfe15d43ab38adebb9cdf6a1f26. R3 relay bundle/config остаётся неизменным. R4 требует собственные exact first-attempt PR/main checks и immutable image, защищённую доставку, durable repair receipt, свежую естественную Education→Diary приёмку, clone/verified backup и D063 public-write boundary. Прежний неуспешный D065 не перепривязывается. Изменение очереди или прав lock не является завершённым SSO/публикацией/приёмкой шести сценариев.

После подготовки обнаружен новый main `3cc30b6ad2790bfe15d43ab38adebb9cdf6a1f26` с отдельным рефакторингом Фазы 1. Все его изменения сохраняются. Его Quality34204053510, Proof34204053460 и Verify34204053516 успешны; R4 всё равно требует новых exact PR/main проверок. D-066/D-067/D-068 заняты решениями рефакторинга; это решение имеет номер D-069, а D066 в именах release-файлов остаётся технической меткой задачи.

## D-070 — Восстановить Drizzle metadata из точных schema trees и доказать security migrations отдельно

Дата: 2026-09-08
Статус: принято владельцем в явном поручении выполнить пункты 2.1–2.2 рефакторинга
Владелец: Владелец ArtHello OS

Snapshots 0016–0017 восстанавливаются генерацией из точных исторических деревьев схем после соответствующих миграций, с непрерывной цепочкой `prevId`. Приёмка требует пустого `drizzle-kit generate` на текущей неизменной схеме; ручное конструирование неизвестного состояния запрещено.

`migration-twin.mjs` отдельно доказывает 0009–0010 на синтетических owner/accountant/viewer sessions и login-attempt evidence: owner становится unrestricted, non-owner sessions отзываются, audit доступен, rollback 0010 сохраняет sessions, но не восстанавливает отозванные токены, а rollback 0009 явно признаётся деструктивным. Этот disposable PostgreSQL 16 proof не заменяет открытый restored-sandbox gate и не разрешает production migration.

## D-065 — Устанавливать School relay с имеющимися правами служебного пользователя

Дата: 2026-09-08  
Статус: принято как уменьшение требуемых привилегий в рамках поручения владельца восстановить систему и опубликовать исправления  
Владелец: Исполнитель ArtHello OS

PR355 слит как 8d4fc1cb589db8550cc4e7857e1a7537ac771ae5 (M2). Exact first-attempt main Quality 34196009060, Proof 34196008965 и Verify 34196009029 успешны; в настоящем hosted Docker проверены DNS, TLS, UID1001 port443, source ACL и 724 теста приложения. D064 run 34196169336 / job 101964316036 остановился на sudo: a password is required. Bootstrap вызывает sudo до чтения stdin, создания файлов и выполнения repair.py: School relay/network/state не создавались; ArtHello image/clone/cutover пропущены.

Полные права root требовались выбранному размещению state в /var/lib, а не самому relay. Новый R3 использует существующий разрешённый Docker deployment API служебного SSH-пользователя и только его собственные файлы. Запрещено получать root через Docker, привилегированные контейнеры, host-root mounts, изменение sudoers, chown чужих host-файлов или повышение прав доступа. Если имеющихся прав недостаточно, это остаётся явным blocker.

Каталог state выводится из passwd-home фактического непривилегированного euid, а не из произвольного HOME/XDG input. Проверяются uid/getuid, безопасные владельцы и права каталогов, отсутствие symlink, exact owner файла, ограниченный размер; private state/receipt сохраняются атомарно с no-replace и fsync. Relay получает только readonly bind собственных публичных scripts/runtime.json, без state receipts, секретов, Docker socket и School данных. Receipt и labels явно фиксируют реального владельца state; root ownership не заявляется. Чужие/root-owned объекты не усыновляются и не меняются.

Сохраняются межпроцессная блокировка School, общий workflow concurrency, точная проверка current main перед мутацией, образа/контейнера/сетей/ACL, обычный DNS/TLS probe из School и проверка неизменности School. Недоступная обязательная блокировка не заменяется молча частной блокировкой. Existing verified relay допускает только read-only resume; неоднозначные или чужие состояния блокируются.

Новый отдельный consumer закреплён за PR356, веткой codex/school-arthello-recovery-r3-20260908 и parent M2. D059/D063/D064 и их failed-repair replay guards не перепривязываются. R3 требует своих exact Quality/Proof/Verify, своего immutable image, свежего естественного Education→Diary browser evidence после проверенной конфигурации relay. Все D063 backup/clone/public-write boundary и отложенная активация банковской синхронизации сохраняются. Смена метода установки не считается успешным SSO или приёмкой шести пользовательских сценариев.

## D-064 — Восстановить точечную связь School с ArtHello и продолжить выпуск R2

Дата: 2026-09-08  
Статус: принято как необходимое исправление подтверждённого runtime-дефекта в рамках исходного поручения владельца  
Владелец: Исполнитель ArtHello OS

PR #354 слит как 68a159dc647f35cdd30ce586fc6d5beb93b89a51. Точные main Quality, Proof и Verify успешны. D063 run 34193103822 дошёл до read-only School diagnostic и остановился до image/clone/cutover: School на внутренней Docker bridge arthello-os_backend имеет EAI_AGAIN для DNS и HTTPS ArtHello, а callback сообщает fetch_failed. Файловые права UID1001, quick_check и требуемая схема исправны. Этот результат доказывает первый сетевой blocker, но не исключает последующей ошибки SSO после его устранения.

Исправляется только связь School с фиксированным ArtHello origin. Общая internal network не открывается. Отдельный ограниченный TCP relay получает единственный внутренний DNS alias arthello-188-225-38-55.sslip.io и пересылает соединения только на 188.225.38.55:443. TLS остаётся сквозным: School проверяет настоящий сертификат ArtHello; секрет, origin и callback protocol не меняются. Relay использует уже проверенный и загруженный School image только как Node runtime с явным entrypoint, без приложения, School volumes, environment credentials и секретов. Требуются непривилегированный user, read-only root/script, ограниченные соединения/время/объём, отсутствие опубликованного host port и отдельная egress network. Сетевой smoke обязателен на hosted runner. После подключения точечного alias проверяются конфигурация relay, TLS upstream и запрос из School; при неудаче удаляются только созданные этой попыткой relay и отдельная сеть.

До мутации сверяются точные School image/revision/container/network и отсутствие конфликтующего alias. Идемпотентный повтор допускается только для полностью совпадающей проверенной конфигурации по durable receipt; неоднозначное состояние блокируется. Не выполняются изменения School базы, пользователей, сессий, ключей, образа или общей сети. Rollback удаляет лишь созданные в этой попытке relay/network, не восстанавливая старую School БД поверх новых записей.

Новый отдельный R2 workflow закреплён за PR #355, веткой codex/school-arthello-recovery-r2-20260908 и parent main 68a159dc647f35cdd30ce586fc6d5beb93b89a51. Он повторно требует exact first-attempt Quality/Proof/Verify уже нового merge SHA и соответствующий immutable ArtHello image. Ни D059, ни D063 не перепривязываются. После School network repair выполняются диагностика и настоящий естественный browser SSO; только свежее matching acceptance допускает ArtHello cutover. M1 image не выдаётся за новый M2 source.

Все data/backup/clone/public-write boundary и activation marker требования D063 сохраняются. До начала ArtHello cutover разрешён ограниченный повтор лишь при доказанном пропуске этого шага и read-only подтверждении прежнего School repair; начатый или неоднозначный ArtHello cutover запрещает replay SHA. Отчёт о сетевой доступности не считается успешным входом и не закрывает исходные шесть пользовательских сценариев.

## D-063 — Выпустить объединённые исправления через доступный hosted builder и защищённую доставку

Дата: 2026-09-08  
Статус: принято как реализация явного поручения владельца выпустить все исправления в production  
Владелец: Исполнитель ArtHello OS

Свежие Verify и Quality PR #353 прошли на GitHub-hosted runners; private artifact успешно сохранён. Прежняя блокировка минут и хранилища не считается текущим доказанным препятствием. Подготовка отдельного собственного builder сохранена в draft PR #353, но наличие или приобретение такой машины не заявляется. Для текущего выпуска используется фактически доступный hosted builder; production runner выполняет только проверенную доставку готового immutable image и cutover, без checkout PR head, npm install, codegen или сборки приложения на production.

Новый отдельный workflow deploy-arthello-recovery-20260908.yml закреплён за recovery-веткой codex/recovery-release-20260908, PR #354 и исходным main 9e4c49161e643997fe821a91b84c60c6e38ede33. До доставки он проверяет текущий подписанный squash main, точный source tree, принадлежность PR и actor, первый успешный attempt exact-SHA Quality, Proof и Verify, приватный artifact, его checksum, evidence и переносимый runtime fingerprint. Старый одноразовый D-059 controller не изменяется и не переиспользуется.

Первое обращение к production — ограниченная read-only диагностика School без вывода секретов, callback codes, сессий и строк персональных данных. До резервной копии, clone и cutover требуется настоящее свидетельство естественного входа из Обучения ArtHello в видимый авторизованный дневник School. Свидетельство хранится отдельно в docs/acceptance/2026-09-08-school-live-acceptance.json ветки codex/recovery-evidence-20260907; его отсутствие не заменяется заглушкой. Оно связывает intendedCandidateArtHelloSha с кандидатом, observedLiveArtHelloSha и observedLiveSchoolSha с реально наблюдаемыми живыми версиями, содержит точные HTTPS origins и упорядоченные действия браузера. Запрещены injected sessions и вручную сконструированные callback URLs. Время наблюдения не может быть будущим или старше 45 минут. Отсутствие ошибок в логе само по себе не является успешным SSO.

Consumer может повториться только если для всех предыдущих запусков и attempts доказано, что cutover не начинался: соответствующий шаг пропущен либо весь job пропущен без выполненных шагов. Любой начатый, успешный или неоднозначный cutover запрещает повтор того же SHA. Повтор не ослабляет exact-source Quality, Proof и Verify.

После School gate обязательны изолированный clone-preflight, проверенная резервная копия канонической SQLite, проверка свободного места, backup-first cutover и сохранение прежнего контейнера. Автоматический snapshot rollback допустим лишь до durable public-write boundary: локальные проверки выполняются до неё, а перед первой возможной публичной записью сохраняется fsync marker. После этой границы ошибка сохраняет candidate и новые данные, выдаёт RECOVERY_REQUIRED и запрещает восстановление старого snapshot поверх пользовательских записей. Backup bridge использует постоянный host-каталог, read-only socket mount и выделенную числовую группу; приложение не получает Docker socket. Clone не получает bridge и не запускает внешнюю синхронизацию. TOCHKA_AUTOSYNC_ENABLED=1 разрешён только production-контейнеру с сохранённой конфигурацией, но сам по себе не активирует таймер. Перед каждым tick требуется точный root-owned read-only marker, связанный с SHA и новым nonce текущей попытки; host записывает его атомарно с fsync только после успешных публичных проверок. ALFACRM_IMPORT_ENABLED не включается до фактической приёмки credentials, tenant, scope и полноты выбранной загрузки.

Успешная доставка и health не закрывают исходные шесть требований владельца. После cutover обязательна новая проверка естественного входа на опубликованной версии и живая приёмка банка, доступов, обращений, AlfaCRM и резервных копий. Неисполненные внешние синхронизации и проверки отражаются как незавершённые, без утверждения «всё работает».

## D-062 — Синхронизировать Точку по сохранённым настройкам без открытого браузера

Дата: 2026-09-08  
Статус: принято как реализация исходного поручения владельца о самостоятельной синхронизации счетов и реестра для ДДС  
Владелец: Исполнитель ArtHello OS

Node runtime запускает только ограниченный read-only исполнитель INT-T-TOCHKA через внутренний Miniflare dispatchFetch. Одноразовый служебный ключ создаётся в памяти процесса; он не возвращается пользователю, не хранится в Git/БД и не журналируется. Тело служебного запроса не может выбрать иное действие, подключение или платёж.

Автоматическая загрузка использует только уже сохранённые и подтверждённые компанию, юрлицо, разрешённые банком счета, дату начала и расписание. Lease, credentialGeneration, устойчивые statement IDs и атомарный commit исключают устаревший импорт и дубли. Явная пауза и отзыв/замена ключа блокируют дальнейшее исполнение; временная ошибка допускает ограниченный повтор с backoff. Неполная/отклонённая выписка не становится успешной синхронизацией и не обновляет last_success_at.

TOCHKA_AUTOSYNC_ENABLED=1 включается только у целевого production-контейнера после проверенного cutover. Clone/preflight и восстановленные копии данных остаются с выключенным планировщиком. Создание, подписание и отправка платежей запрещены. Неизменяемые банковские факты и ручная управленческая классификация из D-060 сохраняются.

Приёмка требует реального запуска без открытой страницы, сверки количества и сумм операций с банком, продолжения pending и повторного запуска без дублей. Локальные tests не заменяют эту приёмку. Модель одной настройки INT-T-TOCHKA не объявляется подключением нескольких неподтверждённых юрлиц.

## D-061 — Проверять релиз на отдельной машине сборки GitHub

Дата: 2026-09-08  
Статус: принято по явному поручению владельца вернуться к GitHub + собственной машине сборки и выпустить исправления  
Владелец: Исполнитель ArtHello OS

GitHub остаётся репозиторием и источником истории. Перенос в GitVerse приостановлен. Рассмотренный release-кандидат можно проверять до merge на отдельной одноразовой build-VM без production credentials, volumes, чужих host sockets, production private network и cloud control plane. Изоляция сети и удаление по TTL обеспечиваются внешним контроллером; название runner и самоописание JSON не доказывают изоляцию. Docker на builder остаётся root-эквивалентным.

Manual workflow-контроллер запускается из точного актуального main, проверяет exact SHA/tree фиксированной release-ветки codex/recovery-release-20260908 либо актуального main, и сверяет их с внешним provisioning record до checkout. Автоматический PR/PR-target не получает builder. Одна VM исполняет один job без повторного использования её диска. D-040 продолжает запрещать PR code на production runner.

Secret scan, Quality, PostgreSQL, migration/permission/visual proof, Verify v52 и проверки immutable image сохраняются. Provenance явно обозначает self-hosted-isolated-ephemeral. Production получает готовый образ через проверенную приватную доставку, затем обязательны clone-preflight, проверенная резервная копия, cutover, rollback guards и живая приёмка шести пользовательских сценариев. Успешная сборка не равна успешному выпуску.

Подготовка скриптов не означает наличия VM, административных полномочий регистрации runner или работающего artifact storage. Стоимость и приобретение нового сервера/хранилища не согласованы и не выполнялись. Старый одноразовый D-059 deploy не переиспользуется; новый consumer должен проверять фактический контракт собственной сборки. Данные и действующая версия сохраняются до доказанного безопасного cutover.

## D-068 — Разделить import-specifier policy по runtime-контурам

Дата: 2026-09-08
Статус: принято владельцем в явном поручении завершить три пункта Фазы 1
Владелец: Владелец ArtHello OS

TypeScript, исполняемый Node напрямую или через `tsx`, и библиотека БД используют явные относительные `.js` specifiers, совместимые с ESM output. Vite/browser и generated-контуры сохраняют extensionless specifiers, которые разрешает bundler. Механическая единая политика для обоих контуров запрещена. Постоянный тест проверяет Node-контуры, а API build и полный typecheck являются runtime/compiler proof.

Legacy formatting включается ratchet-гейтом: известный baseline может только сокращаться, а новый неформатированный файл блокирует CI. Массовое форматирование существующего application tree не смешивается с behavioral candidate.

## D-067 — Удалить универсальный legacy `/sync` runtime

Дата: 2026-09-08
Статус: принято владельцем в явном поручении завершить три пункта Фазы 1
Владелец: Владелец ArtHello OS

Утверждены dispositions реестра `docs/refactoring/legacy-sync-inventory.json`. Универсальный router `routes/sync.ts`, его mount, OpenAPI operations, generated client и операторские призывы запускать эти endpoints удаляются атомарно. Уже доказанные read-only source capabilities остаются только в guarded sandbox importer. Неподтверждённые materialization, финансовая классификация и reconciliation не переносятся автоматически: они остаются заблокированными до отдельных scoped jobs, доменных правил и evidence.

Центральный fail-closed gate `/sync/*` сохраняется для оставшейся исторической Google-leads операции и любого ошибочно возвращённого legacy path. Удаление кода не открывает production sync или live-интеграции.

## D-060 — Банковский факт неизменяем, управленческое разнесение хранится отдельно

Дата: 2026-09-04  
Статус: принято владельцем в явном поручении слить PR №338, выпустить его в production и загрузить выписки Точки  
Владелец: Исполнитель ArtHello OS

Сумма, дата, направление, банковский контрагент, назначение и идентификатор операции из `bank_transactions` являются исходным банковским фактом и не изменяются при ручном разнесении. В `financial_operations` отдельно хранятся управленческие поля: статья ДДС, класс и статья ОПиУ, период начисления ОПиУ, управленческий контрагент и смысл операции. Изменение этих полей доступно только разрешённой финансовой роли и обязательно записывается в аудит.

Каждый путь открытия карточки операции, включая строки реестра и расшифровку ОПиУ, обязан заново инициализировать черновик данными именно выбранной операции. Повторное использование черновика другой операции запрещено, потому что может привести к бухгалтерски неверному разнесению при сохранении.

Изменение схемы выполняется только идемпотентной additive-миграцией: новые поля добавляются при отсутствии, существующие банковские и финансовые строки не удаляются. До production-активации обязательна проверенная резервная копия, после запуска — проверка целостности, сохранности финансовых данных и публичного health; при ошибке сохраняется автоматический откат.

Повторная загрузка выписок Точки выполняется read-only и идемпотентно по всем подтверждённым счетам за явно заданный период, с последующей сверкой количества операций и сумм поступлений/списаний с ответом банка. Правила автоклассификации повторяющихся платежей являются отдельным следующим этапом и не изменяют банковский факт.

Историческая техническая метка релиза `D069` в именах patch/workflow — имя банковской релизной задачи и не ссылается на принятое позднее архитектурное D-070 о Drizzle metadata.

## D-066 — Использовать единую семантику Zod v4

Дата: 2026-09-04
Статус: принято владельцем в явном поручении продолжить унификацию Zod
Владелец: Владелец ArtHello OS

ArtHello OS использует API Zod v4 как единственную семантику runtime-валидации. Ручной код, схемы слоя данных и сгенерированные API-схемы импортируют Zod через публичную точку `zod/v4`; корневой импорт `zod`, предоставляющий совместимость с Zod v3 при текущем переходном пакете, запрещается постоянным anti-drift гейтом.

Это решение относится к поведению, а не только к написанию импортов. До перевода действующих validator characterization-тесты фиксируют принимаемые и отклоняемые значения, результат parse, обработку неизвестных полей, optional/nullish/default, coercion и публично наблюдаемую структуру ошибок. Миграция не получает права незаметно исправлять или ужесточать API-контракт: выявленное различие сохраняется совместимым либо выносится в новое D-решение.

`lib/api-spec/openapi.yaml` остаётся источником generated API-контракта. Orval явно нацеливается на Zod v4; повторный codegen не должен возвращать корневой импорт и после второй генерации обязан давать нулевой diff. Текущий точный pin `zod: 3.25.76`, уже предоставляющий `zod/v4`, этим решением не повышается: переход package pin на major 4 требует отдельной проверки совместимости Orval, drizzle-zod и остальных потребителей.

Приёмка требует characterization-suite, полного запрета корневого импорта, `pnpm run typecheck`, `pnpm run test:full`, `pnpm run build:full` и воспроизводимого codegen. Откат возвращает миграционный PR целиком; частичный откат к неучтённой смеси v3/v4 запрещён.

## D-059 — Исправить совместимость workerd service binding без перехода на OAuth

Дата: 2026-09-03  
Статус: принято как продолжение подтверждённого production-дефекта D-058; одноразовый release PR `#328` закреплён  
Владелец: Исполнитель ArtHello OS

D-058 опубликован production run `33762398372`: точный SHA `7e926f4c29aa7e3c3208a55451db035714861e28` активен, Node-контейнер получил ожидаемый защищённый HTTP 403 от Точки. Повтор владельца на `IMG_9996(2)`/`IMG_9997(2)` снова завершился сообщением «Не удалось связаться с Точкой». Воспроизведение на закреплённой Miniflare `4.20260515.0` установило причину: workerd не реализует `redirect: "error"` и отклоняет внутренний Request до вызова `TOCHKA_TRANSPORT`.

Внутренний worker → service binding hop использует `redirect: "manual"`, который принимает workerd. Это не разрешает банковские редиректы: Node-мост повторно проверяет официальный origin, точные read-only методы и пути и выполняет внешний `fetch` с `redirect: "error"`. Заголовки по-прежнему очищаются, платёжные пути запрещены, JWT не журналируется. Ответ Точки читается внутри 45-секундного таймаута и ограничивается 2 000 000 байт, поэтому задержавшееся или чрезмерное тело не может бессрочно удерживать запрос.

Переход на OAuth сейчас не выполняется. Ошибка возникает до банковской аутентификации, поэтому OAuth token exchange и последующие API-вызовы встретили бы тот же дефект транспорта. Кроме того, OAuth требует зарегистрированного приложения, `client_id`, `client_secret` и redirect URI; его следует пересмотреть при подключении сторонних компаний от имени клиентов, а не как обход этого сбоя.

Одноразовая выкладка разрешена только из `vitaliyozolin-dotcom/ArtHello-OS:codex/d059-tochka-binding-redirect-20260903` через PR `#328`, прямого потомка D-058 `7e926f4c29aa7e3c3208a55451db035714861e28`. Приёмка требует 8/8 transport/service-binding тестов, first-attempt exact-SHA Quality/Proof/Verify, production Node TLS egress, изолированный clone-preflight, backup-first cutover, rollback guards и публичный health-check. После релиза владелец повторяет подключение: допустимы либо реальные данные, либо конкретный HTTP-ответ банка о ключе/правах; общая транспортная ошибка недопустима.

## D-058 — Выполнять read-only запросы Точки через ограниченный Node service binding и не увеличивать форму на iPhone

Дата: 2026-09-03  
Статус: принято по повторному фактическому дефекту; одноразовый release PR `#327` закреплён  
Владелец: Исполнитель ArtHello OS

D-057 успешно прошёл hosted Quality/Proof/Verify и production deployment `33752514068`: отдельный Node‑контейнер подтвердил TLS‑доступ к Точке ожидаемым защищённым HTTP 403. Повторная проверка владельцем с реальным ключом всё равно завершилась общей ошибкой «Не удалось связаться с Точкой». Это отделяет доступность Node от фактического пути приложения: API‑маршрут исполняется внутри Miniflare/workerd, чей `fetch` не использует Node `NODE_EXTRA_CA_CERTS`. Скриншоты `IMG_9991`/`IMG_9992` также подтверждают автоприближение iOS после фокуса на поле ключа с размером шрифта 13 px.

Только четыре документированных read-only вызова Точки передаются из worker в Node через function-valued Miniflare service binding: клиенты, счета, создание отчёта-выписки и чтение готовой выписки. Мост принимает только официальный origin, точные методы и пути, JWT Bearer, ограниченный JSON выписки; удаляет cookie/forwarded и лишние ответные заголовки, запрещает query, redirects, чужие origin и все платёжные пути. Ключ не журналируется. Production preflight вызывает экспорт того же модуля без ключа, а Docker image явно содержит этот модуль. Мобильные `input/select/textarea` мастера получают 16 px — это устраняет автозум Safari без запрета пользовательского масштабирования.

Одноразовая выкладка разрешена только из `vitaliyozolin-dotcom/ArtHello-OS:codex/d058-tochka-node-bridge-ios-20260903` через PR `#327`, являющийся прямым потомком production‑коммита `1cdca3fd9cb63f6dd7c8d8ce137e4126b4fc1123`. Завершение требует целевых transport/UI тестов, first-attempt hosted Quality/Proof/Verify точного SHA, `ARTHELLO_TOCHKA_TLS_EGRESS=VERIFIED` из реального transport-модуля, изолированного clone-preflight, backup-first cutover, rollback guards и публичного health-check. Фактическое принятие ключа и импорт банковских данных подтверждает владелец после релиза; ответ 401/403 банка не считается транспортной ошибкой и должен отображаться отдельно.

## D-057 — Проверять TLS Точки отдельным stateless-контейнером без секретов

Дата: 2026-09-03  
Статус: принято как fail-closed продолжение порученного D-055/D-056; одноразовый release PR `#326` закреплён  
Владелец: Исполнитель ArtHello OS

D-056 merge `a48eed30b3a0910c603ff7ad2285a42b3785a507` прошёл Quality, Proof и Verify. Production run `33750926328` успешно прошёл раннее место отказа D-055, загрузил и сверил hosted image и School secret, запустил read-only clone на копии данных и подтвердил его health/D1. Затем Docker штатно запретил `network connect` для контейнера, созданного в private `none` mode. Workflow запустил и подтвердил rollback; live-контейнер не был остановлен, backup/cutover не начинались.

Stateful clone сохраняет `--network none` на всём жизненном цикле. TLS-проверка переносится в отдельный одноразовый контейнер того же immutable image с `--read-only` и production network, но без data volume, env-file, банковского ключа, Authorization и любых secret mounts. Он проверяет активный закреплённый trust anchor и обращается только к официальному read-only endpoint; после ожидаемого защищённого 4xx контейнер удаляется. Verify закрепляет четыре запуска immutable image: ровно три stateful запуска используют общие secret mounts, единственный сетевой probe не использует ни одного.

Одноразовая выкладка разрешена только из `vitaliyozolin-dotcom/ArtHello-OS:codex/d057-tochka-stateless-egress-20260903` через PR `#326`, являющийся прямым потомком D-056 `a48eed30b3a0910c603ff7ad2285a42b3785a507`. Завершение требует нового first-attempt Quality/Verify, `ARTHELLO_TOCHKA_TLS_EGRESS=VERIFIED`, изолированного clone-preflight, backup-first cutover, rollback guards и публичного health-check.

## D-056 — Проверять X.509 Точки внутри immutable runtime, а не на production-host

Дата: 2026-09-03  
Статус: принято как fail-closed продолжение порученного D-055; одноразовый release PR `#325` закреплён  
Владелец: Исполнитель ArtHello OS

GitHub-подписанный squash merge D-055 `66ca9490d7e765a1338ad0c4ce8cdc2c163c9d36` прошёл повторные Quality, Proof и Verify на `main`. Production run `33749916676` остановился на проверке checkout с exit code 127 до скачивания image, SSH, Docker, clone-preflight и любых изменений live-контейнера или данных. Причина — новый checkout-step вызвал host-команду `node`, которой намеренно нет на минимальном self-hosted runner.

Коррекция не ослабляет проверку сертификата. Host продолжает сверять точный SHA-256 PEM и Dockerfile-контракт стандартными утилитами. Полная X.509-проверка CA-признака, самоподписи, срока и fingerprint уже выполняется при hosted Docker build; активный trust store повторно проверяется процессом Node внутри запущенного immutable container; реальный TLS-выход к официальному read-only endpoint Точки проверяется тем же контейнером до остановки live. Verify workflow теперь отдельно запрещает host-вызов Node в checkout-contract.

Одноразовая выкладка разрешена только из `vitaliyozolin-dotcom/ArtHello-OS:codex/d056-tochka-runner-preflight-20260903` через PR `#325`, являющийся прямым потомком D-055 `66ca9490d7e765a1338ad0c4ce8cdc2c163c9d36`. Завершение требует нового первого attempt hosted Quality/Verify, production `ARTHELLO_TOCHKA_TLS_EGRESS=VERIFIED`, backup-first cutover, rollback guards и публичного health-check.

## D-055 — Доверять официальной TLS-цепочке API Точки в production

Дата: 2026-09-03  
Статус: принято владельцем по фактической ошибке production; одноразовый release PR `#324` закреплён  
Владелец: Исполнитель ArtHello OS

Мобильный скролл D-054 подтверждён фактически: поле ключа и нижние действия доступны. Следующий реальный запуск дошёл до серверной проверки, но завершился сообщением «Не удалось связаться с Точкой». Официальный API `https://enter.tochka.com/uapi` использует российскую TLS-цепочку, которой нет в стандартном наборе доверия базового Node-образа production.

В runtime добавляется только закреплённый корневой сертификат `Russian Trusted Root CA` с SHA-256 fingerprint `D2:6D:2D:02:31:B7:C3:9F:92:CC:73:85:12:BA:54:10:35:19:E4:40:5D:68:B5:BD:70:3E:97:88:CA:8E:CF:31` и SHA-256 файла `aa800ef345422d6158c6fafe1c06c429dbda21c3df4bb1ccb45a920ec1111399`. Сборка обязана проверить самоподпись, CA-признак, срок и точный fingerprint; отключать проверку TLS запрещено.

До остановки действующего контейнера clone-preflight временно подключается к production-сети и без ключа, Authorization или иных секретов обращается к официальному read-only endpoint Точки. Только TLS-соединение и ожидаемый защищённый HTTP 4xx считаются доказательством доступности; redirect, 2xx, 3xx, 5xx или сетевой/TLS-сбой останавливают release. Импорт остаётся в границах D-053: счета, остатки, выписки и проведённые операции; создание, подписание и отправка платежей запрещены.

Одноразовая выкладка разрешена только из `vitaliyozolin-dotcom/ArtHello-OS:codex/d055-tochka-tls-20260903` через PR `#324`, являющийся прямым потомком текущего `main` `c93a066dae1e3cb1cee83b17de6b2624a0f73baa`. Завершение требует hosted Quality/Verify, доказательства точного сертификата внутри immutable image, production TLS egress preflight, backup-first cutover, rollback guards и публичного health-check.

## D-054 — Вложенное окно подключения Точки владеет мобильной прокруткой

Дата: 2026-09-03  
Статус: принято владельцем по прямой повторной проверке production; одноразовый release PR `#321` закреплён  
Владелец: Исполнитель ArtHello OS

Окно «Настройки» и вложенная форма «Банк Точка» являются двумя независимыми слоями. Вложенная форма ограничивается текущей динамической высотой экрана, сохраняет фиксированные заголовок и нижние действия, а жест вертикальной прокрутки обрабатывает только её центральная часть. Старые глобальные правила `.setup-wizard` не могут переводить эту форму в `height:auto`, `max-height:none` или `overflow:visible`.

Основание — повторный прямой отзыв Виталия и снимки IMG_9985/IMG_9986: нижнее окно «Настройки» прокручивается, а расположенное поверх окно подключения Точки остаётся обрезанным. Исправление изолировано CSS-классами Центра интеграций и защищено тестом, который явно воспроизводит конфликт с обоими прежними глобальными правилами.

Приёмка требует целевого теста, линтера, production-сборки, hosted Quality и Verify на точном SHA, неизменяемого Docker image, clone-preflight, резервной копии перед cutover, автоматического rollback и публичного health-check. Одноразовая выкладка разрешена только из `vitaliyozolin-dotcom/ArtHello-OS:codex/d054-tochka-modal-scroll-20260903` через PR `#321`, являющийся прямым потомком production-коммита `1038a1960aaf62f57a161d8a16c804161a7f6c53`.

## D-053 — Прокручиваемое окно Точки и полный read-only импорт банковского факта

Дата: 2026-09-03  
Статус: принято владельцем в явном поручении продолжать до production-релиза

Окно настройки интеграции использует фиксированные заголовок и нижние действия, а длинная центральная часть прокручивается независимо внутри динамической высоты экрана. На мобильном устройстве поля, официальный документ и кнопки остаются достижимыми; касание внутри формы не закрывает окно.

После проверки ключа и выбранной компании ArtHello OS выполняет документированный read-only поток Точки: получает доступные счета, запрашивает формирование выписок и читает готовые выписки по каждому разрешённому счёту. В систему сохраняются маскированные счета и остатки, реестр выписок и проведённые операции. Проведённые рублёвые операции идемпотентно попадают в «Финансы → Реестр» со статусом «Требует разбора» и не включаются в ОПиУ до ручной классификации. Повторная загрузка не создаёт дублей и не перезаписывает сделанную человеком классификацию.

Транспортный allowlist разрешает только чтение клиентов и счетов, POST создания отчёта-выписки и GET готовой выписки на официальном API Точки. Создание, подписание, отправка или отзыв новых платежей не реализованы и не разрешены. Миграция D1 только добавляет таблицы банковских счетов, выписок и операций; существующие финансовые записи не изменяются. Одноразовый выпуск закреплён за branch `codex/tochka-readonly-sync-20260903`, PR #320 и единственным parent `70f767723ca2de151d80782a6cea236a9f651ed0`; обязательны exact-SHA Quality/Verify, защищённый Environment, clone-preflight, резервная копия до cutover и автоматический rollback.

## D-052 — Вернуть кликабельность окнам интеграций

Дата: 2026-09-03  
Статус: принято владельцем по фактической production-проверке

После D-051 владелец проверил окно Банка Точка на `https://arthello-188-225-38-55.sslip.io` и приложил снимок: любой клик внутри формы закрывал окно. Причина подтверждена в исходном коде: форма настройки ошибочно имела `data-ah-help-root="true"`. Глобальный слой контекстной помощи намеренно задаёт корню помощника `pointer-events: none`; поэтому форма исключалась из hit-testing, а клик попадал в расположенный позади scrim с `onClick={close}`.

Исправление удаляет служебный атрибут только с формы интеграции, не меняет банковские ключи, API, права, данные или правила закрытия диалога. Регрессии Точки, Т‑Банка и общая acceptance-проверка запрещают `data-ah-help-root` на `<form>`. One-release trigger закрепляется за exact branch `codex/fix-integration-modal-hit-testing-20260903`, PR #319 и единственным parent `163ff8a6f337b21b7dbb6560a88324ee57e4478f`; production сохраняет hosted Quality/Verify, защищённый Environment, резервную копию, health-check и автоматический rollback.

## D-051 — Восстановить ArtHello cutover через авторитетный School SSH-контур

Дата: 2026-09-03  
Статус: принято как fail-closed восстановление порученного production release

GitHub-подписанный squash merge D-050 `f14dad5d938de023385b996877a1ec6d1aaca5d6` прошёл Quality, Proof и v52 Verify на `main`. Production run `33713845429` остановился до активации нового ArtHello-контейнера: внешний School health был зелёным, но ArtHello runner выполнил локальный `docker inspect school-1-11`, тогда как канонический School-контейнер `school-1-11` находится на защищённом удалённом Docker-хосте. Workflow подтвердил `ARTHELLO_ROLLBACK=VERIFIED`; текущий ArtHello не останавливался, данные и маршрут не менялись.

Follow-up ограничен decision record и двумя release workflows. Он сохраняет ArtHello concurrency и добавляет repository-wide School concurrency, повторно использует только существующие step-scoped `production-ru` SSH credentials, закреплённый ED25519 fingerprint и удалённый `/var/lock/school-1-11-production.lock`. До чтения секрета проверяются полный ID, running/healthy и labels `school.system=school-1-11`, `school.environment=production`; принимается ровно один `CENTRAL_ACCESS_SECRET` длиной 32–4096 без whitespace. Значение не попадает в argv, env, logs или GitHub outputs, сразу маскируется и атомарно сохраняется только при отсутствии локального файла; существующий файл обязан совпасть.

Совпадение ключа доказывается без изменения данных School: запрос с заведомо неверной подписью для `{}` обязан вернуть exact 401 `{"error":"Неверная подпись синхронизации"}`, а HMAC из защищённого файла — exact 400 `{"error":"Неизвестное действие синхронизации"}`. Проверки выполняются после получения ключа, перед preflight/live-мутацией и после переключения маршрута; любое отклонение завершает cutover fail-closed либо запускает проверяемый rollback.

One-release trigger закреплён за exact branch `codex/recover-school-container-discovery-20260903` и PR #318. Workflow отдельно подтверждает failed run `33713845429`, запрещает повтор SHA D-050, требует новый GitHub-подписанный squash merge с единственным parent D-050, attempt 1 для Quality/Verify/deploy и отсутствие любого предыдущего deploy run нового SHA.

## D-042 — Одноразовый School cutover перепривязать к фактическому main

Дата: 2026-09-02  
Статус: принято владельцем как продолжение ранее порученного product release; изменяет только controls-provenance D-041

PR №300 слит GitHub-подписанным squash commit `d4ba1b23f77f72668087ef4aedc91c3c3155dfe1`, однако перед merge `main` независимо продвинулся от принятого base `c3182d598fd7a7a00a5388003754293219e41096` до `7a511765586d0641d59e9f5fae51cb128f7e19cc`. Поэтому фактический merge имеет parent `7a511765586d0641d59e9f5fae51cb128f7e19cc` и tree `79e7cf87d5f3519be2ca66008d78f1539919f737`, а исходный D-041 gate намеренно отклоняет его до checkout, SSH и Docker. Production не запускался и не изменялся.

Для единственного запуска D-041 controls-provenance перепривязывается только через будущий GitHub-подписанный squash merge PR №305 из exact branch `codex/school-curriculum-controls-repin-20260902` с base `d4ba1b23f77f72668087ef4aedc91c3c3155dfe1`, одним parent и tree, равным проверенному финальному head PR №305. Перед merge фактический `main` обязан всё ещё точно равняться этому base; при любом новом продвижении merge запрещён до новой явной перепривязки и повторных CI/review. Follow-up изменяет только `.github/workflows/deploy-school-curriculum-main.yml` и этот decision record: release SHA/tree, validation evidence, payload hashes, import, rollback/re-gate и продуктовые файлы остаются без изменений. После squash merge PR №305 допустим только dispatch этого exact merge commit; любой последующий commit `main` снова завершается fail closed. Закрытый без merge draft PR №304 не является допустимым controls commit и не несёт отдельного изменения.

## D-041 — School 2026/2027 публиковать ручным проверяемым cutover

Дата: 2026-09-02  
Статус: принято владельцем для product release; запуск только через D-040 gate

Владелец поручил внести в School утверждённый набор 2026/2027 и вывести принятый релиз в production. Источником истины для расписания является приложенный файл: в нём подтверждены 189 еженедельных уроков только для классов 1–6 (41/28/29/29/31/31); отсутствие строк 7–11 не восполняется догадкой и не даёт права удалять существующие данные других классов. Для математики 2 класса принято КТП на 170 тем и 170 часов при пяти уроках в неделю. После исключения четырёх периодов каникул календарь даёт 171 слот: занятия распределяются с 01.09.2026 по 28.05.2027, а 31.05.2027 остаётся резервом.

Импорт расписания, каникул и КТП идемпотентен и до открытия записи обязан пройти точную read-only сверку 189 строк, 4 периодов, 170 тем, 170 сессий, provenance, SQLite integrity и foreign keys. Замена расписания ограничена классами источника 1–6; строки уроков, связанные с ними сессии и исключения других классов сравниваются до/после импорта и обязаны остаться неизменными. Ручное сопоставление ученика в утверждённом исключении также сохраняется. Архивные аккаунты преподавателей не назначаются. Учитель сохраняет право вручную менять фактическую тему и домашнее задание; кнопка «урок проведён» и автоматический сдвиг будущих тем не вводятся. Методист проверяет и возвращает план с комментарием, завуч утверждает календарь, расписание, назначения и активацию программы.

Production cutover разрешён только из `main` отдельным `workflow_dispatch` с точным typed confirmation `DEPLOY-SCHOOL-2026-2027`, от владельца, через защищённый Environment `production-ru` с required reviewer, `prevent_self_review=true` и `can_admins_bypass=false`. Workflow обязан до checkout, SSH и Docker проверить confirmation и фактические protection rules. Единственным допустимым controls commit является будущий подписанный squash merge PR №300: GitHub API должен подтвердить exact base `c3182d598fd7a7a00a5388003754293219e41096`, утверждённую source branch, одного parent и равенство tree проверенному PR head; любой более поздний commit в `main` fail closed не может повторно запустить этот release.

Допуск source подтверждается до checkout через GitHub API: финальный hotfix PR №303 должен быть слит exact head `9da48a1d6eb12a257442d72808e770c739b7fcc4` в release `f41dfb5de4eeeed4ed26d94009a18388728f42b7` с tree `2ecda1a0a872caab7aadbc303c152c1207d3030a`; validation run `33592942510`, workflow `346579718`, job `100130562121` должны иметь `conclusion=success` на том же head и tree, а squash commit — быть подписан GitHub. Validation включает полный импорт 189 строк с sentinel 7 класса, доказывает сохранность данных вне источника и проверяет overlay исправленного schedule importer поверх runtime archive.

Offline build не доверяет изменяемому локальному тегу. Принятый validation log фиксирует `node:24-bookworm-slim@sha256:ba849c60be29959425b8734d57b8b4b7d56f98edd9504c9af091d5281095a71e`. Cutover проверяет точный исходный `Dockerfile.offline` (`0a71ac38…`), создаёт проверяемую controls-overlay копию (`7b1bdbe5…`) с этим digest, при необходимости загружает только exact digest и строит candidate с `--network none --pull=false --no-cache`. До доступа к данным сверяются platform, отсутствие ONBUILD, Node 24, base image ID, префикс RootFS-слоёв и labels образа; те же base labels обязаны быть у production-контейнера.

До открытия записи выполняются offline build, clone-preflight, остановка старого контейнера, maintenance gate, финальный проверенный SQLite snapshot, live import и сверка защищённого scope. До первой live-мутации создаётся отдельный rollback volume, labels связывают его с exact release, run и attempt, а синхронизированный marker атомарно фиксирует `PRECOMMIT_STARTED`. Любая ошибка до commit boundary восстанавливает snapshot и прежний контейнер; проверенное восстановление данных и приложения при сохранённом `restart=no` переводит marker в `PRECOMMIT_RECOVERED`, после чего прежняя restart policy восстанавливается и проверяется точно. Основной cutover проходит состояния `PRECOMMIT_STARTED → ROLLBACK_READY → ROLLBACK_DISABLED → RELEASE_VERIFIED → RELEASE_ACTIVE`. Файл `commit-state` в `/tmp` используется только как быстрый сигнал текущему workflow и не является источником истины после перезагрузки. После live import cutover не снимает maintenance gate: он повторно проверяет его внутри контейнера и с публичной стороны, затем синхронизирует `RELEASE_VERIFIED` и завершается с `restart=no`; gate остаётся включённым на всём протяжении независимой runner-side проверки разрешённых GET-поверхностей и блокировки auth/API. Точный preflight кандидата до live-мутации уже проверяет SSO и family login. Только отдельный идемпотентный activation-step под тем же production lock проверяет удерживаемый gate, восстанавливает и точно проверяет прежнюю restart policy, синхронизирует `RELEASE_ACTIVE`, затем снимает gate и подтверждает публичное здоровье. Повтор activation после потери SSH подтверждает или завершает то же durable-состояние, не создавая вторую активацию. Поэтому потеря runner/control plane, длительная недоступность Docker или перезагрузка до завершения authorization не открывает неподтверждённый кандидат для записи.

При любой ошибке deploy, публичной проверки или activation отдельный `always()` re-gate сначала оставляет exact cancel fence в общем production lock, ждёт тот же `flock`, проверяет готовность Docker и только затем читает durable state. Все предшествующие шаги имеют явные step-timeouts: их суммарный максимум до re-gate равен 170 минутам, самому re-gate зарезервировано 170 минут, завершающим шагам — 8 минут; общий worst-case 348 минут оставляет 12 минут запаса внутри 360-минутного job-limit. Deploy-step допускает полный remote worker budget `3000s + 600s` и считает runner-side poll deadline от начала launch равным 3900 секундам, оставляя отдельное окно для launch skew и записи terminal status; 105-минутный step cap также резервирует bounded EXIT reconciliation. Все Docker-вызовы аварийного пути ограничены собственным timeout, acquisition immutable ID имеет bounded retries и exact-name fallback, а внешний SSH budget больше полного внутреннего worst-case: зависший диагностический или evidence-вызов не может бесконечно отложить containment. Только успешная пустая exact-name enumeration считается доказанным отсутствием контейнера. Отсутствие rollback volume при отсутствии commit доказывает, что live-мутация ещё не начиналась, потому что volume и `PRECOMMIT_STARTED` синхронизируются раньше неё. Только exact `PRECOMMIT_RECOVERED` с совпадающими labels, отсутствующим commit, работающим прежним production, точной сохранённой restart policy и снятым gate допускает no-op. `PRECOMMIT_STARTED`, `ROLLBACK_READY`, `ROLLBACK_DISABLED`, `RELEASE_VERIFIED`, `RELEASE_ACTIVE`, известный commit или любое противоречивое/нечитаемое доказательство сначала устанавливают и независимо проверяют maintenance gate и `restart=no`, в том числе для остановленного контейнера. ID, name, image и mounts берутся одним inspect и после этого все изменения адресуются только зафиксированному immutable ID; successful exact-name enumeration сохраняет ID даже при нечитаемых ancillary-полях, поэтому они не блокируют containment. Повторный сбой внутренней проверки gate снова запускает обычный containment; сбой публичной семантики gate/maintenance принудительно оставляет `restart=no`, останавливает контейнер и одним финальным inspect доказывает `Running=false` вместе с `restart=no`, а при невозможности доказать оба условия переходит к удалению. Если один из инвариантов gate/restart не доказан, re-gate останавливает контейнер; если не доказаны оба либо остановку нельзя подтвердить, он удаляет по заранее зафиксированному immutable ID только exact production container без `-v`. Успешная bounded Docker enumeration обязана доказать отсутствие ID и production-name, а известные image, data и backup volumes повторно проверяются. Лишь после доказанного containment pre-captured полный inspect best-effort сохраняется как root-only manifest в отдельном labelled emergency volume, а sanitized marker различает сохранённый и недоступный manifest. Невозможность записать manifest не отменяет containment: владелец явно принимает outage и ручное восстановление runtime-конфигурации вместо риска reboot-autostart неподтверждённого writable container; named volumes, известный image и rollback evidence при этом не удаляются. В остальных аварийных состояниях контейнер и rollback volume сохраняются как операционное доказательство и средство ручного восстановления.

## D-040 — Недоверенная автоматизация не получает production capability

Статус: принято владельцем для кандидата Фазы 0A; требует Reviewer/Coordinator gate

Код из `pull_request` или PR-head checkout не исполняется на self-hosted runner, job с production Environment, SSH-доступом к production или ином production capability. Доступ к Docker socket считается root-эквивалентным независимо от Unix-пользователя. Production-destructive workflow запускается только вручную через `workflow_dispatch`, требует точное typed confirmation до checkout/Docker и protected Environment с required reviewer. Replit post-merge не изменяет схему БД. Постоянный YAML-aware gate анализирует все workflow и fail closed блокирует возвращение опасных trigger/job/checkout сочетаний; его machine-readable отчёт входит в proof evidence.

## D-036 — Raw lineage и snapshot-state не восстанавливать догадкой

Статус: принято в source candidate; независимый финальный gate ожидается

`alpha_raw_records` является append-only. Повторное получение того же payload создаёт новое observation batch/scope/page, а изменённый payload — новую raw-версию. Каждая материализованная AlfaCRM-строка обязана ссылаться на существующие raw и batch; migration `0014` завершается fail closed при legacy rows без доказуемого provenance вместо автоматического backfill.

Current/stale reconciliation выполняется только после полной успешной пагинации минимального scope. Partial, repeated page, max guard и network error не tombstone-ят предыдущий snapshot. Лиды и ученики сохраняют историю, но после успешного student snapshot одна запись не может одновременно оставаться current lead. Pending family candidates без текущего evidence становятся stale; решения человека не переписываются.

## D-035 — Incremental AlfaCRM начинается с проверяемого change discovery

Статус: принято в исходниках; live data не проверены

Полный read-only snapshot остаётся обязательной базой. Лиды загружаются отдельно от учеников; абонементы клиентов, платёжные справочники и журнал изменений имеют raw + normalized слои с provenance и идемпотентными business keys. Incremental-режим читает `log/index` от watermark с перекрытием 1–7 дней и честно называется discovery: до реализации безопасной rematerialization он не доказывает обновление всех domain-таблиц. Запись в AlfaCRM запрещена.

## D-034 — Sites не является банковским OAuth backend

Статус: принято, текущий consent заблокирован

Корень owner-only Sites нельзя регистрировать как Redirect URI «Точки»: контрольная оболочка не хранит банковские secrets и не должна получать authorization code. Допустим только точный HTTPS URL защищённого API вида `/api/banking/oauth/callback`, совпадающий с runtime config. `.chatgpt.site`, HTTP, корневой путь и произвольные callback-пути завершаются fail closed.

## D-033 — Новый клиент «Точки» использовать только в read-only контуре

Статус: production client auth подтверждён, доступ к данным заблокирован до OAuth

Владелец разрешил временно использовать новый production client. Service token проверяет приложение, но не даёт доступа к счетам. До корректного callback, пользовательского consent, hybrid token, protected backend Secrets и backup/rollback разрешены только безопасные auth/status probes. Платёжные scopes, создание платежей и иные денежные действия запрещены.

## D-032 — Реальный payroll импортировать как evidence, а не как утверждённые правила

Статус: принято и выполнено в изолированной sandbox-БД

Значения и formula snapshots реальной таблицы сохраняются с происхождением строки. Вычисленные выплаты и начисления можно сверять, но формулы, ставки, KPI и условия не становятся payroll rules без явного утверждения. Несопоставленные identities не объединяются автоматически; персональные строки, суммы и реквизиты не публикуются в Sites.

## D-031 — Полный AlfaCRM read разрешён только в отдельную sandbox-БД

Статус: разрешено владельцем; свежий импорт заблокирован сетью

Текущие временные credentials допускаются для read-only загрузки всех требуемых CRM-сущностей в отдельную БД вне source checkout. Запись в AlfaCRM, production sync и передача PII в Sites запрещены. Семьи не подтверждаются автоматически по телефону: importer создаёт только кандидатов для ручного решения. После end-to-end проверки credentials перевыпускаются и помещаются только в protected backend environment.

## D-030 — Финансовые инварианты проверять без бизнес-допущений

Статус: принято как synthetic guardrail, не как финансовая готовность

Регрессионные тесты используют только integer minor units и вымышленные test IDs: проверяют `opening + inflows - outflows = closing`, исключение внутренних переводов из консолидированного ДДС, раздельные cashflow/accrual/P&L периоды, единственную активную версию payroll rule, точные reversals и deterministic rounding. Они не содержат реальных сумм, ставок, KPI или финансовых статей и не открывают financial truth gate.

## D-029 — Legacy sync и public 5xx fail closed

Статус: принято в исходниках, runtime release не выполнен

Public 5xx boundary никогда не возвращает exception message, upstream body, probe output, идентификаторы или PII. Историческая `/sync` поверхность централизованно возвращает `LEGACY_SYNC_DISABLED` и остаётся недоступной, пока не заменена scoped source jobs. Это безопасное отключение, а не утверждение, что legacy code полностью удалён.

## D-028 — PR body является индексом, CI artifact — доказательством

Статус: принято для финального цикла 3

Quality workflow checkout-ит точный `pull_request.head.sha`, сверяет фактический `HEAD` и публикует immutable artifact с `head_sha`, `tree_sha`, source archive SHA-256 и deterministic Sites artifact SHA-256. PR body обновляется после CI/Sites и только индексирует доказательства. Он изменяем и сам по себе не подтверждает равенство local/remote tree.

## D-027 — Tree является ключом идентичности local/remote package

Статус: принято для приватной Git Data history

Local и remote commit SHA могут различаться, потому что remote branch создаётся через Git Data API поверх отдельной истории. Пакет считается содержательно одинаковым только при точном равенстве Git tree. Для каждого checkpoint дополнительно фиксируются private PR head, terminal CI run, Sites version/deployment и owner-only access. D-028 уточняет механизм: PR body — индекс, а равенство tree доказывается immutable CI artifact.

Равенство tree и отчёт Создателя не заменяют независимую проверку Ревизора.

## D-021 — Временная проверка текущими credentials

Статус: разрешено владельцем только для A.3 read-only probe

Текущие credentials можно использовать до ротации только в памяти одноразового процесса и только для авторизации, metadata/count probes и чтения после отдельного OAuth consent. Запрещены сохранение в source/Sites/CI logs, запись business data, sync в production БД и любые платежные действия. Перед production credentials всё равно перевыпускаются.

Это ограниченное исключение уточняет D-010 и D-026: источник можно проверить без публикации записей, но production, детальная выгрузка, постоянная синхронизация и финансовые расчёты не открываются.

## D-022 — Service token «Точки» не считать доступом к счетам

HTTP 200 на `client_credentials` подтверждает приложение, но не банковские данные. Доступ к счетам и выпискам признаётся только после Authorization Code flow, hybrid token и read-only smoke. HTTP 403 с service token является ожидаемым безопасным результатом.

## D-023 — Шифровать банковскую connector config

Статус: принято в исходниках

Secret-bearing config хранится только как AES-256-GCM envelope. AAD связывает ciphertext с connector ID и key ID. Активный key выбирается защищёнными env; отсутствие ключа, подмена ciphertext или legacy plaintext secret завершаются fail closed. Миграция разрешена только после подтверждённого backup ID и явного confirmation token.

## D-024 — PostgreSQL 16 sandbox создаётся CI-сервисом

Статус: выполнено, GitHub Actions run #6 PASS

Quality workflow поднимает одноразовый PostgreSQL 16 service и выполняет реальные migrations, HTTP/session/CSRF/audit checks, webhook rollback/retry, schema drift, rollback и fail-closed startup. Run #6 (`30161527465`) прошёл `test:full`, `test:postgres` и `build:full`. Это evidence только для синтетической одноразовой БД; оно не заменяет восстановленную sandbox-копию и не разрешает production migration.

## D-025 — Все AlfaCRM запросы проходят одну очередь

Статус: принято в исходниках, unit PASS

Очередь сериализует запросы и выдерживает 260 ms между стартами. Ошибка задачи не ломает очередь; одновременная аутентификация объединяется.

## D-026 — Раскрытые в переписке ключи скомпрометированы

Ключи со скриншотов не переносятся в код, Sites, CI или постоянный env. D-021 разрешает их временное использование только в памяти одноразового read-only probe. До production и постоянной синхронизации владелец перевыпускает AlfaCRM API/X-APP keys и Tochka client secret и сохраняет новые значения только в protected backend Secrets.

## D-001 — Продолжать существующий монорепозиторий

Дата: 2026-07-23
Статус: принято

Архив признан существующей кодовой базой ArtHello OS. Новый несвязанный продукт не создаётся. Sites control surface добавлена как отдельный безопасный runtime-адаптер внутри того же репозитория.

## D-002 — Исторические числа не являются текущей истиной

Дата: 2026-07-23  
Статус: принято

Значения из `replit.md` и прежних отчётов сохраняются только как исторические свидетельства. Они не показываются как актуальные метрики и должны быть перепроверены через авторизованный read-only доступ.

## D-003 — Sites не подключается к production на первом checkpoint

Дата: 2026-07-23  
Статус: принято

Первый owner-only checkpoint содержит только обезличенные статусы аудита. В нём нет секретов, персональных данных, зарплаты и банковских операций.

## D-004 — Не имитировать цифры

Дата: 2026-07-23  
Статус: принято

Если факт нельзя подтвердить, интерфейс показывает `не подтверждено`, `неизвестно` или `нужен доступ`. Случайные метрики запрещены.

## D-005 — Закрыть исходники по принципу deny by default

Дата: 2026-07-23  
Статус: принято с условием

Общий auth-gate защищает API. Публичными остаются только health, login/session и browser OAuth callback Точки, защищённый state. Входящие website, bank и Evotor callbacks временно закрыты до определения проверяемой аутентификации. Перед выпуском в Replit совместимость callback-контрактов должна быть протестирована.

## D-006 — Не хранить банковские секреты в Sites

Дата: 2026-07-23  
Статус: принято

Sites не получает банковские или AlfaCRM секреты. A.2 реализует encrypted vault в исходниках; включать или обновлять банковские подключения нельзя до backup, guarded migration legacy rows, ротации раскрытых credentials и проверки на восстановленной репрезентативной sandbox-копии.

## D-007 — Сохранить существующий визуальный язык

Дата: 2026-07-23  
Статус: принято

Контрольная оболочка использует обнаруженный стиль ArtHello: светлый фон, белые карточки, компактная боковая навигация и фиолетовый акцент. Отдельный дизайн-конкурс не проводился.

## D-008 — Принудительно пересобирать declaration-файлы

Дата: 2026-07-23  
Статус: принято

Из-за сохранённых `tsconfig.tsbuildinfo` и отсутствующих `dist` обычный `tsc --build` давал ложные ошибки `TS6305`. `typecheck:libs` использует `tsc --build --force`.

## D-009 — Не запускать production migrations в Фазе A

Дата: 2026-07-23  
Статус: принято

Migration runner и схема изучаются только статически. Любая production-миграция требует snapshot/backup, проверенного rollback, отдельного разрешения и ворот Reviewer/Coordinator.

## D-010 — Не открывать live gate до закрытия HIGH

Дата: 2026-07-23
Статус: принято по решению Координатора

Owner-only sanitized checkpoint и production readiness — разные статусы. Одноразовый PostgreSQL 16 CI gate пройден, но пока открыты migration legacy bank config, ротация раскрытых credentials, небезопасные legacy errors/probes, scoped handlers, callback authentication и проверка на восстановленной репрезентативной sandbox-копии, запрещены Replit release, production, постоянная синхронизация и чтение детальных AlfaCRM/банк/БД records, polling и реальные персональные или финансовые данные. D-021 разрешает только ephemeral auth/metadata/count probe без сохранения записей. Owner UI обязан показывать эти блокеры полностью; отсутствие детального live-доступа не считается доказательством безопасности.

## D-011 — Browser auth только через server-side session

Дата: 2026-07-24
Статус: PostgreSQL 16 CI PASS; production release gate не пройден

Browser frontend не хранит bearer token. Session token передаётся в HttpOnly Secure SameSite cookie, а в PostgreSQL хранится только его SHA-256 hash. Изменяющие запросы требуют double-submit CSRF. Login lockout хранится в БД, чтобы несколько API-процессов не обходили лимит.

## D-012 — RBAC deny by default

Дата: 2026-07-24
Статус: заменено более строгим D-014

Первоначальная policy давала viewer чтение, а accountant — явный финансовый write allowlist. Ревизия выявила отсутствие обязательных branch/legal-entity predicates, поэтому эта policy больше не считается достаточной.

## D-013 — Security migration не запускается автоматически

Дата: 2026-07-24
Статус: принято

Таблицы `auth_sessions` и `auth_login_attempts` оформлены Drizzle migration `0009`. Scope/audit оформлены migration `0010`. Они намеренно не добавлены в legacy startup runner и не применялись к production. До sandbox backup → restore → apply → HTTP/PostgreSQL smoke → rollback выпуск API запрещён.

## D-014 — Non-owner business access закрыт до scoped handlers

Дата: 2026-07-24
Статус: принято в исходниках, runtime gate не пройден

Session хранит явные списки филиалов и юридических лиц из protected environment. Наличие metadata само по себе не доказывает фильтрацию результата. Поэтому все business routes для accountant/viewer fail closed, пока конкретный handler не применяет оба predicates и не проходит cross-scope negative tests. Owner сохраняет полный authenticated access.

## D-015 — Sensitive access audit и startup должны быть fail closed

Дата: 2026-07-24
Статус: startup и allowed access audit подтверждены в PG16 CI; deny/outage audit smoke открыт

Migration `0010` добавляет канонизированный audit разрешённых и запрещённых sensitive routes. Если разрешённый запрос нельзя записать в audit store, API возвращает `503`. Ошибка startup migration теперь прерывает запуск до listener и banking polling. Обе защиты должны быть доказаны на восстановленной sandbox-копии.

## D-016 — Внешние callbacks не публиковать без provider-auth

Дата: 2026-07-24
Статус: принято

Website, bank и Evotor POST callbacks остаются за user auth/CSRF gate и поэтому неработоспособны для внешних провайдеров. Website handler получил обязательный `Idempotency-Key`, но этого недостаточно для public exposure. Нужны provider-specific signature/token verification, replay window, durable idempotency и negative tests; до этого callback endpoints не добавляются в public allowlist.

## D-017 — Website lead записывать одной транзакцией

Дата: 2026-07-24
Статус: принято в исходниках, PostgreSQL 16 CI PASS

Одинаковый `Idempotency-Key` сериализуется PostgreSQL advisory transaction lock. Raw event, lead event, processed flag и source status записываются одной транзакцией. Key связан с hash canonical payload: другой payload получает `409`. Старый raw-only partial write восстанавливается повтором без второй raw-записи. PostgreSQL 16 CI подтвердил rollback/retry/recovery; до public callback всё равно обязательны provider-auth, replay protection и negative tests.

## D-018 — Security schema проверять до listener

Дата: 2026-07-24
Статус: принято в исходниках, PostgreSQL 16 CI PASS

Успех legacy startup runner недостаточен. API до `listen()` обязан подтвердить catalog columns и indexes auth/audit schema, а также timestamp и SHA-256 hash migrations `0009–0010` в `drizzle.__drizzle_migrations`. Любое расхождение останавливает процесс до polling.

## D-019 — Диагностика только по allowlist, audit только по route templates

Дата: 2026-07-24
Статус: принято в исходниках

Banking health response строится только из явно разрешённых полей; неизвестное новое поле не может автоматически попасть клиенту. Audit path выбирается из registry шаблонов, а любой незарегистрированный путь сворачивается в безопасный placeholder. Значения slug, email, phone, UUID, numeric и percent-encoded identifier в audit не сохраняются.

## D-020 — Rollback 0010 не восстанавливает отозванные sessions

Дата: 2026-07-24
Статус: принято и документировано

Migration `0010` отзывает существующие non-owner sessions. Down migration удаляет scope/audit schema, но не может восстановить отозванные токены. После rollback accountant/viewer должны войти заново; это обязательный шаг runbook, а не дефект, который маскируется SQL.

## D-043 — Главная, доступы, единые карточки и банковские подключения следуют фактической ответственности пользователя

Дата: 2026-09-02  
Статус: принято владельцем в явном поручении на production-релиз; уточняет D-006, D-014, D-023 и D-033

Главная ArtHello OS является рабочим дашбордом, а не фиксированной витриной. Её состав по умолчанию определяется фактической ролью; пользователь может выбрать виджеты, изменить их порядок и размер и вернуть ролевую раскладку. Раскладка сохраняется для сочетания пользователя и роли. Владелец получает полный операционный обзор и право на настройку, но отсутствие данных не заменяется выдуманными показателями. Запрещена общая CSS-сетка, которая превращает все блоки Главной в две колонки и растягивает пустые карточки на высоту экрана.

Меню, избранное, поиск разделов и прямые переходы используют одну карту эффективных разрешений. Недоступный роли раздел не показывается; прямой URL к нему завершается fail closed и возвращает пользователя на доступную Главную. Владелец является высшей системной и конфигурационной ролью: он управляет ролями, интеграциями и их разрешениями. Медицинские данные остаются отдельным чувствительным контуром и не открываются одним фактом владения; владелец назначает такой допуск отдельной роли медработника.

У вручную созданной единой карточки нет внешнего проверяющего и статуса «качество». Такая карточка честно помечается «Создано вручную». Очередь «Требует сверки» появляется только при доказанном конфликте, дубле или импортированном источнике, которому действительно требуется решение человека; интерфейс явно сообщает причину и кто должен принять решение. История происхождения и проверки сохраняется.

JWT/API-секрет Точки вводит сам владелец в замаскированном поле ArtHello OS при настройке подключения. Сначала кандидат проверяется безопасной read-only цепочкой банка, и только после успешной проверки несекретная привязка и зашифрованный секрет сохраняются атомарно. Значение не возвращается клиенту, не попадает в логи и хранится как AES-256-GCM envelope в локальной production-БД; отдельный runtime master key не записывается в БД или репозиторий. Перепривязать, ротировать, применить или отозвать банковский credential может только канонический владелец. Подключение относится к подтверждённому банковскому customerCode существующего юридического лица, а не к филиалу или одному счёту: один ключ может обслуживать все счета, разрешённые этим ключом у провайдера. Счета сопоставляются с филиалами отдельно; один счёт может принимать поступления нескольких направлений, поэтому распределение допускается на уровне операции, а неоднозначные операции остаются в очереди классификации. Если другое юрлицо не входит в разрешения ключа, для него требуется отдельное подключение — не отдельный ключ на каждый локальный филиал или счёт.

Декоративные знаки вопроса из формы подключения удаляются. Критичные пояснения остаются обычным доступным текстом рядом с полем. Приёмка требует поведенческих тестов ролевого меню, прямого URL, персональной раскладки, происхождения карточек и owner-only обработки секрета, успешной сборки и браузерной проверки точного production-кандидата под владельцем и ограниченной ролью. На живом адресе после cutover отдельно проверяются публичная поверхность, аутентификационная граница и неизменность School; реальные пароли и JWT в CI не передаются. Проверка одних строк или CSS-селекторов не считается доказательством корректной раскладки.

Одноразово для релиза PR `#310` из `vitaliyozolin-dotcom/ArtHello-OS:codex/owner-dashboard-tochka-20260902` D-040 уточняется только в части запуска: явное поручение владельца в текущей задаче является авторизацией выпуска, а вместо недоступного `workflow_dispatch` используется `workflow_run` после успешной hosted-проверки `Verify ArtHello v52 release` на точном подписанном squash-коммите, который в момент каждого необратимого шага остаётся текущим `main`. Environment `production-ru` остаётся обязательной платформенной границей, но наличие отдельно настроенного required reviewer не заявляется без проверяемого доказательства. Ограничения конкретного PR, владельца, репозитория, ветки, actor и первого attempt исходной Verify-проверки, точные Quality и Verify gates, изоляция self-hosted runner, backup-first cutover и автоматический rollback остаются обязательными. Повтор успешной выкладки того же SHA и повторный attempt production-run запрещены. Это исключение не распространяется на следующие релизы.

## D-044 — Fail-closed recovery после отказа импорта проверенного образа

Дата: 2026-09-02  
Статус: принято владельцем в рамках того же явного поручения применить изменения и выпустить их в production; одноразовый recovery PR `#311` закреплён

Первый production-run D-043 `33679820312` остановился до clone preflight, backup, миграции и cutover: GitHub artifact был успешно доставлен, его внешний digest и внутренний evidence checksum совпали, а production-контейнер и volume не создавались. Повторный attempt запрещён самим D-043, поэтому восстановление выполняется новым подписанным squash-коммитом через отдельный PR из ветки `codex/production-loader-recovery-20260902`.

Recovery не меняет принятые продуктовые решения и данные приложения. Он заменяет непрозрачный pipe `gzip | docker load >/dev/null` на штатный `docker image load --input`, сохраняет вывод Docker, добавляет явные маркеры каждого provenance/image шага и до импорта публикует только несекретную диагностику версии Docker и свободного места. Автоматическая широкая очистка образов, контейнеров, build cache или volumes запрещена. Любая ошибка снова завершает процесс до работы с production-данными.

Одноразовое исключение закреплено только за PR `#311` из `vitaliyozolin-dotcom/ArtHello-OS:codex/production-loader-recovery-20260902` и одновременно проверяется environment workflow и hosted policy-gate. Точный репозиторий, recovery-ветка, PR, actor/merger, первый attempt Verify, успешные Quality/Verify, текущий подписанный main SHA, protected Environment `production-ru`, clone preflight, backup-first cutover и автоматический rollback остаются обязательными. Исключение прекращается после первого успешного deployment или первой неустранимой ошибки после начала cutover.

## D-045 — Между Docker image stores сверяется переносимый runtime fingerprint

Дата: 2026-09-02  
Статус: принято владельцем в рамках явного поручения завершить production-релиз; одноразовый recovery PR `#312` закреплён

Recovery-run D-044 `33683416460` подтвердил digest и evidence артефакта, свободное место, успешный `docker image load` и точный загруженный tag, затем остановился до clone preflight и cutover только на равенстве `.Id`. Production Docker 29.1.3 с containerd image store и hosted builder могут представлять идентичный сохранённый образ разными видами ID (config, manifest или index digest), поэтому межхостовое равенство `.Id` не является переносимым доказательством.

Hosted evidence теперь дополнительно содержит SHA-256 канонического runtime fingerprint: платформа, дата образа, значимые поля запуска из Config и полный упорядоченный список RootFS layer digests. После импорта production вычисляет тот же fingerprint через локальный `docker image inspect`; cutover разрешён только при точном совпадении fingerprint, revision/source-tree labels, непривилегированного User и уже проверенного SHA-256 архива. Hosted и production Image ID сохраняются в журнале только как диагностические представления. Ослаблять archive/evidence SHA, точный tag, PR/actor/main SHA, Quality/Verify, protected Environment, clone preflight, backup или rollback запрещено.

Одноразовое исключение закреплено только за PR `#312` из `vitaliyozolin-dotcom/ArtHello-OS:codex/portable-image-fingerprint-recovery-20260902` и жёстко проверяется обоими workflow. Повтор старых run запрещён. Исключение прекращается после первого успешного deployment или первой неустранимой ошибки после начала cutover.

## D-046 — Активная D1 определяется binding identity, а не количеством исторических SQLite

Дата: 2026-09-02  
Статус: принято владельцем в рамках явного поручения завершить production-релиз; одноразовый recovery PR `#313` закреплён

Production-run D-045 `33685272835` успешно подтвердил точный архив, evidence, переносимый runtime fingerprint и загруженный image, затем остановился на первом read-only inventory clone-снимка. В production volume находятся восемь SQLite-файлов с историческими копиями полной схемы; прежний инвентаризатор ошибочно считал каждый файл с таблицами `app_users` и `system_runtime_state` одновременно активной application database. До запуска нового приложения, изменения live-данных, создания rollback backup и изменения Caddy процесс не дошёл; старый контейнер остался активен, а автоматический rollback подтвердил его health.

Активная база теперь выбирается только по runtime-контракту `ARTHELLO_D1_PATH=/data/d1`, binding `DB -> arthello-production`, unique key `miniflare-D1DatabaseObject` и детерминированному HMAC-SHA-256 object ID алгоритма зафиксированного Miniflare. Выбор по времени изменения, размеру, числу строк, наличию schema marker или «единственности» запрещён. Инвентаризатор открывает канонический файл, блокирует symlink/path escape, сохраняет integrity, foreign-key и encrypted-credential digest проверки; число совместимых исторических копий допускается только как агрегатная несекретная диагностика.

До снимка live volume и после запуска clone и production-кандидата workflow через read-only `/proc/*/fd` обязан доказать, что процесс держит канонический application database и не держит иной non-metadata D1 SQLite. Тесты воспроизводят каноническую базу плюс семь полно-схемных decoy-копий и отдельно доказывают fail-closed поведение при отсутствующем, повреждённом или symlink target. Все image provenance, exact PR/actor/main SHA, Quality/Verify, protected Environment, backup-first cutover и rollback требования D-043–D-045 сохраняются.

Одноразовое исключение закреплено только за PR `#313` из `vitaliyozolin-dotcom/ArtHello-OS:codex/canonical-d1-inventory-recovery-20260902` и жёстко проверяется обоими workflow. Повтор старых run запрещён; исключение прекращается после первого успешного deployment или первой неустранимой ошибки после начала cutover.

## D-047 — Procfs D1-дескрипторы проверяет тот же непривилегированный runtime user

Дата: 2026-09-02  
Статус: принято владельцем в рамках явного поручения завершить production-релиз; одноразовый recovery PR `#314` закреплён

Production-run D-046 `33687247475` подтвердил exact main, Quality/Verify, архив, evidence, Docker import и переносимый runtime fingerprint, затем остановился на первом metadata-only FD gate до pause, snapshot, копирования, запуска clone, остановки live, backup или изменения Caddy. Rollback снова подтвердил здоровье неизменённого старого production. Канонический D1 path и inventory D-046 остаются правильными.

Причина — FD scanner запускался через `docker exec --user 0:0`, тогда как PID 1 и workerd работают под `node`. Production-контейнер намеренно не получает `CAP_SYS_PTRACE`; procfs ptrace access может запретить container-root читать ссылки `/proc/<pid>/fd` процесса другого UID. Это дало ложный отказ до появления разрешающего маркера.

Scanner теперь выполняется стандартным пользователем контейнера, до чтения проверяет, что его UID непривилегированный и совпадает с UID PID 1, затем применяет неизменный allowlist канонического D1/WAL/SHM и запрещает иной открытый non-metadata D1. При отказе в журнал выводятся только роль этапа, безопасная причина и агрегатные количества; пути, строки БД, credential и идентификаторы пользователей не выводятся. Hosted gate отдельно запрещает возврат `--user 0:0` в этой функции и требует проверки same-UID, non-root и rejection diagnostics.

Одноразовое исключение закреплено только за PR `#314` из `vitaliyozolin-dotcom/ArtHello-OS:codex/same-user-d1-fd-recovery-20260902`. Все требования D-043–D-046 к exact PR/actor/main SHA, первому attempt, Quality/Verify, protected Environment, provenance образа, canonical inventory, backup-first cutover и rollback сохраняются; повтор старых run запрещён.

## D-048 — Главная без автоматических inline-help маркеров

Дата: 2026-09-03  
Статус: принято владельцем в явном поручении немедленно исправить Главную и выпустить изменение в production; одноразовый release PR `#315` закреплён

На Главной ArtHello OS во всех состояниях, включая режим настройки экрана, мелкие автоматические значки `?` не показываются. Глобальная кнопка «Помощь» в правом нижнем углу и ручной пошаговый тур по блокам Главной сохраняются. Подавление задаётся явной зоной `data-ah-inline-help="off"` и не распространяется на формы других разделов.

Причина — общий контекстный помощник сканировал каждый чекбокс и список «Размер» редактора Главной и создавал возле них отдельные плавающие маркеры. На плотной сетке это превращало помощь в визуальный шум. Исправление не меняет данные, роли, разрешения, состав и сохранение раскладок, банковские подключения или School.

Приёмка требует отсутствия малых маркеров на Главной в обычном и редактируемом виде, сохранения глобальной кнопки «Помощь» и `data-help-block`-якорей, полного набора тестов и production-сборки. Одноразовый выпуск закреплён только за PR `#315` из `vitaliyozolin-dotcom/ArtHello-OS:codex/clean-dashboard-help-20260903`. До cutover обязательны exact PR/branch/main SHA, первый attempt, успешные Quality и Verify, Environment `production-ru`, неизменяемый проверенный образ, clone-preflight и резервная копия; при ошибке сохраняется автоматический rollback. Повтор успешной выкладки того же SHA запрещён.

## D-049 — Компактные блоки Главной собираются в строку и перетаскиваются вручную

Дата: 2026-09-03  
Статус: принято владельцем в явном поручении изменить Главную и выпустить изменение в production; одноразовый release PR `#316` закреплён

Главная сохраняет 12-колоночную сетку: компактный блок занимает 4 колонки, широкий — 8, полный — 12. Режим плотного размещения заполняет более поздними компактными блоками свободные ячейки предыдущей строки, поэтому три компактных блока могут стоять на одной горизонтали без пустой трети. На средней ширине остаются две колонки, на мобильной — одна.

В режиме «Настроить экран» видимый блок можно перетащить за его заголовок и отпустить до или после другого блока. Перестановка применяется один раз при отпускании и сохраняется существующим локальным и серверным механизмом отдельно для пользователя и точной роли. Стрелки «выше/ниже» сохраняются как доступное управление для клавиатуры и мобильных устройств. Схема данных, роли, разрешения и формат сохранённой раскладки не меняются.

Приёмка требует плотного заполнения строки компактными блоками, работоспособного drag-and-drop с визуальным состоянием цели, сохранения нового порядка после перезагрузки, неизменной ролевой изоляции и прохождения production Docker pipeline. Одноразовый выпуск закреплён только за PR `#316` из `vitaliyozolin-dotcom/ArtHello-OS:codex/dashboard-clean-drag-layout-20260902`. До cutover обязательны exact PR/branch/main SHA, первый attempt, успешные Quality и Verify, Environment `production-ru`, неизменяемый проверенный образ, clone-preflight и резервная копия; при ошибке сохраняется автоматический rollback.

## D-050 — Персональное рабочее пространство, понятный учёт и управляемые интеграции

Дата: 2026-09-03  
Статус: принято владельцем в явном поручении исправить перечисленные сценарии и выпустить результат; одноразовый release PR `#317` закреплён

Карточки пользователей отображают основные сведения и статусы в одной–двух строках, а должность при создании вводится свободно. Шаблон роли только предварительно отмечает разрешённые разделы: владелец обязан иметь возможность вручную снять или добавить каждый допустимый доступ до сохранения. «Интеграции» и «Доступы» находятся в настройках, а состав «Избранного» хранится отдельно для каждого пользователя и не расширяет его эффективные права.

Окна интеграций являются рабочими формами, а не демонстрационными карточками: поля, переключатели и действия доступны с клавиатуры и мышью, ошибки объясняются рядом с причиной. «Альфа-Банк» заменён на read-only подключение «Т-Банк» по официальному Open API; выпуск не получает права создавать платежи. JWT проверяется сервером, секреты не возвращаются в браузер, а при настроенном банком IP allowlist исходящий публичный IP должен совпасть с явно заданным владельцем значением. Подключение «Точки» сохраняет существующие owner-only и read-only ограничения.

Вход в электронный дневник использует только доверенные HTTPS origins ArtHello OS и School. Центральный секрет извлекается при выпуске из единственного работающего контейнера School, передаётся ArtHello OS только через read-only secret file и не записывается в репозиторий, GitHub secrets, environment или логи. Синхронизация прав и семей выполняется через durable outbox с идемпотентным event id, lease, CAS и сохранением порядка ревизий; владелец может безопасно повторить неуспешную доставку.

Сканы договоров сначала проходят ограниченную по типу и размеру серверную обработку, затем распознанный результат открывается как редактируемый черновик и сохраняется только после явного подтверждения пользователя. Ключ OCR/генерации изображений допускается только как runtime secret file. Понятные номера вида «Задача №1» и «Договор №0001» являются интерфейсными метками; внутренние идентификаторы остаются техническими ключами и не показываются пользователю без диагностической необходимости. «Приёмка» объясняет назначение и ответственного, а жизненный путь строится только для явно выбранного сотрудника.

Приёмка требует линтера, production-сборки, полного набора тестов, hosted Quality и Verify на точном squash-коммите, неизменяемого Docker image, изолированного clone-preflight, резервной копии до cutover и автоматического rollback. Одноразовый выпуск закреплён только за PR `#317` из `vitaliyozolin-dotcom/ArtHello-OS:codex/owner-workspace-integrations-20260903`. Production workflow обязан проверить exact repository, branch, PR, actor, первый attempt исходной Verify-проверки и актуальность SHA в `main`; повтор успешной выкладки того же SHA запрещён. Отсутствующие необязательные ключ OpenAI или подтверждённый T-Банком egress IP не подменяются тестовыми значениями: соответствующая возможность остаётся явно не настроенной.

## D-079 — Завершение жизненного цикла собственных тестовых образов

Дата: 2026-09-08. Статус: техническое продолжение действующего поручения Виталия подготовить изолированный браузер и продолжить выпуск; исполнитель Codex. Нового общего разрешения на очистку сервера это решение не создаёт.

D076 run34285119759/job102259393169 на2026-09-08T22:19:37Z остановился до скачивания архива и до входа: свободно6005696KiB при требовании7995036KiB. Пароль этой попыткой не использовался. D076 остаётся диагностикой, application cutover не выполнялся. Его прежний invocation cleanup и все проверки ёмкости сохраняются.

Отдельный шаг жизненного цикла может удалить только воспроизводимый тестовый образ, созданный R8 run34283447507: ID sha256:0763e7e6404c4ecf19b81bdcc236dd815a0210e9bb6b087adec279692cc7701c, source5385090d48f4dae29c314dff7ae974d854560940, единственный tag arthello-e2e:5385090d48f4dae29c314dff7ae974d854560940, portable fingerprint5970abba2518f5fc1f3bb27ef2624570e8050cbc606a3cb300f596031c26564e. Требуются точное совпадение ID/tag/source/fingerprint, e2e-browser, UID1000:1000, отсутствие неожиданных тегов/digests и ссылок из любых контейнеров, включая остановленные. Неоднозначность или отказ Docker останавливают действие.

Шаг выполняется только после trusted-main checkout под существующими production-ru и двумя shared locks, с новой проверкой current main перед мутацией и повторной сверкой образа. Восстановимость подтверждается существующим неистёкшим R8 artifact10078598718 с закреплёнными source/run/name/digest. Единственная разрешённая мутация — docker image rm --no-prune по полному закреплённому ID. Force, общий prune, смена target, удаление контейнеров, application images, backups, volumes или данных запрещены. Подтверждённое отсутствие обоих ID/tag допускает безопасный пропуск; ошибка доступа/daemon не считается отсутствием.

В конце только canonical D076 отдельный always-step также завершает жизненный цикл образа текущего запуска, если его полный ID был опубликован только после всех успешных import/identity checks. Для этого второго, единственного текущего target обязательны verified image ID, текущий exact source, fingerprint и archive name из needs.bundle, совпадающие tag/роль/UID, отсутствие любых consumers, доступный неистёкший artifact этого producing run, fresh main и повтор identity. Отсутствие verified output запрещает удаление и поиск замены по тегу. Ошибка cleanup видима; исходный archive cleanup остаётся неизменным. Этот финальный шаг не добавляется между pre/post browser приёмками release controller.

Эти два класса target — один закреплённый исторический образ и один проверенный образ текущего запуска — исчерпывают действие D079. Это явное узкое дополнение к прежнему запрету очистки образов ради места, а не переименование общей очистки. Старые D076 cleanup/capacity и R8 replay/cutover guards не расширяются. После шага обычный D076 вновь измеряет место; достаточный объём не обещается по логическому размеру образа. При недостатке места запуск прекращается с фактическими числами. Новый образ всегда собран для нового exact source; подмена старым образом запрещена.

Приёмка: отрицательные проверки чужой identity/тега/пользователя/роли/fingerprint, активных и остановленных consumers, недоступного daemon, неподтверждённого отсутствия и истёкшего recovery artifact; проверка единственной команды удаления и повторного current-main; независимый exact-head review, применимые hosted gates и фактический отчёт. Без live результата это preparation, а не выполненная очистка или успешный вход. D079 не является выпуском приложения; будущему кандидату нужен новый свободный D-номер и собственная проверенная release identity.

Уточнение представления Docker по фактическому D074 run34288036300/job102268045952 на2026-09-08T22:52:49.703Z: все признаки закреплённого R8 image и fingerprint совпали; RepoDigests содержит единственную ссылку arthello-e2e@sha256:0763e7e6404c4ecf19b81bdcc236dd815a0210e9bb6b087adec279692cc7701c, то есть на тот же полный ID. Эта служебная self-reference не является неожиданной ссылкой на иной image. Проверка допускает прежний пустой массив либо ровно одну ссылку arthello-e2e@<полный проверяемый ID>. Null, иные repository/digest и несколько ссылок запрещены. Targets, recovery artifact, повторные identity/current-main/no-consumer проверки, non-force deletion и capacity не меняются. Наблюдение не выполняло удаления или входа.


## D-085
Дата: 2026-09-09. Статус: принято к подготовке и проверке в рамках ранее разрешённого выпуска.

### Узкое продолжение R11 после остановки очистки R10
R10/PR379 опубликован в main как 2e57dd22c6ff1cec1fcad0bd11af479e465c00bb, tree 445762d319e5c792d97617e8a4515045bc3b4126. Main Quality34321728778, Proof34321728769 и V52 34321728779 — SUCCESS; Ruby contract и Caddy134 также PASS. Protected run34322039891, bundle102370988472 — SUCCESS, deploy102371510204 — FAILURE. На установленном Caddy прошли все134 HTTP/redaction проверки и сверка бинарника, затем EXIT-cleanup не смог удалить файл из собственного вложенного каталога test-fixtures с mode0555. Разрешение School sync secret, clone/cutover и browser-after не выполнялись. Последняя наблюдённая рабочая версия6596f69390ad539577ec2640e8ef40c7e12c22dc не переключалась. Старый точный R9 browser image удалён разрешённым D084 retirement; образы R10 импортированы, но приложение R10 не запускалось. Это pre-auth abort, не live/business PASS.

R11/PR380 сохраняет замороженные R10/R9/V52 и протокол D083 candidate state, gateway binding, browser acceptance, backup, rollback/public/bank activation. Меняются только новая identity/prefix/parent, отдельный guard с точным доказательством остановки R10, исполняемый helper очистки собственного fixture и дополнительный hosted regression gate. Исправление chmod ограничено собственными обычными каталогами текущего временного fixture; symlink/подмена владельца не дают расширить область удаления. Пакеты, права Docker, UID приложения и песочница не меняются. Локально тест воспроизводит прежний дефект в среде UID0 без capabilities и с NoNewPrivs; hosted gate дополнительно требует обычный UID. Исправленный сценарий обязан успешно очищать собственный fixture; условия частичной подготовки и подмены путей проверяются отдельно.

R10 нельзя вслепую перезапускать как новый кандидат. R11 guard проверяет точные source/tree/run/attempt/jobs/полный граф шагов R10, успешные Caddy134 и binary marker, конкретную ошибку очистки и отсутствие School secret resolution/clone/auth/public/bank. Ранние доказательства R5–R9 и same-source held-candidate resume сохраняются. До candidate-auth применяется прежний rollback; после границы входа запрещены snapshot restore, удаление кандидата и повтор public/bank. Неизвестный исход требует отдельно проверенного forward-fix с текущей БД.

Новый contract под deploy/v52/recovery-r11 попадает в существующий V52 trigger, хеширует все новые управляющие файлы и нормализует workflow к неизменённому R10 с перечисленными дельтами. Quality сохраняет все прежние jobs и добавляет d085-candidate-tests, чей точный PASS включён в protected provenance. Merge только проверенного PR380 с префиксом `D085: guarded R11`, ожидаемым родителем2e57dd22 и неизменным main во время защищённого задания. Общая gateway/School блокировка, production-ru, проверка текущего main, вне-VPS сборка и exact-image evidence обязательны.

Прежнее разрешение пользователя на исправление, тестирование и выпуск действует. Новые секреты, роль собственника, привилегированный запуск, общая очистка Docker или публикация банковских/дневниковых данных не требуются и не добавляются. AI готовит, проверяет и запускает этот ограниченный сценарий по точным evidence; неизвестные состояния не трактуются как разрешение ослабить guard. Банковская диагностика PR371 пока не привязана к принятому live source; денежная сверка четырёх счетов/реестра/ДДС, свежий resync и остальные бизнес-сценарии не объявляются завершёнными.


## D-086 — R12: ограниченное завершение жизненного цикла браузерного образа R10

Дата: 2026-09-09. Статус: подготовка и проверка в пределах ранее разрешённого выпуска. Владелец — Виталий; исполнитель — Codex. Цель прежняя: принятый выпуск и исходная бизнес-приёмка, а не только зелёный CI.

Факт: R11 main e579a20a2a1a40fab489340511f8c35dc6929081, tree d88300198e34e84a197118d4121f7da1fd3484cd. Quality34323893079, Proof34323893031 и V52 34323893021 SUCCESS. Protected run34324235442: bundle102377827825 SUCCESS, deploy102378379405 FAILURE. До скачивания browser archive измерены5807140KiB свободного места при требовании7995084KiB; scratch requirement3348735KiB выполнено. R9 browser уже отсутствовал. insufficient_import_space/exit2 остановили сценарий до Caddy/School secret resolution/clone/auth/public. Live source6596f69390ad539577ec2640e8ef40c7e12c22dc не переключался. Результат не является приёмкой приложения.

R12/PR381 имеет отдельные identity/head/parent и новый replay guard, который доказывает точные source/tree/run/attempt/jobs/полный граф R11 и конкретную остановку capacity. Все доказательства R5–R10, same-source pre-clone/held-candidate правила, границы auth, rollback и public остаются прежними. Frozen R11/R10/R9/V52 и Quality не редактируются. R11 исправление собственной fixture cleanup сохраняется. Контракт нового источника нормализует R12 к R11 только по перечисленным дельтам, хеширует helper/tests/capacity/fingerprint и отдельный hosted gate.

Единственная новая мутация — docker image rm --no-prune по полному R10 browser ID sha256:44ef654e88c3e92d4e1a1d967b5054594b0b7ed96928819111ba52fee28d937d. Source2e57dd22c6ff1cec1fcad0bd11af479e465c00bb; единственный tag arthello-e2e:2e57dd22c6ff1cec1fcad0bd11af479e465c00bb; роль e2e-browser; UID1000:1000; fingerprint9efafc066c918c5d68188e4eb3bc23c2feba82eb02875442d3db1072bafc1696. Recovery artifact10092321489, name server-browser-2e57dd22c6ff1cec1fcad0bd11af479e465c00bb-34322039891-1, run34322039891, digest sha256:6d4b187bf776d59a4386e19b03993c29f939a31b610444c058ce59b4fca1f53e, size640802387. Требуются доступность и неистёкший срок artifact с совпадением source/repository/run/name/digest/size, свежий current-main, повтор полной identity, только допустимая self-reference RepoDigest и отсутствие любых consumers, включая остановленные контейнеры. Ошибка Docker/API, неизвестный tag/digest или неоднозначное отсутствие останавливают действие. Никакие имена target не принимаются через CLI.

Шаг работает только после exact-main checkout под существующими production-ru и двумя shared locks, после прежнего R9 retirement и перед неизменённым capacity gate. Во время held-candidate resume пропускается. После удаления либо доказанного отсутствия helper повторно измеряет scratch/Docker space по прежней capacity.mjs: два архива, три expanded-копии, резерв2GiB. Реальный reclaim не обещается по логическому размеру. При недостатке места — blocked с численными фактами, до нового download/import. Ни reserve, ни source-identity нового browser не ослабляются.

Предложение условно удалить также R10 application image отклонено независимым review: снятие одного из двух тегов не может быть атомарно связано с закреплённым image ID и допускает гонку переназначения. Оно не входит в принятый R12 helper; application image не читается и не удаляется этим шагом. Force, broad prune, новые Docker privileges, удаление контейнеров/volumes/backup/данных, reuse/relabel browser, роль собственника и новые секреты не добавляются. Старые полномочия cleanup иных frozen шагов не расширяются.

Новый Verify ArtHello R12 continuation выполняется на обычном hosted UID по точному PR-head/main source с contents:read, без production Environment и секретов. Проверяет Ruby preservation contract, новый replay и отрицательные retirement tests. Его единственный d086-candidate-tests job, run_attempt1 и SUCCESS по exact main обязательны в protected provenance вместе со всеми прежними Quality/Proof/V52 gates. Merge только PR381 с префиксом D086: guarded R12 и ожидаемым parent e579a20a. Main во время protected run не менять. Локальные mock tests доказывают границы, фактическое освобождение места и полный natural browser должны подтверждаться защищённым запуском.

Контракт AI: готовить и проверять только этот ограниченный вариант по прежнему разрешению пользователя; не запрашивать повторный пароль/общее разрешение, не ослаблять guard ради успешного статуса, неизвестное считать blocked. Следующая метрика — фактический capacity PASS, затем candidate/post-public employee→Education→diary с identity и denied-module проверкой. Банковские четыре счёта/реестр/ДДС/resync, все доступы, обращения, AlfaCRM, контент/задачи и backup-приёмка остаются отдельными незавершёнными пунктами до собственных evidence. PR371 и visual accepted-runtime pin остаются закрытыми до принятого live source.

## D-087 — Наблюдение банковских счётчиков после подтверждённого public release

Дата: 2026-09-09. Владелец: Виталий; исполнитель: Codex. Статус: подготовка в рамках действующего поручения продолжить выпуск и исходную бизнес-приёмку. Источник полномочий — D-075 и уже предоставленное Виталием разрешение на read-only проверку production-данных. Supersedes: нет; D-075 сохраняет границы чтения, D-086 и прежние решения сохраняют историю выпуска. Полная запись маршрута и AI-контракта находится в `docs/PROJECT_PASSPORT_PRODUCTION_RU.md`, D087.

Зависимость этого шага — фактически принятый опубликованный runtime. Зафиксированная идентичность: source `77f26ec9bcba7233f39d5e8cb9f59c276bc8c1ed`, tree `ac3fcaac06acf33bf9ee32e438d0c040adb38fd7`, image `sha256:5a39c36001cb79abe6d0bc8d691275b58cdec5456e7d13d95fc717eba6702284`; protected run `34326582961`, attempt `1`, deploy job `102386111240`, результат `success`, время `2026-09-09T08:11:09Z`. Источник доказательств: [R12 protected release](https://github.com/vitaliyozolin-dotcom/ArtHello-OS/actions/runs/34326582961); candidate browser: [R12 deploy: candidate SSO at 2026-09-09T08:10:46.386Z](https://github.com/vitaliyozolin-dotcom/ArtHello-OS/actions/runs/34326582961/job/102386111240); post-public browser: [R12 deploy: post-public SSO at 2026-09-09T08:11:02.888Z](https://github.com/vitaliyozolin-dotcom/ArtHello-OS/actions/runs/34326582961/job/102386111240); backup evidence: [R12 deploy: backup state verified](https://github.com/vitaliyozolin-dotcom/ArtHello-OS/actions/runs/34326582961/job/102386111240). Без подтверждения этих фактов `EXPECTED_LIVE_SOURCE_SHA` остаётся пустым и диагностика не открывается. Состояние подготовки, прошедшие fixture/CI или отдельный успешный release step не заменяют фактический public outcome.

PR371 уточняет прежнюю проверку единственного consumer для принятого runtime: канонический data volume используют ровно два работающих контейнера — точное опубликованное приложение и его точный штатный R7 backup worker с read-only `/data`. Их source/tree/image/container identity, mounts, ресурсы и приватные candidate/activation/backup state связываются с контекстом выпуска; проверяются сохранённые gateway evidence и текущий public route. Наличие этих файлов само по себе не является новым browser receipt или доказательством истории бэкапов. Неизвестный consumer, drift, некорректный state или ошибка доступа прекращают чтение. До и после probe проверяется та же выборка runtime; каноническая SQLite отдельно подтверждается живыми D1 file descriptors.

Доступ остаётся в рамках существующего owner-controlled exact-main workflow: только первый attempt успешного Quality push-main с префиксом merge commit **`D075: read-only production data`**, тот же repository/owner, `production-ru` и обе общие блокировки gateway/School. Новый номер решения не меняет технический D075 trigger. `CHECKED_SOURCE_SHA` — SHA доверенного checkout диагностики, `EXPECTED_LIVE_SOURCE_SHA` — отдельно закреплённый SHA уже принятого приложения. Диагностический merge не является deployment. Quality не меняется; отдельный hosted `Verify bounded production data diagnostic` проверяет точный PR-head/main, а protected job повторяет целевые проверки перед чтением.

Прежние 15 табличных счётчиков и восемь ссылочных проверок сохраняются. Банковское дополнение отчёта содержит только фиксированные статусы и счётчики для `INT-T-TOCHKA`: фактические количества account keys и юридических лиц, реестр проведённых RUB-операций и его связи, последнюю sync-запись с `dry_run=0`, покрытие периода с `2026-09-01` по текущую дату UTC, совпадение количества операций с сохранёнными выписками и нарушения внешней идентичности. Запрос считает все найденные account keys подключения; для завершения требуются ровно четыре корректных уникальных ключа и одно юридическое лицо. Идентификаторы счетов, контрагенты, назначения и суммы не выводятся. Дополнительный `accountsWithContainingStatementInLatestRun` считает последние готовые выписки, включающие весь период; он не заменяет точное равенство дат в `coveredInLatestRun` и `accountsWithMatchingTransactionCount` и не ослабляет `checksComplete`. Пустой реестр не доказывает активность. Дубликаты и отсутствующая внешняя идентичность проверяются по всем сохранённым операциям этого подключения, остальные банковские счётчики операций — в указанном периоде.

В production вызов не задаёт `syncNotBefore`; значение `sync.freshness=not_requested` не доказывает свежий resync после выпуска. `bounded_checks_complete` означает только завершённые ограниченные проверки сохранённых счётчиков и связей. Финансовая сверка четырёх счетов с банком, деньги/ДДС, новый resync, права UI/API, обращения, выборочный AlfaCRM и ручные/автоматические бэкапы остаются отдельными пунктами исходной приёмки. Маркер `READONLY_FINISHED=aggregate_observation_not_live_acceptance` не является PASS всего проекта.

Сохраняются UID1000, канонический volume read-only/no-copy, network:none, read-only rootfs, cap-drop, no-new-privileges и лимиты. SQL writes, копия БД, получение credentials, запросы к банку, resync, платежи, смена ролей/маршрутов, публикация приложения и restore не добавляются. При отсутствующей схеме, превышенном лимите, неизвестной identity или ошибке выводится фиксированный неполный/blocked статус, без сырых исключений и персональных записей. Следующий переход выбирается по фактическому отчёту, без подмены финансовой приёмки счётчиками.


## D-088 — Завершённое read-only наблюдение и расписание ожидания банка

Дата:2026-09-09. Техническое продолжение D075/D087 в рамках действующего поручения Виталия проверить четыре счёта и исходную бизнес-приёмку. R12 остаётся принятой рабочей версией77f26ec9bcba7233f39d5e8cb9f59c276bc8c1ed; диагностический main9b6c7069b5163d99385ccc4a48066bf760235bf4 не публиковал приложение. Новых полномочий на платежи, credentials, импорт, resync или SQL writes это решение не создаёт.

Факт D087: protectedrun34328837388/job102392362747 на2026-09-09T08:23:37.152Z подтвердил exact app/backup consumer и canonical D1, прочитал согласованный SQLite snapshot и выдал incomplete_or_issues. Найдены4счёта/4уникальных ключа/1юрлицо,12записей выписок,0bank_transactions/financial_operations. Последняя сохранённая sync-запись pending,0rejected/errors/conflicts; покрытие exact и containing периода2026-09-01…09-09 в последнем run равно0. После правильного exit2 по замечаниям launcher выдал общий probe_failed_or_timed_out и не выполнил повторный observer. Это реальный начальный snapshot, но не завершённое финальной сверкой наблюдение и не доказанная ошибка банка.

Отсутствующие developer_feedback/events и AlfaCRM snapshots/merge-candidates tables создаются лениво обычными API обработчиками модулей. Их отсутствие в snapshot само по себе не доказывает сломанный выпуск. D088 не вызывает эти обработчики и не создаёт таблицы. Полнота исходной бизнес-приёмки сохраняет отдельные критерии.

Launcher различает корректный полный отчёт, корректный отчёт с замечаниями, blocked, malformed и timeout/ошибку процесса. Для валидных complete/incomplete результатов обязательна повторная точная сверка тех же consumers/runtime после probe. Успешное завершение чтения может сопровождаться exit2 и incomplete_or_issues; замечания не становятся PASS. На выходе только проверенный ограниченный JSON и фиксированные маркеры, без сырых stderr/исключений. Прежние UID, read-only/no-copy canonical volume, network:none, cap-drop, no-new-privileges, ресурсы/deadline и cleanup только своего helper сохраняются.

К прежним банковским счётчикам добавляется отдельное ограниченное наблюдение сохранённых scheduler/setup/retained-statement metadata: валидированные даты последней sync-записи и возраст состояния scheduler, фиксированные исходы/счётчики расписания и lease, startDate/интервал, сохранённые окна запросов и нормализованные статусы импортов. Генерации сравниваются только по безопасным сохранённым metadata без вывода значений. Для retained jobs отсутствие сохранённого provider status или недоказанное соответствие текущему scope остаются явными unknown/not_stored/unverified; наличие job не означает, что банк ещё готовит выписку. Запрещены IDs/номера счетов, суммы, произвольный config JSON и чтение/decryption credential values. Прежние exact-window, containing-window и checksComplete не ослабляются.

Причина расширения: активация R12 только разрешила timer; setup/startDate/nextAt и lease сохранялись. Pending исход планирует повторный tick, однако прежний latest sync может быть старше последнего not_due/busy tick, который не создаёт sync run. Один pending snapshot не различает будущее расписание, lease, старый frozen window и актуальное ожидание provider. Новые наблюдения определят следующий конкретный шаг; никаких банковских запросов или принудительного resync в диагностике нет.

Прежний D075 owner/repository/first-attempt успешного Quality push-main trigger, production-ru, shared locks и pin actual live77f сохраняются. Merge title по-прежнему начинается D075: read-only production data; D088 — номер нового решения, не новый доступ. Quality и frozen release workflows не меняются. Требуются независимый exact-source review, целевые реальные регрессии, hosted exact-head CI и фактический защищённый результат. AI выполняет только этот проверяемый read-only сценарий; при identity drift или некорректном отчёте останавливает вывод/завершение; неизвестную schema отмечает в отчёте, не подставляет fixtures и не ослабляет guard. Финансовая сверка, fresh resync, ДДС и другие бизнес-сценарии остаются открытыми до своих evidence.


## D-089 — Hosted-проверка экранов «Контент» и «Задачи» принятого R12

Дата:2026-09-09. Владелец — Виталий; исполнитель — Codex. Подготовка в рамках исходного поручения проверить контент, задачи и их номера. R12 source77f26ec9bcba7233f39d5e8cb9f59c276bc8c1ed/treeac3fcaac06acf33bf9ee32e438d0c040adb38fd7 фактически принят в protectedrun34326582961; candidate и post-public natural SSO PASS. Это основание для pin образа, а не результат проверки этих экранов. D088 банковское наблюдение остаётся отдельным пунктом; main не менять до его terminal outcome.

Новый hosted workflow повторно использует исходный проверенный image artifact10093988488/run34326274447, runtime fingerprinta28b64b3a57f96eb8e23c18d888d033f70d90f92b2ef98df5cc7b07c920d153a. Проверяются source/tree/run, неистёкший уникальный artifact, внутренние checksum/source hashes и imported runtime fingerprint. Приложение не пересобирается. Существующие harness blobeee9647d7f0e4799e293613c01eaa4199d80dc06, fixture и browser source сохраняются. Новый scoped adapter выбирает только Content/Tasks из прежних сценариев, не выполняя исходный полный entrypoint.

Исполнение ограничено GitHub-hosted runner с contents:read/actions:read и отдельной очередью. Создаются собственные новые network/volume/контейнеры, пустая БД и синтетические credentials. Fixture разрешён только после подтверждения нового пустого volume и остановки приложения; данные production не копируются. Browser работает без root/capabilities, с песочницей, read-only root и закрытой сетью. Только свои runtime resources удаляются после исполнения; в artifacts — только синтетические PNG, metrics и ограниченное evidence. Production Environment, SSH, собственническая сессия и секреты выпуска не используются.

Новый исполнимый путь дополняет ручной workflow_dispatch: first-attempt push на refs/heads/main от owner, чей commit message начинается D089: hosted Content Tasks visual. Оба actor/triggering_actor и repository закреплены, source job обязателен. PR, иной owner/ref/prefix и push retry visual job не запускают. startsWith GitHub сравнивает без учёта регистра; каждый отдельный соответствующий owner push может создать новый run. Ручной dispatch сохраняет прежние owner/main ограничения. Это конкретный путь выполнения ранее разрешённой приёмки через проверенный owner merge, без расширения production authority.

Ожидаются14 PNG: Content/Tasks на390×844 и1440×900, пустые и заполненные состояния, task dialog. Проверяется видимая существующая строка «Задача №801», без изменения правил нумерации приложения. Только успешный hosted run с полным manifest/scoped-result/evidence и14 PNG позволяет принять этот ограниченный визуальный сценарий. API-ответы данных синтетические; пиксельного baseline, live финансовой правды, сохранения форм, реального генератора контента, доставки назначений и всех прав пользователя этот тест не доказывает.

AI готовит и проверяет собственный scoped runner, проверяет точные источники и исполняет разрешённый hosted сценарий. При mismatch provenance, дополнительном файле artifact, непустом volume, отсутствии sandbox, ошибке cleanup или неполном наборе evidence — blocked/failure, без замены runtime либо запуска fixture на live. Source review,12 adapter tests и4 trigger tests с35 сценариями — подготовка; окончательный статус зависит от реального Chromium. Прежние Quality, R12 и банковские workflows не изменяются. Полная бизнес-приёмка ведётся по отдельным фактическим результатам.


## D-090 — Клиент AlfaCRM использует действующий CSRF-контракт

Дата: 2026-09-09. Владелец — Виталий; исполнитель — Codex. Статус: подготовка исправления в рамках действующего поручения завершить выпуск и бизнес-приёмку. Supersedes: нет. D088/D089 и прежние ограничения сохраняются; запись маршрута и AI-контракта — паспорт production, D090.

В принятом R12 source `77f26ec9bcba7233f39d5e8cb9f59c276bc8c1ed` мастер AlfaCRM отправляет `x-arthello-csrf` из `arthello_csrf`, тогда как действующий сервер выдаёт `__Host-arthello_csrf` и проверяет только `x-csrf-token`. Ограниченная локальная fixture выполнила настоящий POST-отправитель компонента против неизменённых `appendAuthCookies`, `verifyAuthenticatedRequestCsrf` и origin guard: RED — один ожидаемый отказ `csrf_denied` из пяти тестов; после исправления GREEN — 5/5. Независимый review подтвердил ограниченный scope; это evidence подготовки, не наблюдение live POST.

Изменяются ровно два code/test файла под `deploy/v52/overrides`: одна строка в `app/components/AlfaCrmSetupWizard.tsx` использует `"x-csrf-token": readCookie("__Host-arthello_csrf")`; новый `tests/alfacrm-csrf-contract.test.mjs` проверяет совместимость настоящего отправителя и сервера. Серверные auth/access/origin/CSRF проверки, роли, connection assignment, branch scope, endpoint, payload и cookies сервера не меняются. Legacy fallback не добавляется. Отрицательные случаи сохраняют запрет legacy-only cookie/header, неверного токена и иного origin.

`ALFACRM_IMPORT_ENABLED`, подтверждение источников и направления финансовых операций остаются прежними. Исправление запроса не разрешает импорт, не доказывает upstream доступ и не снимает intentional finance projection block. Новые credentials, сетевые вызовы AlfaCRM, сообщения и production-действия при подготовке не выполнялись.

Перед продвижением: сохранить историю D088/D089 при переносе на актуальный main, сверить exact source/tree и неизменность auth dependencies, получить hosted полный application build/tests и необходимые CI. Выпуск и обычное пользовательское поведение подтверждаются отдельно по действующим release gates. Пока hosted aggregate и live browser/API evidence отсутствуют, D090 не считается выпущенным или завершённой приёмкой AlfaCRM.


## D-091 — Код результата автоматической синхронизации банка

Дата: 2026-09-09. Продолжение разрешённой проверки четырёх счетов. D088 завершил валидное наблюдение с финальной сверкой consumers: run 34330892606, job 102398954685, время 08:46:04.026Z. Рабочее приложение остаётся R12 source 77f26ec9bcba7233f39d5e8cb9f59c276bc8c1ed. Четыре счёта и 12 записей выписок существуют, bank_transactions и financial_operations пусты. Последняя sync-запись pending от 08:11:14.266Z не отражает две последующие ошибки scheduler.

Наблюдались outcome=error, failures=2, released lease, совпадение сохранённой generation и nextAt=2026-09-09T09:01:16.902Z. Интервал соответствует штатному 30-минутному ожиданию после второй ошибки; конкретная причина не установлена. Существующий scheduler сохраняет числовой httpStatus, который не вошёл в D088. Текст исключения и отдельный код причины не сохраняются.

D091 добавляет только безопасное чтение autosync.httpStatus (целое 100–599 либо null), httpStatusState (observed/missing/invalid) и нормализованного updatedAtUtc. Сырые JSON, ошибки, IDs и credentials не выводятся. Схема валидатора расширяется ровно этими полями; прежние SQL-проверки, checksComplete, exit 2 для замечаний и финальная сверка не меняются. По результату 500 нельзя объявлять конкретную ошибку банка: это также значение по умолчанию при исключении callback до ответа. 422 обычно означает отклонённый результат sync, 200 с outcome=error — неприемлемый результат или разбор JSON; 409 обычно формирует busy. Не домысливать отсутствующие сведения.

Прежние D075 owner/Quality/main/first-attempt, environment, locks, canonical read-only volume, ограничения helper и accepted-live pin сохраняются. Merge prefix остаётся D075: read-only production data. Никаких сетевых запросов к банку, resync, SQL writes или смены конфигурации. Требуются независимый review точного source/tree, целевые тесты совместимости и hosted CI, затем фактический законченный отчёт. Неизвестные значения остаются неизвестными; identity drift, некорректный отчёт и timeout блокируют завершение. Полная финансовая приёмка остаётся открытой.


## D-092 — Права файлов при сборке hosted-браузера Content/Tasks

Дата: 2026-09-09. Владелец — Виталий; исполнитель — Codex по действующему поручению проверить контент, задачи и их номера. D092 готовит исправление сборки вспомогательного браузера после фактического отказа D089. Технический trigger `D089: hosted Content Tasks visual` и его owner/repository/main/first-attempt ограничения сохраняются. Номер решения D092 не меняет trigger и не даёт production-доступ.

Факт: run `34331175805`, job `102399900660`, head `3632008d57f149b9e300b22f502e6dc48a5326ac` проверил и импортировал принятый application artifact. Установка `playwright-core@1.62.1` и Docker build завершились с кодом 0; smoke остановился с `ERR_MODULE_NOT_FOUND` при импорте из `/opt/arthello-e2e/run.mjs`. Fixture не запускался. Получено 0 artifacts и 0 PNG; Content/Tasks visual acceptance не пройдена.

Реальный локальный pnpm `11.7.0` воспроизвёл каталоги `0700` и файлы `0600` при `umask 077`. Symlinks оставались внутри build context. Повторная установка с `umask 022` может повторно использовать файлы `0600` из cache, поэтому одного изменения umask недостаточно. Frozen Dockerfile сохраняет права при `COPY`, создаёт root-owned файлы и исполняет браузер как UID/GID `1000:1000`. Проверка под UID1000 в текущей среде недоступна (`OS EINVAL`), Docker отсутствует; чтение пакета внутри нового образа и Chromium ещё не подтверждены. Сохранённый failed-run не содержит фактических mode/link diagnostics, поэтому совпадение локального механизма с каждой деталью удалённого отказа не объявляется отдельно доказанным.

Предлагаемое исправление ограничено собственным временным public browser context. В него копируются ровно восемь неизменённых входов принятого source: `Dockerfile`, `.dockerignore`, `package.json`, `pnpm-lock.yaml`, `run.mjs`, `flow.mjs`, `smoke.mjs`, `proxy.mjs`. Browser install выполняется с локальным `umask 022` и копированием package content из store. До нормализации проверяются отсутствие absolute/dangling/escaping symlinks и специальных файлов; при нарушении сборка останавливается. Только в этом собственном public context файлы становятся читаемыми, каталоги — доступными для обхода после root-owned Docker COPY и запуска UID1000. Глобальный `umask 077`, секреты и приватные fixture/evidence пути сохраняются. Не применять chmod к исходному checkout, общему store, рабочим данным или чужим ресурсам.

Accepted R12 pin остаётся `77f26ec9bcba7233f39d5e8cb9f59c276bc8c1ed`, tree `ac3fcaac06acf33bf9ee32e438d0c040adb38fd7`, producing run `34326274447`. Dockerfile, browser source, lock, original harness, scoped adapter, его двенадцать тестов и существующий trigger не изменяются. После локальной регрессии и независимого review нужны exact-source hosted CI и новый фактический browser smoke; затем полный scoped результат с 14 PNG. Новая реальная pnpm assembly regression: 4 теста PASS с точными browser inputs и frozen lock под pnpm `11.7.0`. Проверены relocation context с удалением исходного каталога и реальный импорт `playwright-core`, отказ на private modes до нормализации, absolute/escaping/dangling links и shared hardlinks до chmod, возврат `077` и сохранность уже существующего context. Эти результаты не являются PASS образа, UID1000, Chromium или бизнес-приёмки.

AI готовит только этот assembly fix и проверяемые доказательства. Стоп — drift frozen inputs/runtime, escaping/dangling link, special file, выход нормализации за собственный context, ошибка smoke/cleanup или неполный результат. При отказе не ослаблять sandbox/UID, не менять runtime и не переносить fixture на live. Новая публикация и запуск требуют соблюдения текущей очереди: main не менять во время protected банковского наблюдения. Банковская D091, выпуск приложения и оставшиеся бизнес-критерии остаются отдельными пунктами.


## D-093 — Проверить собственные обращения сотрудника и отказ в доступе к backup

Дата: 2026-09-09. Владелец — Виталий; продолжение разрешённой пользовательской приёмки на рабочем R12. После обычного входа в ArtHello отдельный employee-controls flow открывает «Разработчикам», проверяет успешный GET `developer-feedback?scope=mine`, выбирает «Мои обращения», дожидается завершения загрузки и проверяет отсутствие «Все обращения». Затем закрывает диалог и проверяет только HTTP 403 от GET `/api/settings/backups` в той же сессии, без чтения тела и следования redirect. Send, заполнение формы, управление обращениями, роли и дополнительные mutation/network exceptions не добавляются. Прежние employee/Education/School и отрицательная навигационная проверка сохраняются; полный permission matrix, владелец backup status и история резервных копий этим не подтверждаются.

Открытие собственного списка является обычным пользовательским действием: серверный GET может лениво создать таблицы feedback. Это не строгая read-only D075 диагностика. Не запрашивать пароли у пользователя, не фабриковать сессию, не выводить cookies, тела обращений или API responses. Результат содержит фиксированные признаки `feedbackDialog`, `feedbackOwnList`, `feedbackAllHidden`, `backupApiDenied`; не подменять фактический результат fixture-тестом.

Для места под новый browser bundle допускается только условное удаление точного неиспользуемого R12 browser image через новый fixed-target helper. Его image ID `sha256:e07d75e5964218b4382d98e255c5f17829dc6a4429ccef3060fb833d4cc3b61f`, source `77f26ec9bcba7233f39d5e8cb9f59c276bc8c1ed`, recovery artifact `10094111549` и остальные immutable поля закреплены в исходнике. Наличие не предполагается. При полном отсутствии ID и tag — без удаления и без чтения artifact; при наличии — точная identity, fingerprint, отсутствие running/stopped consumers, два свежих подтверждения matching unexpired recovery artifact и повторная проверка main перед удалением по ID без force/prune. Приложение, containers, backup worker, volumes и данные не удаляются. Канонический D076/R8 workflow остаётся неизменным; новый D093 workflow не запускает лишний R8 retirement. Capacity thresholds сохраняются; измерение после retirement не отменяет отказ при нехватке места. Зафиксированный срок artifact — `2026-09-11T08:01:35Z`, но при исполнении требуется свежая проверка.

Новый технический prefix — `D093: employee controls`; отдельный `.github/workflows/check-arthello-employee-controls.yml` сохраняет owner/repository/Quality push-main/first-attempt, environment и обе locks. Канонические D076 workflow, flow, smoke и tests восстановлены byte-for-byte к D092 main `b4c39d5af8ab348882759c48c0f82dbe810a8065`. Причина разделения подтверждена PR387: новый browser fixture прошёл, но frozen R8/R9 bundle equality отклонил изменение канонического bundle. Старые recovery contracts не ослабляются. Отдельный Dockerfile использует тот же pinned browser base/runtime/packages и явно отображает новые employee-controls flow/smoke в пути контейнера. Локально 74 browser/retirement/canonical tests PASS; полные Ruby contracts и новый hosted image требуют CI точного исправленного source. После публикации сохранять main до terminal protected outcome и финального browser cleanup. Это проверка интерфейса без нового application deployment; accepted live R12 и финансовая приёмка остаются отдельными доказательствами.


## D-094 — Фиксированный этап сбоя автоматической загрузки Точки

Дата: 2026-09-09. Владелец — Виталий; продолжение разрешённого исправления банковской загрузки. Последнее фактическое наблюдение D091: run `34332656641` / job `102404738568`, 09:05:27Z; accepted R12, четыре счёта, 12 import records, ноль банковских/финансовых операций. Scheduler сохранил outcome=error, failures=3, callback HTTP500 в 09:01:19Z и следующий обычный запуск 10:01:19.940Z. Это не доказательство HTTP500 от банка: значение по умолчанию сохраняется при исключении до ответа callback.

D094 добавляет только nullable `failureStage` к завершённому scheduler state и внутреннему результату. Допустимы закрытые имена `setup_references`, `credential_read`, `statement_state_open`, `bank_sync`, `statement_fence`, `sync_commit`, `statement_acknowledge`, `statement_release`, `sync_callback`, `response_decode`, `response_result`. При complete/pending/busy значение null; пропуски и superseded сохраняют прежнюю форму. Обёртка отмечает лексический этап и повторно бросает исходное исключение, включая прежний приоритет ошибки finally-release. Ни сообщение/стек/свойства ошибки, ни credentials/payload/банковские идентификаторы не читаются для диагностики. Этап bank_sync сам по себе не подтверждает отказ провайдера.

D075 producer/launcher расширяются ровно полями autosync.failureStage и failureStageState (observed/missing/invalid). Только outcome=error и допустимое имя допускают observed; неизвестные значения и несовместимые поля не отражаются в отчёте. SQL остаётся read-only, прежние финансовые checksComplete и exit2 для замечаний неизменны; обязательна завершающая сверка фактических consumers. Старый R12 без поля корректно даёт missing. Никаких прямых DB writes из диагностического задания, ручного запуска scheduler, изменения scope/дат/ключа/авторизации/lease/backoff или новой service capability.

Четыре application/test overlays должны попасть в отдельный проверенный последующий release, прежде чем их можно считать действующими. Четыре diagnostic/test overlays обеспечивают безопасное наблюдение сохранённого результата. Локально 22 scheduler/actual assembled-route tests, 39 aggregate tests и 21 launcher tests PASS; full exact-source CI и независимый review обязательны. Полная финансовая приёмка остаётся открытой; подтверждать причину и результат исправления только по фактическому следующему обычному запуску. Стоп — source/runtime drift, нарушение auth/lease/exception semantics, неподтверждённый отчёт либо изменение принятых финансовых критериев.

Первый full application run `34335598905` / job `102414127659` дал 767/769 PASS: два старых source-ratchet tests ожидали прямой await acknowledgement/commit, теперь эти же операции обёрнуты lexical observer. D094 синхронно обновляет ровно эти ожидания на точные stage wrappers, сохраняя rejectedCount===0 и строгий commit-before-acknowledge. Дополнительный actual-route behavioral test подтверждает: rejected rows и отказ commit не подтверждают statement, успешный commit предшествует acknowledgement, lease всегда освобождается. Исправленные 22 focused tests PASS; полный новый image build остаётся обязательным.


## D-095 — Единый формат номера задачи с четырьмя цифрами

Дата: 2026-09-09. Владелец — Виталий. Требование пользователя — «Задача №0001…». Accepted R12 и текущий source выводят `Задача №1` / `Задача №801`: общий taskRecordLabel использует recordSequence напрямую, хотя стандартный recordLabel уже дополняет номер до четырёх цифр. D095 меняет одну строку общего форматтера: taskRecordLabel вызывает recordLabel("Задача", value). Существующие очередь, карточка, drawer, подзадачи, уведомления, история и связанные ссылки используют этот helper, поэтому получают одинаковый формат.

Хранимые IDs, API paths и связи не меняются. Четыре formatter tests PASS; до исправления два новых ожидания не проходили, после проходят все четыре. Проверены 1→0001, 801→0801, границы цифр и legacy/linked values; независимый source/callsite review PASS. Full application CI и фактическая проверка на новом runtime обязательны. Accepted R12 synthetic visual всё ещё закреплён на старом source77f и №801; не менять его ожидаемое значение без нового runtime provenance.

Существующий recordSequence сворачивает значения по модулю10000; форматированные подписи не гарантируют глобальной уникальности. Поиск продолжает сопоставлять raw IDs, поэтому введённый padded номер может не найти raw ID без совпадающей строки. Эти прежние ограничения фиксируются отдельно; D095 не выдаётся за новую систему последовательной нумерации или исправленный поиск. Стоп — изменение raw IDs/связей, регрессия иных record labels, несовпадение source либо неподтверждённая сборка. Этот source change станет действующим только после следующего release; live R12 пока прежний.


### D092 — Прочитать существующий visual receipt после успешного smoke

Фактический D092 hosted run `34333105787` / job `102406140419` на source `b4c39d5af8ab348882759c48c0f82dbe810a8065` прошёл сборку браузера и Chromium sandbox fixture (09:11:12.017Z), затем подготовил empty fixture и остановился с `ARTHELLO_CONTENT_TASKS_SCOPED=BLOCKED`. Это исправило первоначальную packaging-проблему; визуальная приёмка не завершена. Сохранён artifact `10096640452`, 389416 bytes, SHA256 `dd600c987b34a1d92a1270a63926815b2a1715c9efb01400a7d1434748e7de5a`, expires 2026-09-23. Локальное получение официально материализованного файла не удалось (HTTP403 при прямом transfer, HTTP502 через поддерживаемый helper); bytes/PNG не просмотрены, обход ограничений не выполнялся.

Для чтения уже созданного receipt добавляется отдельный GitHub-hosted workflow `inspect-d092-visual-evidence.yml`, prefix `D092: inspect existing visual evidence`. Только owner/repository/push-main/attempt1 и точный current-head; PR запускает только synthetic source tests. Permissions ровно contents/actions read, без Environment, production secrets, Docker, browser/fixture rerun или изменения старого adapter. Guard проверяет fixed producer/run/source/artifact/name/size/digest/unexpired metadata, затем обычный authenticated GET ZIP; reader проверяет размер/digest ZIP и ограниченную схему receipt. В лог идут только фиксированный stage/result и counters. PNG не извлекаются и не объявляются просмотренными; receipt не раскрывает произвольный текст причины. Новое принятое поведение продукта не вводится, номер нового D-решения не требуется: это продолжение сбора D092 evidence.

Локально 5 reader, 6 metadata/refusal и 4 trigger tests PASS, проверки Python -O, YAML/shell и независимый семифайловый review PASS. Фактическое чтение старого receipt и определение следующего UI исправления остаются pending до этого нового hosted run. Стоп — несовпадение identity/digest/схемы, истёкший artifact, изменение main или неподтверждённое чтение. Не перезапускать UI вслепую и не объявлять 14 PNG либо visual PASS по наличию artifact.


### D092 — Совместимость workflow fixtures с принятым R12

Hosted inspection run `34337932856` / job `102421685466` на source `0e8e75ffd73f6a85e8171d4d261d6a482c967136` успешно прочитал fixed artifact10096640452 (2026-09-09T10:01:29.9312752Z). Фактический receipt: tasks-empty-390x844, completedCaptures2, pngEntries4, taskNumberAssertion not_confirmed, screenshotsInspected false. Две проверки пустого Content предшествуют этому этапу; Tasks capture не завершился. Actual exception и PNG не просмотрены.

В exact accepted WorkflowWorkspace blob `4a8f839a6f2ab52a504973319d05b44596699487` loading заменяет data ответом API, Documents tab обращается к data.permissions.canManageDocuments; frozen emptyWorkflow не содержит permissions. Все четыре вкладки открываются до первого Tasks screenshot. Дополнительно frozen populatedWorkflowDetail не содержит permissions, требуемые drawer (canManage/canApprove). Это доказанные несовпадения схем тестовых данных; связь первого с actual blocked stage обоснованно предполагается, но без raw runtime exception не объявляется непосредственно наблюдённой.

Исправление ограничено новым scoped adapter: после полного Git-blob контроля неизменённого harness в два однозначных fixture boundary добавляются точные permissions objects существующего synthetic OWNER — три overview и четыре detail booleans, подтверждённые accepted API. Изменений app roles, endpoints, production data, frozen full harness, геометрии/UI assertions или literal №801 нет. Полный набор проверок должен теперь пройти на прежнем accepted R12 artifact; до фактического hosted result visual acceptance остаётся открытой. D095 padding относится к будущему runtime и не подставляется в старую проверку.

Новое поведение продукта не вводится; это продолжение D092 восстановления evidence. Hosted запуск выполняется существующим owner exact-main/attempt1 механизмом с техническим merge prefix `D089: hosted Content Tasks visual`; main остаётся неизменным до результата. Стоп — source/fixture boundary drift, ослабление assertions, иной runtime pin или неподтверждённый результат. Следующий запуск обоснован конкретным schema mismatch, не произвольным повтором.


## D096 — Guarded R13 после принятого R12 (2026-09-09)

Владелец — Виталий. Продолжение разрешённых исправлений и публикации без повторного запроса пароля. Принятый R12: source `77f26ec9bcba7233f39d5e8cb9f59c276bc8c1ed`, tree `ac3fcaac06acf33bf9ee32e438d0c040adb38fd7`, run `34326582961`, attempt1; bundle/deploy завершены успешно. Natural employee → Education → School identity проверен до и после публикации; D093 run `34335665105` дополнительно подтвердил own feedback UI/list, отсутствие All и backup API403. Это принятый работающий baseline; старый success не переосмысляется как abort и не перезапускается.

Цель R13 — доставить уже проверенные D090 (правильные CSRF cookie/header Alfa wizard), D094 (ограниченный failureStage банковского callback) и D095 (четыре цифры task label) отдельным свежим выпуском. Образ, source/tree, текущий main, PR и exact-source Quality/Proof/V52/R12/R13 gates должны совпасть. До фактического protected success это подготовка, не опубликованный R13. D092 hosted inspection run34337932856 прочитал уже существующий visual artifact: stage tasks-empty-390x844, два capture records, четыре PNG entries; actual PNG не просмотрены, task number не подтверждён. Это фиксирует незавершённую проверку Tasks, не полную visual приёмку.

Метрика после выпуска — реальные candidate/post-public natural browser receipts и наблюдение нового failureStage на обычном scheduler tick; это не обещание успешного банковского импорта, Alfa import или Content creation.

Новый контроллер использует историю успешного R12 и отдельную fresh live проверку accepted app/source/image/runtime, сохраняет School relay identity. Только подтверждённый retained candidate того же нового run может продолжить после сбоя до public boundary; после возможного seal, authentication или public-start нет fresh replay/старого database restore. Проверки run/history до checkout встраивают точные проверенные helper bytes и четыре неизменённых pure D080 validators. Оба shared locks, production Environment, canonical D076 bundle, capacity checks и прежние frozen release contracts сохраняются. Никаких broad image/container sweeps; optional retirement касается только точного неиспользуемого recoverable R12 browser image. Его отсутствие не доказывает достаточную ёмкость.

Резервное копирование: сохраняется и используется точный принятый worker `570d3fb2f96dfa7a08d0a98fbeda0d208f08c0805efcc444bc958fbabf515052` и существующие history/control volumes; новый второй worker/history не создаётся. Новому run принадлежат отдельные activation/seed и adoption state. Перед cutover создаётся свежий rollback snapshot текущей canonical D1. До seal возможный copyback требует containment, остановки всех точных consumers, quiesce/verify-copyback и восстановления исходного restart policy/здоровья reader. Любая неоднозначность seal или durable boundary сохраняет ресурсы и блокирует восстановление старых данных. Digest sealed adoption входит в private candidate context. Точный исторический предшественник R12 допускается только как остановленный consumer, доказанный accepted context digest; неизвестные consumers запрещены, predecessor не удаляется.

Банковские auth/selected scope/dates/leases/backoff/activation protocol не расширяются; forced retry остаётся вне выпуска. Данные, payment operations, роли и сообщения сотрудникам не создаются вручную. Стоп — source/provenance/identity/capacity/history/consumer/backup proof mismatch или отказ владельца. После публичной границы восстановление выполняется отдельной оценкой текущих данных, не старым snapshot. Полные owner-backup, daily future execution, Alfa scoped import, Content и financial coverage остаются открытыми до своих реальных доказательств.

Закрепление нового выпуска: PR392, branch `codex/arthello-r13-20260909`, parent `7f398e3180fb341754c267760463dec968fc6420` / tree `bd803a153bdadfb2fced0890190a22769042c40e`. Родитель включает D092 fixture compatibility из PR391; его отдельный hosted visual результат при подготовке ещё ожидается. Новая release identity не равна опубликованному R13; фактический результат дописывается после protected run.


## D-097 — Уникальность банковской операции в пределах счёта

Статус: подготовлено в исходниках; production-выпуск и банковская приёмка ожидаются.

Для действующего SQLite/D1 v52 уникальный ключ операции Точки — `(connection_id, provider_account_id, provider_transaction_id)`, согласованный с уже существующим account-scoped ID. Старый индекс без account допускается только как распознаваемый источник транзакционной миграции; неизвестное определение останавливает bootstrap. Миграция не меняет строки банка, суммы, provenance, связи и ручную классификацию. После импорта нельзя автоматически сужать индекс при откате кода.

При ошибке sync_commit планировщик сохраняет только фиксированный `commitFailureKind`, никогда сообщение исключения. Lease, доступ, credential generation, пауза и backoff остаются прежними. Локально воспроизведённая коллизия не объявляется установленной причиной live-сбоя без отдельного доказательства. Приёмка требует реальных операций по четырём счетам и сверки ДДС. Mini-spec и проверка миграции: `docs/acceptance/2026-09-09-tochka-account-identity.md`.



## D-098 — R14 для сохранения операций Точки

Дата: 2026-09-09. Владелец — Виталий; продолжение разрешённого исправления и публикации. Цель — доставить D097, согласующий уникальность банковской операции с её счётом, и получить реальные операции в реестре и ДДС. PR397 объединён в `e74e41d9b6d49f8854e8ad50b0cffc0a6b2cde80` после четырёх успешных exact-head gates. Последнее наблюдение production 17:30Z: четыре счёта, 12 READY imports, ноль bank/finance rows, failureStage=sync_commit; следующий обычный запуск 22:01:53.357Z. Коллизия воспроизведена локально, точная причина live exception пока не доказана.

Новый выпуск закреплён на PR398, branch `codex/arthello-r14-20260909`, parent `e74e41d9b6d49f8854e8ad50b0cffc0a6b2cde80`. Принятый live baseline — R13 source `f5fa3e46e3510e6fc98ae4455f4b499c0ba30695`, producing run34340461537, image `sha256:99b77401c79bb439d3ecbc06d6895b22aae311b3264c27be7dcdf9fabb4b3554`. Backup service остаётся принятым R12; его история и данные сохраняются. Перед cutover требуется свежий snapshot и точная проверка всех canonical consumers. Остановленные R12 и R11 predecessors допускаются только каждый со своим проверенным историческим receipt. Любой другой consumer блокирует выпуск.

R14 воспроизводит принятый R13 controller через закреплённую трансформацию, новые history/live/adoption adapters и natural browser wrapper. Сам frozen R13 не перезапускается. Выполненные active R13 deploy/verify workflows заменяются R14 парой; R12 и R13 contracts остаются обязательными отдельными jobs на exact source с архивными YAML comparison fixtures. Active budget14 не увеличивается. Retire helper касается только точного unused R13 browser image при двух свежих recoverability проверках; app/worker/containers/volumes не удаляются.

Сохраняются auth, scope четырёх счетов, даты, scheduler lease/generation/backoff и все границы публикации. После potential seal, candidate authentication или public start запрещено восстановление старых данных; rollback кода не сужает account-scoped индекс. До production запуска необходимы review итогового SHA/tree/diff и успешные exact-head Quality, Proof, v52 и все три R14 verification jobs. Production PASS и финансовая приёмка объявляются отдельно по фактическим receipts и обычному банковскому запуску. Stop: source/identity drift, нарушение backup/data boundaries, недостаточная capacity, неуспешные проверки либо отказ владельца.

